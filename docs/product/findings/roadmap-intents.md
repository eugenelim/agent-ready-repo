# Roadmap Intents

Deferred roadmap items that are not yet specs — things the team intends to
build but has not yet shaped or scheduled.

**How entries arrive here:** when an owner explicitly decides to record an
out-of-scope finding that looks like a future feature or improvement (not an
RFC-level proposal). This is the register for that roadmap intent.

**How entries leave:** when an item is scoped into a brief and spec, update
`Disposition` to `→ spec/<slug>`. When an item is dropped, note the reason.

| Problem | Source | Surfaced by | Date | Priority | Disposition |
|---|---|---|---|---|---|
| The DECIDE response ladder carries only one direction of a claim/check mismatch. `narrow-the-claim` shrinks a claim to what a weak check reaches and sits on the cut axis, which is walked first under a stop rule — so an author whose claim outruns its check takes that answer and stops, moving the property out of contract, and the mirror answer (make the check reach the obligation, when some check can) is absent: `strengthen` occurs zero times in the skill. One companion: the fix axis locates surfaces but never states that a repair must assert the property rather than a consequence of it, so a repair can strengthen a check toward another consequence each round while every surface in the frontier stays green. Recommended as two refinements to existing rules rather than a new answer, since changing the answer set is Ask-first and naming the answers is a deferred criterion; the discriminator is already authored as deferred AC-0024, whose owning intent permits shipping the prose. | `packs/core/.apm/skills/work-loop/SKILL.md` `## Step 5. DECIDE` | eugenelim | 2026-09-13 | High | Open |
| After a review round only the semantic walk is obliged, and the shipped text negates the mechanized one. The authority record splits the frontier by mode — order 2 is "the class, not the instance. A sweep, not a reading", order 3 is companions that "go stale silently because nothing reads them", order 4 says "the same sweep belongs to a repair decided during review" — and records that the three historical defects "divide exactly along the walk-versus-sweep line", one found by a flat text search on a literal token and two only by reading neighbouring prose. DECIDE carries only the reading half: `sweep` occurs zero times inside it, and "This is a walk, not a text search" negates the literal mode across the whole frontier, including the two orders its own authority assigns to a sweep. The mechanized sweep does exist as the Anchor-test sweep (grep for `hashlib`, `sha`, content equality, `len(lines)`, counted assertions) but is obliged only before EXECUTE, so nothing re-fires it after a repair — which is when a prose edit reds a content pin in a file the author never opened. Fix: split the one-mode sentence so the literal and the paraphrasing halves each name their instrument, and oblige both after a round, re-firing the anchor-test sweep over the files a repair touched. | `packs/core/.apm/skills/work-loop/SKILL.md` `## Step 5. DECIDE`; anchor-test sweep at step 8a | eugenelim | 2026-09-13 | High | Open |
| Mutation-proof discipline has no owner in the core pack. Neither `work-loop`'s `SKILL.md` nor its reference files state a mutation-proof obligation: every occurrence of "mutation" is the state-change sense, apart from one "mutation-testing mindset" phrase inside the `quality-engineer` trigger. The rule that a proof must revert to the pre-fix implementation rather than a do-nothing stub therefore has no shipped home, and a loop run without an externally supplied supervisor protocol has nothing to fall back on. Distinct from the row above: that one refines how an answer is chosen, this one is an absent obligation on how a repair is verified. | `packs/core/.apm/skills/work-loop/` | eugenelim | 2026-09-13 | Medium | Open |
