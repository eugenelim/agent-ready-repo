# Project: <one-line outcome>

> **This is a template.** Copy it to `docs/product/projects/<slug>.md` and
> fill in what you have. A project is a per-repo, time-bounded outcome — a
> chunk of the roadmap bounded by a milestone and owned by this repo. Use
> `workspace-status` at session start to see the current queue state.

- **Slug:** `<slug>` <!-- kebab-case; matches the filename -->
- **Outcome:** <!-- one sentence: the user-facing or system change this project delivers -->
- **Appetite:** <!-- a constraint, not an estimate: "a few weeks, not a quarter" -->
- **Milestone:** <!-- the milestone this project belongs to, e.g. "M1 · Workspace Foundation" -->
- **Brief:** <!-- optional: `<brief-slug>` if this project is the execution of a received brief -->
- **Initiative:** <!-- optional: `INI-NNN` if this project rolls up to an initiative -->

## Milestone map

<!-- One row per milestone within this project. Keep it short — a project
usually has one milestone; add rows only when the project genuinely spans
multiple milestones in series. -->

| Milestone | Scope | Status |
| --- | --- | --- |
| M?.? | <!-- what ships in this milestone --> | <!-- Draft / In progress / Shipped --> |

## Briefs

<!-- Links to received briefs whose decomposed specs are tracked here. A
brief is the cross-feature input; this project is the per-repo execution
vehicle. One row per brief if multiple briefs feed this project. -->

| Brief | Status |
| --- | --- |
| [`<brief-slug>`](briefs/<brief-slug>.md) | <!-- auto from brief coverage lint --> |

## Notes

<!-- Optional: anything that doesn't fit above — key decisions, blockers,
open questions. Keep it minimal; the canonical records are in ADRs and specs. -->
