# Brief: an intent and a brief can be closed through a workflow, on recorded evidence

- **Slug:** `intent-lifecycle-and-closure`
- **Received:** 2026-09-23
- **Owner:** eugenelim, Platform Core maintainer
- **Status:** Executing
- **Parent intent:** intent:lifecycle-and-closure
- **Slice 1 started:** 2026-09-24. Slice 1, [`lifecycle-transition-contract`](../../specs/lifecycle-transition-contract/spec.md): its T3 corpus migration is delivered in commit `d00f39e34`. A material amendment to that spec — restating AC-0010, and adding no criterion — was reviewed to `Clean` over seven adversarial rounds and re-approved on 2026-09-24. That spec's status is the Spec map's to report, not this line's. **The cut was widened on 2026-09-24**, after a shaping review returned `Findings` against the delivery map: slice 3 takes the brief state table slice 1 did not ship, and slice 4 is added for the retention extension. Four slices; slices 2, 3 and 4 are not yet cut.
- **Ready confirmed:** 2026-09-23 by eugenelim, lifecycle owner. Taken on a `Findings` result, not a `Clean` one — recorded here because `author-delivery-brief` expects a revision-bound `Clean` and this transition did not have one. Six delivery-brief shaping reviews ran. The first found two Blockers, both resolved by moving work rather than arguing: the supersession split went to [`intent-identity-and-registration`](intent-identity-and-registration.md), which owns the shipped spec it touches. The five rounds after that returned no Blocker. The last returned two Concerns and one Nit; all three are fixed above and were not re-reviewed. The owner judged the contract sound and the residual findings to be authoring craft rather than contract defects.
- **Source / provenance:** Mode `repo-origin`; locator [`docs/product/intents/FEAT-0005-lifecycle-and-closure.md`](../intents/FEAT-0005-lifecycle-and-closure.md), de-risked 2026-09-23. Authored under [ADR-0119](../../adr/0119-retire-the-initiative-ladder-into-the-recursive-intent-graph.md).

## Outcome

An intent or a brief whose work is delivered, abandoned or superseded reaches a terminal status through a workflow rather than a hand edit, and the decision is recorded with the evidence it rested on. A reader of the open list sees live bets. A reader of a closed artifact sees who closed it, when, and on what basis.

## Current-state evidence

The parent intent's § Where closure stands today owns the measurement and its date. It establishes four things this brief builds on: no workflow can set `Fulfilled`; the intents that carry it were set by hand and almost none records why; most of those never reached `Accepted`, so they already sit in a state the transition rule would forbid; and the gate governing a closure gets lighter as the claim gets bigger, with the intent exit gate absent entirely.

Two further facts are this brief's own.

- **The field contract this brief gates transitions on already exists.** [`intent-metadata-shape-contract`](../../specs/intent-metadata-shape-contract/spec.md) establishes the closed `Status` vocabulary, the three shaping-progress fields (`De-risked:`, `Shaping-reviewed:`, `Decomposed:`) each validated when present, and preamble bounding under AC-0011. It routes the transition rule here. It is a spec, so it is cited here rather than in § Governance references, which carries RFCs and ADRs only.
- **Those fields were populated on 2026-09-23**, wherever the fact was verifiable from the artifact or from git, and left absent everywhere else. Absent and `no` are different claims: absent records that nobody decided, `no` asserts a decision against. § What sits outside the loop owns what that route means and what stays open.

## Scope / Non-goals

**In scope.**

- The state table and legal transition set for an intent and for a brief, including which workflow may make each move and what the three declared shaping-progress fields mean for a transition.
- `Draft` narrowed to mean open, and the rule that no transition may be gated on anything read out of an artifact body rather than from a declared preamble field.
- The three-way classify at a closure check — refuse, not eligible, eligible — with a refusal naming the precondition it is missing.
- The evidence packet presented to the human who decides, and the closure record written afterwards.
- The staleness refresh: descendants re-read rather than remembered, the base current at the closing edge, and the artifact's own claims revalidated.
- The cut-closed declaration on a brief.
- One reader-facing guide page covering the states, the transitions and the closure flow, extended by each slice as part of that slice's completion. This is this brief's own scope decision, not an inherited obligation.
- Migrating the intents that reached `Fulfilled` without `Accepted`. Each is walked back through `Accepted` or reclassified to a terminal state it qualifies for. There is no recorded-exception outcome, by owner decision of 2026-09-23; the parent's § Legal transitions owns the ground. The parent's decomposition decision places the migration inside the transition slice's completion, because the rule may not ship leaving the corpus in a state it forbids.
- Every slice here changes `packs/core/` content, so each carries that pack's standing release obligations once rather than restating them per slice: a matching version bump in `pack.toml` and `.claude-plugin/plugin.json`, an update to the skill's eval harness for any non-cosmetic change, and a changelog entry. A release that changes an adopter's obligation also owes a free-standing `### Highlights` block, never nested under `[Unreleased]`.

**Non-goals.**

- **Owning the derived graph as a published surface.** The parent capability's, delivered by [FEAT-0002](../intents/FEAT-0002-intent-graph-navigation.md) and [FEAT-0003](../intents/FEAT-0003-intent-delivery-traceability.md). **Narrowed 2026-09-24**, when the parent's § Boundary moved **computing eligibility for a closure decision** into this feature — the largest ownership change this brief has taken, made because both those intents are `Status: Draft` with `Decomposed:` absent and the closure check could not wait on them. What remains excluded is **persistence, publication, and any surface a second consumer reads**. What is now in scope is the closure check constructing an **in-memory descendant set** at decision time, under the three conditions the parent's § Boundary states and owns — see there; they are deliberately not reproduced here. The walk is the **full descendant closure**, not one hop — § The lifecycle decides that, not this slice. So the check may build and walk that set per decision; it may not become the repository's rollup.
- **Defining an *intent* preamble field's shape — its name, its value vocabulary and its validation.** [FEAT-0001](../intents/FEAT-0001-intent-identity-and-registration.md)'s, under the parent's § Boundary, which the owner amended on 2026-09-23 to say the boundary covers intents only. A **brief's** preamble field is not excluded: the cut-closed declaration's shape and its preamble bounding are slice 3's. The parent's § Boundary owns the ground. This brief decides what a field's value means for a transition, for both artifact kinds.

  A brief's preamble bounding is slice 3's third instance of a pattern that already exists for intents and for specs; that slice's Size paragraph names both.
- **Retention or disposal of a closed record.** This non-goal was conditional on `close-work`'s existing disposition contract covering a product bet, and **the condition failed**: the parent's § Validation hook owns the reading and its evidence, settled 2026-09-24. So the retention extension is **in this brief's scope**, and § Candidate delivery slices carries it as slice 4 — a later slice rather than a candidate inside the cut confirmed on 2026-09-23, because that cut was confirmed while A4 was still open. This is a material change to the delivery map: the Ready review bound in the preamble no longer covers this section, and the fresh review it owes runs when `author-delivery-brief continue` next presents a cut. `Status` does not return to `Draft` — § Brief lifecycle derives it from child state, not from review evidence.
- **Outbound tracker projection.** [CAP-0004](../intents/CAP-0004-external-tracker-projection.md)'s surface.
- **Deciding closure automatically.** The parent's § Outcome › Guardrail.

## Constraints / Appetite

Five constraints carry forward from the parent, which owns the reasoning and the case behind each. They are pointers, not copies — the parent's C1 in particular splits ownership in a way a one-line restatement loses.

- **C1** and **C2** — the parent's § De-risk record › Constraints carried forward. C1 governs when the check must refuse a carve-out; C2 forbids a verdict on an empty child set. Both are refusal conditions a delivery slice implements.
- The parent's **§ Outcome › Guardrail** forbids a status set by a count, its **§ The lifecycle** step 3 requires refuse and not-eligible to stay distinct, and its **§ Staleness refresh at closure** forbids reading any of it from cache.

No appetite ceiling is set, by owner decision of 2026-09-23: the capability has to land whole, so a scope cap would cut something the outcome needs rather than protect the cut. § Candidate delivery slices sizes each slice instead, to sequence them.

## Assumptions / Risks

- **[Survived, for one flat pass only]** A closure-time staleness refresh is affordable. The parent's A2 owns the measurement — 675 artifacts re-derived in 0.27 s warm — which establishes that nothing needs caching. It does **not** reach the cascade this design creates; the risk below owns that.
- **Risk: the per-descendant-transition cascade is an open cost.** Every descendant reaching a terminal state fires a closure check on each of its ancestors, and each such check walks the full descendant closure beneath that ancestor. The cost scales with tree depth rather than corpus size, so it grows as the ladder fills rather than as intents are added. The parent's § Unresolved questions owns the question and records it as cheap today and open; slice 2's spec owes the bound, measured on a synthetic tree at a stated depth because today's almost-undecomposed corpus is the degenerate case.
- **[Survived, with a gap]** Shaping-review condition 5 is stable and can fail, so `Accepted` is worth something as a precondition. Tested on a coarse gap only. A subtle-gap arm is owed.
- **[Killed, reframed, re-testable]** The evidence packet is self-sufficient only for an intent whose `Decomposed:` records a ratified delivery set. The parent's A3 owns the walk that killed the wider claim. The intent whose packet failed now carries `Decomposed:`, so the reframed assumption can be re-tested against the same case. That re-run is owed.
- **[Killed]** `close-work`'s disposition contract covers a closed intent's record without extension. No eligibility row reaches a bet that closed without delivering. The parent's § Validation hook owns the reading and its evidence; § Scope / Non-goals owns what the kill costs.
- **Risk: the not-eligible branch has never run.** The retrospective walk produced refusals and eligible verdicts but no not-eligible, because nothing in the corpus has live work recorded beneath it. It is the normal case once the ladder fills and the only path with no evidence behind it.
- **Risk: the check refuses most of the corpus for a long time.** Most intents have not been decomposed, so `Decomposed:` is legitimately absent and the check refuses them by design. Correct behaviour, and also almost all of the behaviour until the ladder fills.
- **Risk: the migration is a judgement per artifact.** Whether each one is walked back through `Accepted` or reclassified cannot be decided mechanically.

## What sits outside the loop, and what does not

`work-loop`'s own description sets this boundary. The risk-trigger block does not: it chooses between light and full **once the work is already in the loop**, so it cannot decide membership, and this section does not use it for that.

That description names what belongs in the loop — including **migration** — and names what does not, including **shaping**. Both halves matter here, because the same field can be written by either kind of work.

- **Writing a shaping-progress field on one intent, as part of that intent's own shaping, is shaping.** `decompose-intent` recording its own terminus is that skill's output. Excluded from the loop by name, and this is the shape the fields are meant to be maintained in.
- **Rewriting the field across the corpus in one pass is a migration.** It is not a by-product of shaping any single intent; it is a change to many artifacts made for its own sake, which is the include list's own term. That puts it in the loop.

The rule's live consumer is slice 1. Its migration walks a set of intents through a status decision each, which is a corpus migration and therefore in-loop work under the same rule.

The 2026-09-23 population described in § Current-state evidence was also the second kind and ran as though it were the first. Its disposition is closed; what remains open is the standing obligation on the shaping skills, which the parent's § Unresolved questions owns.

Mode selection for the slices below is not made here. `work-loop` selects it at the change in hand.

## Candidate delivery slices

Enumerated, not confirmed — and the two are different acts. The candidates are listed because [ADR-0077](../../adr/0077-feature-projection-and-tracker-authority.md) D1 routes on how many independently shippable changes there are, so the count has to exist for this artifact to be the right one.

**If the confirmed cut is one slice, this brief is retired.** D2 permits a one-spec brief only as a repository projection of a cross-repository feature, and this feature is single-repository. The trigger is the **slice-cut confirmation**, where `author-delivery-brief continue` presents the proposed cut as a set and the lifecycle owner confirms it; a confirmed set of one retires the brief there and the feature routes directly to a spec under D1. The owner performs the retirement: the brief moves to a terminal collection through `close-work`, and the surviving slice goes to `new-spec` with this brief's scope as its context. The rule lives here rather than in § Ready gaps because that section is dropped on leaving `Draft` and this trigger fires after that.

"That the cut finished at one" is not itself observable — [ADR-0098](../../adr/0098-artifact-admission-and-delivery-brief-lifecycle.md) D6 confirms *a* slice at a time, so a brief can materialize one and stop, and no brief can yet declare its cut closed. The confirmation moment is what makes the count readable. The **cut** is made *after* the Ready transition, as a separate human confirmation — [ADR-0098](../../adr/0098-artifact-admission-and-delivery-brief-lifecycle.md) D5 makes Ready its own confirmation and D6 lets a Ready brief carry zero specs, so the cut cannot be a Ready precondition. Nothing here binds it.

The guide page named in § Scope is not a slice of its own.

The cut is confirmed as a **set of four**, which is what makes the count readable for ADR-0077 D1 and D2. Materializing is separate: under [ADR-0098](../../adr/0098-artifact-admission-and-delivery-brief-lifecycle.md) D6 each slice is selected and materialized under **its own** human confirmation, so no two slices are materialized in one pass.

1. **The intent transition contract.** The state table and legal move set **for an intent**: what the three declared fields mean for a transition, `Draft` narrowed to open, and the migration of the intents that reached `Fulfilled` without `Accepted`. It carries neither the cut-closed criterion nor the **brief** half of the table — both are slice 3's, the latter moved there on 2026-09-24 because the fourteen criteria this slice carries, AC-0001 through AC-0014, are each about an intent. It does **not** carry the supersession split. The parent handed that to [`intent-identity-and-registration`](intent-identity-and-registration.md)'s § Post-Ready decisions, which owns both the spec it touches and, under the parent's § Boundary, an intent preamble field's shape. Its state table consumed whichever supersession form had landed; there was no blocking edge between the two. Verification has to observe a transition, which is a pair of states, so a single-snapshot corpus scan cannot establish it on its own.

   **Size.** The largest slice. Measured 2026-09-23 against the working tree. It extends `intent_corpus_lint.py` with a transition rule and walks ten intents through a status decision each. The nearest comparables carry heavy criteria sets — `intent-metadata-shape-contract` 27 acceptance criteria, `checkable-adr-metadata` 32 — and this slice is the same kind of work: a field-and-state contract with a blocking gate. Expect that order.

2. **The closure check in `close-work`.** Trigger, gather-at-decision-time, three-way classify, the evidence packet, the human decision point, and the closure record. The disposition lookup is optional and must handle an absent product-bet disposition; the parent's § Unresolved questions owns that decision. Depends on slice 1 for the meaning of terminal, and on `Decomposed:` for a ratified delivery set. **How it obtains child state is settled, 2026-09-24 by eugenelim, lifecycle owner: the check resolves each descendant's `Status` from the artifacts at decision time and persists nothing.** This required moving eligibility computation into the parent intent's § Boundary for this child, which the owner recorded there the same day; the earlier reading settled the mechanism against this brief's § Non-goals alone and did not clear that boundary, which is the governing obligation. The alternative — invoking [FEAT-0002](../intents/FEAT-0002-intent-graph-navigation.md)/[FEAT-0003](../intents/FEAT-0003-intent-delivery-traceability.md)'s walker — was declined on availability: both are `Status: Draft` with `Decomposed:` absent, measured 2026-09-24, so it does not exist and nothing is scheduled to build it. **No ordering edge runs in either direction**, and the parent's § Boundary owns the three conditions that keep the parent capability whole. They are not reproduced anywhere in this brief, so they cannot drift from the section that owns them.

   **Child-set discovery is this slice's, and is the work the mechanism choice does not cover.** `Decomposed:` does not enumerate children — `intent_shape._check_decomposed` admits the literal `no`, or an ISO date plus exactly one terminus from `children`, `brief`, `spec`, `direct-light`. It records *that* a delivery set was ratified and *what shape* it takes, and names no member. So the check must build the member set itself, by inverting the declared up-edges (`Parent intent:` and the delivery back-links) across the corpus at decision time. **It is the full descendant closure, not one hop.** The parent's § The lifecycle already decides the depth — its trigger fires on *ancestors*, its not-eligible verdict must name *which descendant* is live, and its eligible verdict requires *everything below* to be terminal — so depth is not a spec author's choice, and the not-eligible fixture below must be deeper than two levels to exercise it. A2 establishes only that a single flat re-derivation is free — 675 artifacts, 0.27 s warm — so nothing needs caching. It does **not** settle this design's cost: the parent's § Unresolved questions records the per-descendant-transition cascade as open and depth-scaling, and it stays open there. This slice's spec owes a bound with a named unit and a named case: a maximum number of artifacts read per closure decision, measured against a synthetic fixture tree at a stated depth rather than against the live corpus. Measuring on the live tree would pass for the wrong reason — the corpus is almost entirely undecomposed, which is the degenerate case. **That list is built per decision and persisted nowhere**, which is what keeps it inside the amended boundary rather than inside the published surface § Non-goals excludes. Recording this closes a gap the mechanism decision left open: without a defined child set, C2 — an empty child set is a refusal, not a pass — is undecidable, because "empty" is unobservable until "the set" is defined. The spec must state the inversion's source fields and its per-decision lifetime as criteria. **Slice 2's brief path consumes slice 3's table.** Terminal for an intent is slice 1's and shipped; terminal for a **brief** is slice 3's, and the parent's § Briefs take the same flow makes a brief's child set its mapped specs, so steps 2 to 5 are identical. Either specify the brief path after slice 3, or consume the shipped `_BRIEF_STATUSES` in the interim exactly as slice 1 was allowed to consume the shipped `Superseded by <slug>` form. **And if slice 4 lands first**, the live contract no longer produces an absent product-bet disposition, so slice 2's absent-disposition handling must be pinned by a fixture rather than by the live contract.

   **Whether the check touches `workspace.toml` is traced, 2026-09-24. The answer splits by path, and only one half is inherited.** [CAP-0003](../intents/CAP-0003-workspace-coordination-reorganization.md) exists to stop repository coordination depending on that registry, so the trace mattered. **The brief path is inherited:** `close-work`'s closeout procedure step 1 already makes updating a brief and moving its exact workspace entry to the matching terminal collection one separately confirmed effect, and its § Workspace and record discipline already keeps a completion receipt on the citing local need there. Slice 2 adds no new class of write on that path. **The intent path is not:** an intent's registration sits in `[backlog].open` — 109 of them, measured 2026-09-24 — and `close-work` has no writer for that collection today. Slice 1 retired this feature's own entry **by hand**, which is the evidence the automated path does not exist. That surface is `work-intake`'s, and `close-work`'s hard stops forbid creating a lifecycle database or global surface registry, so **the spec may not invent a second writer**: it must either route the move through the surface's owner or carry that owner's recorded confirmation that the new writer belongs to `close-work`. Obtaining it is a precondition on slice 2's spec, not a spec-time judgement. **The two are not ordered** — CAP-0003 is `Status: Draft` with `Decomposed:` absent, measured 2026-09-24, and ordering slice 2 behind it would park this work behind a capability not yet shaped to deliver anything; CAP-0003 migrates both paths when it lands. The slice must still move an artifact's `Status:` line and its `workspace.toml` collection membership in the same change, because the canonical reconciliation defined in [`workspace-queue-reconciliation`](../../specs/workspace-queue-reconciliation/spec.md) raises `impossible_transition` on a mismatch.

   **C1 and C2 take an ordering edge on [FEAT-0001](../intents/FEAT-0001-intent-identity-and-registration.md), because neither has an observable to key on today.** C1 refuses a close where a carve-out changed neither the Outcome nor named a co-owner; the parent assigns defining that mark to FEAT-0001 and the refusal to this child. Measured 2026-09-24, `intent_shape.py` declares `Status`, `Accepted`, `Fulfilled`, `De-risked`, `Shaping-reviewed` and `Decomposed` and no carve-out or co-owner field, so the only surviving signal is body prose — which § Scope forbids gating a transition on. C2's legitimate-exception arm needs a declared closed-empty marker and has the same defect. **This edge is real and blocking**, unlike the FEAT-0002/FEAT-0003 one declined above: FEAT-0001 is `Status: Accepted`, `Decomposed: 2026-09-19 brief`, and its brief is `Executing` with a § Post-Ready decisions section that takes inbound handoffs. Record both fields there before slice 2 is specified; slice 2 consumes them and defines neither.

   **Slice 2's spec must carry the not-eligible branch's first evidence.** That branch has never run: the retrospective walk produced refusals and eligible verdicts only, because nothing in the corpus has live work recorded beneath it. The parent's § Validation hook carries constructing a case as its Second owed activity, and nothing re-reads a validation hook at spec time, so the obligation is routed here instead: the spec carries at least one fixture whose tree has a live descendant, asserting the verdict is not-eligible and names that descendant. The Size line's fixture-tree instrument is what makes this affordable.

   **The decisions above are material changes to this brief's delivery map, so the `Ready` review bound in the preamble no longer covers this slice.** `Status` does not return to `Draft`: § Brief lifecycle derives `Executing` from child state rather than from review evidence, and forcing `Draft` would assert this brief never passed its Ready gate, which it passed on 2026-09-23. The fresh review runs when `author-delivery-brief continue` next presents a cut.

   **Size.** Middle, measured 2026-09-23. One workflow change inside `close-work`, no new artifact field, and verification by fixture tree rather than corpus migration. Smaller than slice 1 because it adds no contract other artifacts must satisfy.

3. **The brief state table and the cut-closed declaration.** Widened 2026-09-24 by eugenelim, lifecycle owner, and the widening is a correction rather than an addition: slice 1 delivered the intent half of the table only, so the brief half was unowned. This slice now owns **the brief state table and its legal transition set**, alongside the field saying no further slices are coming, that field's shape and preamble bounding, and `close-work`'s brief path reading it. It must reconcile with the brief state machine already shipped **outside this brief's slices**: `lint-brief-coverage.py` hardcodes `_BRIEF_STATUSES` at line 61 and `_brief_lifecycle_is_valid()` at line 156, inside `author-delivery-brief`. The spec decides whether to absorb that surface or defer to it, and says which; what it may not do is add a third home. **Its refusal criterion lands in `lint-brief-coverage.py`**, which is a real fail-closed exit-1 gate; the `close-work` brief-path reader is a prose recommendation and is advisory, so it cannot carry the criterion. **Verification splits by half.** The cut-closed field is a resting-state predicate, so `lint-brief-coverage.py`'s existing shape decides it. The legal transition set is not: a transition is a pair of states, so a single-snapshot corpus scan cannot establish it on its own — the same reasoning slice 1 carried, which travelled here with the table's scope and must travel with its verification too. How many surfaces that takes and where they sit is the spec's to decide. **Independent of slice 2**, for a reason worth stating exactly: `close-work` already has a brief-closure path today — step 1's non-empty materialized Spec map fully Shipped, plus human confirmation — so this slice extends behaviour that exists rather than behaviour slice 2 creates.

   **Size.** No longer the smallest; the brief state table moved here on 2026-09-24 and it now carries a contract other artifacts must satisfy. The preamble-bounding half is still the third instance of a pattern that exists twice — `intent-metadata-shape-contract` AC-0011 bounds an intent's preamble and `lint-spec-status.py` bounds a spec's, and nothing bounds a brief's. Investigate both before choosing which to mirror. Core-pack only. **The spec must say whether the declaration is required on every brief or only on a brief being closed**, and whether adoption carries a brief-corpus migration across the 18 briefs — the earlier draft's "one additive field across the brief corpus" asserted the wider answer without deciding it. The parent's rule for slice 1 applies unchanged: the rule may not ship leaving the corpus in a state it forbids. **A field-shaped line placed below the preamble block parses as absent rather than as misplaced**, so a misplaced declaration is invisible and the lint reads clean either way: verify placement by reading the field back through the parser, never by eye.

4. **The retention extension to `close-work`'s disposition contract.** Added 2026-09-24 by eugenelim, lifecycle owner, when A4 settled against coverage and fired the conditional non-goal in § Scope. It adds the eligibility row for work that closed **without delivering**. The load-bearing reading: no existing eligibility row reaches that state, so the contract's fall-through refuses classification — a refusal, not a disposition. `packs/core/.apm/skills/close-work/SKILL.md` § Disposition contract owns the rows and the parent's § Validation hook owns the reading and its evidence; neither is restated here, because both change without this copy changing. **Its own slice rather than part of slice 2**, under [ADR-0077](../../adr/0077-feature-projection-and-tracker-authority.md) D1: it is verifiable today against the 6 `Withdrawn` intents measured on 2026-09-24 and the fall-through, with no dependence on the closure check existing. Sharing the `close-work` skill with slice 2 is not shippability, and bundling them would put two independent verification surfaces in one contract.

   **Size.** Small. One eligibility row and its refusal fall-through. No new artifact field and no corpus migration. Measured 2026-09-24.

## Governance references

- [ADR-0119](../../adr/0119-retire-the-initiative-ladder-into-the-recursive-intent-graph.md) — retired the Initiative ladder into the recursive intent graph, which created the closure gap this brief fills.
- [ADR-0077](../../adr/0077-feature-projection-and-tracker-authority.md) — D1 routes a feature by shippability: one independently shippable change becomes a spec, several become a brief. That is why this artifact is a brief. D2 sets the one-spec floor that a single-slice cut would breach, which § Candidate delivery slices carries along with its trigger. D1 is superseded in part by ADR-0098, whose Clause-level replacements carry what moved: the feature-projection table is refined so sufficient direct artifact authority may bypass feature-intent *creation*. The shippability routing is untouched and D2 is not superseded.
- [ADR-0098](../../adr/0098-artifact-admission-and-delivery-brief-lifecycle.md) — the record that most constrains this brief. D5 makes the Ready transition its own human confirmation. D6 lets a Ready brief carry zero specs and makes selecting and materializing a slice a separate confirmation, which is why the cut sits after Ready and why the Spec map is empty here. D7 separates governance references from delivery slices, so only specs enter a rollup. It is superseded in part by ADR-0121, which replaces its D3 and reaches none of these three.
- [ADR-0121](../../adr/0121-a-repository-intent-declares-its-altitude.md) — D1 requires `Level` on a repository intent and replaces ADR-0098 D3's grouping of `level` with optional enrichment. It closes the supersession chain above: both ADR-0077 holdings this brief relies on are live.

## Spec map

Two of four slices materialized. The Status column is auto-derived from each spec; it is not hand-edited.

| Spec | Status |
| --- | --- |
| `lifecycle-transition-contract` | <auto> |
| `brief-lifecycle-contract` | <auto> |
