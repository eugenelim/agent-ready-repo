# Requests For Comments

> Proposals for change. See
> [`../CONVENTIONS.md`](../CONVENTIONS.md#3-rfc--request-for-comments--docsrfc)
> for when to open an RFC vs. an ADR vs. just opening a PR.

| #    | Title | Status | Opened     | Closed |
| ---- | ----- | ------ | ---------- | ------ |
| [0001](0001-bundle-distribution-by-adapter-spec.md) | Bundle distribution by adapter spec + ecosystem build pipeline | Accepted | 2026-05-21 | 2026-05-22 |
| [0002](0002-self-hosting.md) | Self-hosting via the ecosystem build pipeline | Accepted | 2026-05-21 | 2026-05-22 |
| [0003](0003-spec-and-cli.md) | Adapter contract publication + reference CLI | Accepted | 2026-05-21 | 2026-05-22 |
| [0004](0004-install-scope-per-pack.md) | Install-scope dimension — repo or user — defaulted and constrained per pack | Accepted | 2026-05-23 | 2026-05-23 |
| [0005](0005-user-scope-hook-support.md) | User-scope hook support — body reroot + wiring merge mode | Accepted | 2026-05-23 | 2026-05-25 |
| [0006](0006-skill-secrets-storage.md) | Credential storage for credentialed skills — tiered env/keyring/dotfile + two-layer architecture | Accepted | 2026-05-24 | 2026-05-24 |
| [0007](0007-user-scope-converter-pack.md) | First user-scope pack — `converters` (file-to-markdown, markdown-to-html, msg-to-markdown) | Accepted | 2026-05-24 | 2026-05-24 |
| [0008](0008-claude-plugins-install-route-parity.md) | Claude-plugins install-route parity — per-pack SessionStart writer for the install→adapt chain | Accepted | 2026-05-24 | 2026-05-25 |
| [0009](0009-codex-native-skills.md) | Migrate Codex adapter from managed-block AGENTS.md to native `.agents/skills/` | Accepted | 2026-05-25 | 2026-05-25 |
| [0010](0010-apm-install-route-parity.md) | APM install-route parity — per-pack SessionStart writer projected through APM's HookIntegrator | Accepted | 2026-05-25 | 2026-05-25 |
| [0011](0011-pack-allowed-adapters.md) | Per-pack `allowed-adapters` declaration for user-scope adapter resolution | Accepted | 2026-05-25 | 2026-05-26 |
| [0012](0012-repo-scope-per-adapter-projection.md) | Repo-scope per-adapter projection — `--adapter` at `--scope repo`, dist-tree opt-in via `--emit-install-routes` | Accepted | 2026-05-26 | 2026-05-26 |
| [0013](0013-credential-broker-contract.md) | Credential broker contract — in-process shim for static tokens, adapter-root subprocess for SSO; formalise four-broker model | Draft | 2026-05-26 | |
| [0014](0014-answer-first-rfc-format-and-drafting-flow.md) | Answer-first RFC format + research-and-de-risk drafting flow for `new-rfc` | Accepted | 2026-05-28 | 2026-05-28 |
| [0015](0015-wave-scheduled-supervisor-mode.md) | Wave-scheduled supervisor mode — topological-order default, parallel writes opt-in and gated on measured file-disjointness | Accepted | 2026-05-28 | 2026-05-29 |
| [0016](0016-doc-drift-mechanical-gate.md) | Doc-drift prevention — construction + judgment (template / reviewer / DECIDE / CONVENTIONS / in-repo backlog), with a catalogue-governance linter | Draft | 2026-05-29 | |
| [0017](0017-pluggable-api-contract-standards.md) | Pluggable API-contract standards (base+delta, Zalando as starter) + a contract-type-agnostic `new-spec` seam | Accepted | 2026-05-30 | 2026-05-30 |
| [0018](0018-event-asyncapi-authoring-engine.md) | Event/AsyncAPI authoring engine for `contracts` — `event-contract` skill (AsyncAPI 3.1.0 + CloudEvents 1.0.2 envelope, Zalando event rules as starter) | Accepted | 2026-05-31 | 2026-05-31 |
| [0019](0019-product-brief-intake.md) | Product-brief intake + LLD-aware spec/plan — `brief` artifact + `receive-brief` skill (own-the-repo-slice) and a stack-neutral, stack-derived `## Design (LLD)` in spec/plan | Accepted | 2026-06-01 | 2026-06-01 |
| [0020](0020-reference-architecture-foundation.md) | Reference-architecture foundation — normative `docs/architecture/reference.md` golden path (arc42), **template-instantiated on demand** (not core-seeded — avoids guaranteed stack-pack collisions); populated by stack pack / brownfield harvest / greenfield `init-project`; the LLD's steering | Accepted | 2026-06-01 | 2026-06-01 |
| [0021](0021-greenfield-inception.md) | Greenfield inception — the idea→repo front-door (research + value gate → foundation → walking skeleton), the greenfield twin of `adapt-to-project`; composes brief/foundation/spec, declines autonomous generation | Accepted | 2026-06-01 | 2026-06-01 |
| [0022](0022-kiro-adapter-split.md) | Split `kiro` adapter into `kiro-ide` (default, VS Code-fork IDE) and `kiro-cli` (terminal binary); `kiro` kept as deprecated alias; contract v0.8→v0.9; errata to RFC-0005 | Open | 2026-06-01 | |
| [0023](0023-credential-manager-broker.md) | `credbroker` — replace RFC-0013's build-projected stdlib shim with a standalone pip-installable in-process credential library (stdlib core + optional `[crypto]` encrypted vault); phased repo-path→PyPI; env Tier-1 kept as the pip-free floor; daemon/proxy (authsome) out of scope; **reverses ADR-0003** | Accepted | 2026-06-03 | 2026-06-03 |
| [0024](0024-copilot-subagent-projection.md) | Copilot full-parity projection — agents + hooks `dropped`→first-class, user-scope-capable (`~/.copilot/`), packs adapted (`research`/`core`); contract v0.9→v0.10; **supersedes RFC-0012 copilot-scope decision** | Accepted | 2026-06-04 | 2026-06-04 |
| [0025](0025-work-loop-light-mode-and-risk-based-escalation.md) | `work-loop` light mode (lean inline spec + single adversarial pass + no state machine) as the default + risk-based escalation replacing the file-count trigger; subtraction-shaped, no new skill/script/artifact | Accepted | 2026-06-05 | 2026-06-05 |
| [0026](0026-cursor-full-parity-adapter.md) | Cursor full-parity distribution adapter — project all primitives to `.cursor/*` (+ `~/.cursor/*`); first-class commands (`.cursor/commands/`), single-file `hooks.json` merge, agent tool-allowlist degraded to `readonly`; contract v0.10→v0.11; distribution-only | Accepted | 2026-06-11 | 2026-06-11 |
| [0027](0027-gemini-cli-full-parity-adapter.md) | Gemini CLI full-parity distribution adapter — project all primitives to `.gemini/*` (+ `~/.gemini/*`); keep+map agent `tools:` allowlist, tier-preserving model map (`opus→gemini-2.5-pro`/`sonnet→gemini-2.5-flash`/`haiku→gemini-2.5-flash-lite`), new `gemini-command-toml` mode (TOML commands), `AGENTS.md` `context.fileName` bridge, zero-drop hook-event mapping; distribution-only | Accepted | 2026-06-11 | 2026-06-11 |
| [0028](0028-tdd-stub-generation-in-the-core-loop.md) | TDD stub generation woven into the core loop — a load-on-demand `work-loop/references/tdd-stubs.md` turns each TDD-mode acceptance criterion into a compilable, validated red stub in `plan.md` during PLAN's *Design tests up front* step, consumed by EXECUTE; `new-spec` feeds it via a testability self-check. No new skill, no new gate, no new artifact | Accepted | 2026-06-11 | 2026-06-12 |
| [0029](0029-strengthen-security-reviewer.md) | Strengthen the `security-reviewer` — current multi-framework checklist (OWASP Top 10:2025 / ASVS 5.0 / API Top 10:2023 / LLM Top 10:2025 / CWE Top 25 / LINDDUN) delivered via an orchestrator-loaded progressive-disclosure `security-checklists` skill (depth without prompt bloat, ships to every adapter), plus a shift-left spec-stage secure-design mode wired into `work-loop` pre-EXECUTE; three-bucket tool-delegation with Tier-1 absence behavior; repo-convention awareness via `AGENTS.md`+inference (no new file); no contract change | Accepted | 2026-06-12 | 2026-06-12 |
| [0030](0030-product-engineering-pack.md) | A `product-engineering` pack — level-agnostic product shaping (a recursive `intent` tree) that de-risks via a choosable prototype-approach and decomposes into the specs `core` builds; brief = feature-intent projected onto a repo (`receive-brief` stays in `core`); contract maturity behavioral→interaction→wire-at-spec; one-way tracker projection (Linear/Jira Align/none); v1 app-scale + single-component, with a Backstage-anchored cross-component value-stream meta-repo specified for phase 2 | Accepted | 2026-06-12 | 2026-06-13 |
| [0031](0031-catalogue-package-manager-posture.md) | Package-manager *posture* for the pack catalogue — `pack.toml` as the rich superset source of truth with lossy per-tool marketplace projection; first-class metadata fields (readme/license/maintainers/links/categories/keywords) + `[pack.metadata.<tool>]` hatch; `@catalogue/pack` scoped identity; soft (warn-not-error) `categories` vocabulary; infra (hosted registry/indexer) explicitly out. Sets direction + decomposition (enriched-manifest spec first; index-contract / virtual-catalogue / provenance as follow-on RFCs) | Accepted | 2026-06-13 | 2026-06-13 |
| [0032](0032-architect-design-reviewer-subagent.md) | A forked-context `design-reviewer` subagent for the architect pack — ships the convergence loop's *preferred* fresh-context reviewer rung (read-only `Read/Grep/Glob`, reuses `architect-review` rubrics) alongside the existing skill; records the reading that the charter's "three reviewers is the ceiling" scopes the core code-review lenses, not opt-in design-side review (no charter edit) | Accepted | 2026-06-14 | 2026-06-14 |
| [0033](0033-design-craft-pack.md) | A `design-craft` pack — framework-agnostic design *discipline* for interaction/visual designers (a new audience), opt-in user-scope; four skills (`aesthetic-direction`, `design-system-foundations`, `layout-and-information-architecture`, `design-critique`) + a shared `quality-floor` checklist; strictly stack-neutral (no React/CSS/Framer, no values tables) via two guardrails; all skills, zero agents; design intent the build consumes (the design-side twin of RFC-0030's product seam) | Accepted | 2026-06-14 | 2026-06-14 |
| [0034](0034-pack-profiles.md) | Pack profiles — a first-party-curated, named, **single-scope** set of packs installed in one command: a user-scope role toolkit (`solution-architect` = architect+research+contracts) or a repo-scope setup bundle (`full-ceremony` = core+governance-extras+user-guide-diataxis+monorepo-extras), **never mixing scopes**; defined as `profiles/<name>.toml` at the catalogue root (one file per profile, id = filename stem, declares `scope`), CLI-route only (no marketplace.json/self-host/build change); ordered deps-first with a scope-homogeneity + dep-completeness + order lint, one adapter pins the whole batch, no profile entity in state, no schema or adapter-contract bump; framed as RFC-0031-lineage distribution hygiene, not package-manager infrastructure | Accepted | 2026-06-14 | 2026-06-14 |
| [0035](0035-sso-cookie-auth-for-atlassian-pack.md) | SSO-cookie auth for the atlassian pack (Data Center) — wire the atlassian read skills (`jira`, `confluence-crawler`) to the already-shipped `sso-cookie` broker for SSO-only DC instances that block API tokens; reverses RFC-0013 § 9's deferral (whose "just a frontmatter change" premise is false — clients have no cookie path), scoped **DC-only** (Cloud cookie auth removed 2019) and **read-only-first** (writes need deferred XSRF handling); one-broker-with-`creds`-fallback erratum to RFC-0013, enterprise connection params in an adopter-editable `references/sso-config.toml`, ships **Experimental** until a live-DC validation transcript lands | Accepted | 2026-06-16 | 2026-06-16 |
| [0036](0036-markdown-to-office-publishing.md) | Markdown → Office publishing skills for the `converters` pack — three per-format skills (`markdown-to-docx` / `markdown-to-pptx` / `markdown-to-xlsx`) that **template-fill** a user-provided branded Office file (via `docxtpl` / `python-pptx` / `openpyxl`) rather than convert-from-Markdown, completing the pack's Office round-trip (`file-to-markdown` is the inverse); **Tier-1** dependency (declare/detect/fail-clean, mirroring `file-to-markdown`); template flow = detect-on-disk → confirm/elicit → explicit no-template opt-out fallback; **inspect-then-map** fill-point contract (docx Jinja tags + xlsx named ranges must pre-exist, pptx layouts are fill-ready by construction); PDF export + slide visual-QA loop are non-goals/follow-ons | Accepted | 2026-06-17 | 2026-06-17 |
| [0037](0037-pack-activation-evals.md) | Pack-level activation evals — a repeatable empirical way to test each pack by measuring whether its skills *trigger* on the right prompts; adopts the agentskills.io trigger-eval convention as a per-skill `evals/eval_queries.json` (`{query, should_trigger}`, near-miss negatives), references coverage from a new `[pack.evals]` block in `pack.toml`, and adds a `tools/run-pack-evals.py` runner that detects `Skill` activation via headless `claude -p` over 3 runs (`trigger_rate`); runs in its own scheduled/dispatch **report-only** CI workflow off the PR critical path; lint learns a trigger-only `evals/` layout; **Tier A (activation/selection) only**, output-quality grading (Tier B) deferred to a future RFC | Accepted | 2026-06-21 | 2026-06-21 |
| [0038](0038-align-adr-template-with-madr.md) | Align the ADR template with MADR conventions — add a `Rejected` status (a declined proposal is kept, not deleted), MADR 4.0 decision-roles frontmatter (`Decision-makers`/`Consulted`/`Informed`), and optional `Decision drivers` + `Confirmation` sections; lift the post-acceptance lifecycle discipline into the shipped skill; keep it lean (no MADR-full per-option pros/cons, decision stays answer-first); forward-only migration; `governance-extras` → `0.2.0` | Accepted | 2026-06-21 | 2026-06-21 |
| [0039](0039-research-project-mode-and-typed-artifacts.md) | Research **project mode** + typed, topic-named artifacts for the `research` pack — a second axis (depth × lifecycle) adding sustained, stateful **projects** (a 4-skill family `research-project-start`/`-digest`/`-synthesize`/`-check` + a folder with three explicit layers: raw `sources/` → a constructed-column `synthesis-matrix.md` + analytic `memos.md` middle layer the pack lacks today → a typed synthesis); episodic outputs renamed `<topic-slug>-<type>.md` (quick stays inline) with a one-release forward-only `research.md` legacy alias (10 consumers — 9 live + 1 shipped/frozen spec, no frozen ADR/RFC refs); **soft, revisable working hypothesis** (replaces wiki-kit v1's hard central-claim gate); config-driven layout defaulting to **scratch/out-of-repo** (research is process residue; only the distilled conclusion is promoted into governance) with a single-file topic-named `<topic-slug>-brief.md` handoff that drops into an RFC; additive Admiralty-style per-source grading folded into GRADE+triangulation; existing 7 skills reused as phase ops (`compare-hypotheses` **is** `hypotheses.md`); prompt-only, no engine; grounded in a 4-discipline survey (PRISMA/Zettelkasten/grounded-theory/ACH) + wiki-kit v1 prior art | Accepted | 2026-06-22 | 2026-06-22 |
| [0040](0040-consolidated-pack-layout-config.md) | A consolidated, namespaced pack-output layout file — replace the research-only `research-layout.toml` with one `agentbundle-layout.toml` (`[<pack>]` tables, `parent` key = a base under which each unit of work gets a topic-named folder, e.g. `efforts/research/<topic>/` + `efforts/architecture/<topic>/`); two locations with repo-root **overriding** user-profile, per-table override; **path anchoring** (repo-file `parent` is repo-root-relative/portable, user-profile `parent` must be absolute — resolved + surfaced before writing); read **prompt-only** by skill bodies; **adopter-owned, never shipped** — created by skill-elicitation-on-consent or hand-authored, and **maintained** by an installer append-if-exists/never-overwrite step (`pack.toml` `[pack.layout]` default, modelled on `_append_install_marker`); three confirmed consumers (`research`/`architect`/`product-engineering`) wired in one spec; clean rename of `research-layout.toml` (no alias — undistributed) + `research` 0.4.0→0.5.0; scope-keyed `pack.toml` `[pack.layout]` default source (install appends, never creates); cites RFC-0039/ADR-0029, RFC-0035 `sso-config.toml`, RFC-0034, ADR-0021, Cargo/npm/pyproject prior art | Accepted | 2026-06-22 | 2026-06-22 |
| [0041](0041-infra-aware-work-loop.md) | Make `work-loop` function for infrastructure development — four doctrine additions (no executable tooling, no new reviewer): (P1) a **generalized verification-mechanism preflight** (picking a verification mode obligates the mechanism to exist; else task zero builds it — universal across light/full mode); (P2) an **infra verification flavor** (layered GATES: static-preflight → plan → apply → smoke → rollback, on idempotent convergent apply, cross-linked to the plan's `## Rollout`); (P3) a new **`operational-safety` progressive-disclosure reference library** (security-checklists shape: `state-and-idempotency` / `blast-radius` / `environment-isolation` / `cost-and-teardown` / `drift-and-rollback`), carved against `security-checklists` on the reliability-vs-security lens and **consumed by `quality-engineer`** via the existing module-routing table; (P4) **agent-drives-verification** doctrine (the agent reads ground truth itself; Claude Code background/`asyncRewake`/`PreToolUse` as accelerant only); progressive delivery deferred; builds on RFC-0025 risk triggers; grounded in an applied-mode research survey in `0041-notes/`; five doctrine pillars (P1 multi-artifact verification preflight · P2 active end-to-end infra smoke · P3 six-module `operational-safety` library → `quality-engineer` · P4 agent-drives-verification · P5 mandatory security **reviewer + scanner pair** for infra — reviewer reasons failure-classes, a P1-required provider policy-as-code/CSPM scanner holds per-provider depth, URL-free deferred-authority pointer to CIS + per-provider well-architected security); progressive delivery deferred; amended 2026-06-23 (additive) | Accepted | 2026-06-22 | 2026-06-22 |
| [0042](0042-agentic-well-architected-overlay.md) | Agentic well-architected overlay as a first-class **workload-class lens** applied at **both** design time (`architect-design` gains a workload-class routing axis orthogonal to provider, loading the shared lens by construction) and review time (`architect-review` WA mode, unchanged) — the same `lens-genai-agentic.md` consumed by both; expands the overlay from today's 5 concerns to a **progressive, capability-tiered** taxonomy (Tier A *LLM on the path* → Tier B *the system acts*: tool-use authz/intent-verification, human oversight & graduated autonomy, auditable action trails, reliability-under-non-determinism → Tier C *persists/collaborates*: memory integrity, multi-agent coordination), grounded in AWS Agentic AI Lens / Azure AI-workload design areas / OWASP LLM Top 10:2025 + Agentic Top 10 / NIST AI RMF; graduated autonomy framed as engineering judgment (no standard mandates threshold-gated checkpoint removal); GenAI/agentic only (ML/SaaS/serverless stay deferred); reverses the frozen `well-architected-cloud` spec's review-time-only scoping via new RFC→new spec (frozen body can't be amended); no new skill/agent/reviewer, prose-only, routes control-level checks to `security-reviewer`; cites RFC-0032/ADR-0023/RFC-0029/RFC-0041 | Accepted | 2026-06-23 | 2026-06-23 |
| [0043](0043-product-rung-above-capability.md) | A **product rung** for the `product-engineering` intent ladder — two seeded-but-open altitudes above `capability`: **`product-vision`** (why this product should exist · who-by-circumstance · the wedge · demand evidence — the existence bet) above **`product-strategy`** (central challenge · guiding policy · coherent actions · problem/segment sequence — the path), both the *same recursive `intent`* at a higher `Level:`; **decouple `Level` from `Scale`** (the misroute root cause — `scale-intake.md` hardwired `app→feature`/`BU→capability`; the Scale→Level *stamp* becomes a *suggestion* while Scale→leaf-projection is preserved); neither rung mandatory and either authorable directly, with a **sibling-spawn detector** (a concept that won't reduce to one shippable slice signals a product altitude — offer to frame the parent, never block) + **retroactive parent** (back-link orphaned siblings to a reconstructed parent whose altitude — `capability` vs product rung — is inferred and user-correctable); de-risk gains one **`market-existence`** assumption-kind (will-anyone-want-this-at-all + viability — the Cagan viability gap), a distinct token from feature-level `desirability`, reusing the existing pre-PMF qualitative bar; seeded field sets lifted freely from prior-art product toolkits (vision / strategic-intent / multi-lens assumption-map shapes) + JTBD struggling-moment / Lean Canvas early-adopters+existing-alternatives / GStack office-hours wedge+demand / PR-FAQ / Cagan four-risks; wires the `frame→de-risk→decompose` seam into **core**'s `init-project` greenfield front door (by-reference only, no import); **prompt-only — no engine, hook, or new skill**; refines ADR-0019 (which rejected fixed ladders → keep `Level` open) via a follow-on ADR; realizes RFC-0030's anticipated "vision/intent root"; `product-engineering` 0.5.1→0.6.0 + small `core` bump; grounded in an applied-mode research survey (`0043-notes/survey-product-rung.md`) | Accepted | 2026-06-23 | 2026-06-23 |
| [0044](0044-infra-contract-acquisition.md) | **Ground the infra inner loop in platform reality** — the direct follow-on to RFC-0041, closing the one assumption it left standing (that the agent *knows* the platform and that a green `synth` means "works"). Evidenced by **two** real multi-day builds on *different* stacks (scrubbed two-build field report in `0044-notes/`) showing the loop builds/verifies/reviews against **model memory or a cheap proxy**, not the platform's contract and runtime truth — failures **MECE across six inner-loop stages**: **(A1 author·structural)** a new `core` skill `infra-contract-acquisition` — a **tool-keyed not vendor-keyed** tiered oracle protocol (T0 detect → T1 validate/plan/synth/`--dry-run` + machine-readable schema slice, **heterogeneous**: CFN `createOnlyProperties` ✅ / Pulumi `replaceOnChanges` ✅ / **TF force-new NOT in `providers schema -json`** ❌ → `plan`+docs → T2 curated platform skill, detect+recommend if absent → T3 docs → final oracle = runtime probe), declaring **oracle tier + confidence** across the strong→weak spectrum (**not big-three-only** — on-prem/Hetzner/bare-metal covered at the tooling's strength), + an EXECUTE **contract-grounding gate** (acquire-before-you-guess, generalizing "grep to verify a function exists"); **(A2 author·behavioral)** a `cloud-implementation-craft` `operational-safety` module loaded **at EXECUTE** (least-privilege-but-**sufficient** perms, eventual-consistency/propagation waits, timeout/retry/backoff incl. **client cold-start tolerance**, ordering, terminal-failed-state); **(P provision)** durable-**credentials-once** preflight item (e) + `ROLLBACK_COMPLETE`→recreate convergence; **(V1 verify·static)** phased oracle fidelity (lint<plan<runtime; don't over-trust synth — `plan` can pass while `apply` fails); **(V2 verify·runtime)** a **readiness-aware data-plane probe** flavor (in-network-if-private, write→read-back, poll-with-backoff ready-vs-broken, ephemeral self-teardown — the Terratest `defer destroy` shape) refining RFC-0041 P2; **(D debug)** a symptom→layer + **failure-signature→runbook** log playbook strengthening `observability-and-smoke`; reviews ride the existing **`quality-engineer`** (no new agent; dedicated reviewer evidence-gated); doctrine + references only, no executable tooling; MECE cross-checked against an 8-category IaC defect taxonomy + OSS convergence-loop prior art (HolmesGPT/k8sgpt ReAct, Terratest, the LLM-IaC fail→learn→refine→succeed loop) | Accepted | 2026-06-24 | 2026-06-24 |
| [0045](0045-ground-architect-design-in-platform-reality.md) | **Ground the architect *design* phase in platform reality** — the design-time companion to RFC-0044, closing the same memory-vs-ground-truth gap one inner-loop stage earlier (before any code/IaC exists, where there is no oracle to run). Evidenced by **two** real builds (scrubbed `0045-notes/`): an agent design ruled *structurally impossible* by a non-configurable ~29 s front-door timeout the design never named (two days of 504s) **that `architect-review` also missed entirely**, and managed-service contract assumptions ("serverless scales to zero", "VPC-only", single-AZ) asserted from memory and corrected only at deploy. Prose-only on the routing axis RFC-0042 already shipped: **(S)** back the deferred **serverless workload-class lens** (`lens-serverless.md`, both skill copies, byte-identical) as the **cloud-agnostic convergence** of AWS Serverless Applications Lens + Azure Functions WAF guide + GCP Cloud Run best practices, spanning the **whole serverless class — compute, data, search/analytics, event glue — not functions alone** (the field-report miss was a serverless *store*): five concerns (execution & throughput limits + the **sync-vs-async gate**; cold-start & readiness incl. store scale-up; **scale-to-zero economics, capacity floors & per-request/per-byte cost cliffs** — DynamoDB on-demand reaches zero, Aurora/Neptune/OpenSearch Serverless floor; statelessness/idempotency + **at-least-once delivery semantics** (idempotent consumers, DLQ, FIFO-for-ordering); private-serverless network reachability); **(C)** a **dual-consumed platform-contract grounding discipline** (design grounds load-bearing managed-service claims by construction in authoritative prose with confidence — never memory; `architect-review` **independently re-checks** and flags ungrounded ones) — the design-time mirror of RFC-0044's A1, carved explicitly (prose not oracles); **(V)** a **dual-consumed synchronous-path viability check** (sum worst-case latency across hops vs. the binding front-door timeout; long-running op → sync-vs-async gate; AWS/Azure/GCP-documented, not invented); **(K)** version-specific facts (the 29 s, NCU floors, packaging models) stay in **curated platform skills** recommend-when-warranted, never bundled per-vendor; serverless only — ML/SaaS stay deferred; no new skill/reviewer/tooling; cites RFC-0044/RFC-0042/ADR-0032/CHARTER P1–3 | Accepted | 2026-06-24 | 2026-06-24 |
| [0046](0046-convenient-install-defaults.md) | **Convenient install defaults** — make `--catalogue` optional on `install`/`upgrade` and resolve the catalogue source through a narrow, **trusted-by-construction** chain (explicit arg › user `[settings].source` › **editable-install detection** via PEP 610 `direct_url.json` → repo-bounded walk-up to the clone › packaged `_data/install-defaults.toml`); **no repo-supplied source layer and no cwd fall-back** — source is a code-provenance boundary, so a cloned repo can't redirect where executable packs come from (the supply-chain hazard RFC-0040 gated for the lower-stakes write case, here avoided by construction); motivated by a downstream GitLab-API-gateway fork that can't use a `git+https` source (resolver is github-only) nor bake a fixed path (clone-anywhere) — solved because `pip install -e` records each machine's absolute clone path, read at runtime; private downstream forks **blank** the packaged default and rely on detection; `config set source` (new user-`[settings]` key) is the explicit-persistence path (no auto-persist); the in-repo **adapter** override, integrity-pinning for the layer-4 `git+https` fetch, and `list-packs`/`list-profiles` defaults are named follow-ons; follow-on to RFC-0031 D7; spike-verified PEP 610 keystone; cites PEP 610 + Cargo/npm/pip layered-config prior art | Accepted | 2026-06-25 | 2026-06-25 |
| [0047](0047-adopter-and-org-supplied-grounding.md) | **Adopter- and org-supplied grounding for platform, framework, and verification context** — extend the loop's grounding beyond RFC-0041/0044's infra-only, runtime-only reach, **supplied-not-bundled and presence-checked throughout** (extends ADR-0034 Principle 1, doesn't break it). Three layers: **(A)** widen the EXECUTE contract-grounding gate from infra-only to also **framework/library contracts**, reusing `infra-contract-acquisition`'s *detect-and-recommend, never-bundle* tier (an installed internal framework-library skill / an optional Context7-style doc surface / official docs via `research`) — prose+routing, not a new skill, since the gate already names itself the generalization of AGENTS.md's grep-to-verify rule; **(B)** a **persistent recording surface the adopter already owns** — a short infra/verification command block in `AGENTS.md` + the platform/framework/verification sections of `reference.md`, which the work-loop preflight reads *first* (seeds oracle acquisition, never replaces it); **(C)** **organization-pack authoring guidance** for shipping a filled-in `reference.md` + conventions + framework-library skills via existing primitives (seeds + repo-scope profile from the organization's own **detached fork** — RFC-0046/ADR-0036 editable install, no upstream dependency; upstream is a template to fork and own, not layered over), plus making catalogue-seed linting **opt-in** (`lint-seeds`→`lint-catalogue-seeds`, gated on a `[pack].lint-seeds` flag carried only by the four first-party scaffold packs, so org packs are unenforced by construction; single tool, not split). Every layer read-if-present, degrade-honestly-if-absent, never mandated, never CI-gated. Prior art: Cursor rules/AGENTS.md (Team→Project→User), Context7, Backstage golden paths. No open questions (framework-skill = guidance-only: install a published cloud/SDK-vendor skill or author an internal one). Follow-on RFC: org-owned detached-fork distribution | Accepted | 2026-06-25 | 2026-06-25 |
| [0048](0048-autonomous-product-team-operating-model.md) | **The autonomous product-team operating model** — a foundational/umbrella decision letting the catalogue act as a product team vision→code, delivered the RFC-0041/0043 way (**doctrine + pure-markdown skills + reference libraries + reuse of existing reviewers, no new runtime engine**). Adopts as doctrine the **two-regime principle** (generative upstream = human-ratified; verifiable downstream = agent-autonomous), a **judgment→equipping map** (only *value* and *accountability* are irreducibly human), and a **risk-calibrated gate ladder G0–G5 + surfacing predicate** (G3–G5 *are* the existing `work-loop` gates; only G0–G2/G1.5 are net-new). Grows the design/UX seat (rename `design-craft`→`experience` + 3 connective skills: `map-journey`/`blueprint-service`/`inventory-screens`; grounded `aesthetic-direction` + taste-mode `design-critique`), adds 3 primitives (domain-anchor typed artifact, self-coverage gate, structural traceability lint), and **authorizes a coordinator spike — not a build**. Decides the model + a parallel child-effort roadmap; builds nothing here. Scopes discovery→build (G0–G4); the deploy/e2e outer loop + company-OS split to RFC-0049. **Reconciliation pass complete 2026-06-26** — child set complete + drift-aligned via a G0→G5 composed pressure test ([`0048-notes/10`](0048-notes/10-composed-end-to-end-walkthrough.md)) + two fresh-context passes; all drift folded into § Amendments; **freeze held** pending the owner-assigned follow-ons (RFC-0053 implementing spec AC0/layout-key/backlog ACs; CONVENTIONS `Discovery:` header). Research committed in [`0048-notes/`](0048-notes/) | Open (provisional) | 2026-06-25 | |
| [0049](0049-the-release-loop-and-company-os.md) | **The release loop — deployed e2e validation + the minimum-regret deploy carve + the company-OS composition** — child of RFC-0048. Adds the **outer loop**: `work-loop` is the **inner loop** (local build, made self-sufficient by local-infra-equivalents — fakes→Pact→Testcontainers→LocalStack→docker-compose); a new **`release-loop`** under **`release-lead`** deploys the integrated whole to an **ephemeral environment**, runs e2e, observes telemetry, and **iterates with the inner loop until convergence**, shipping to prod at G5. **Minimum-regret autonomy carve:** agents run the inner loop + the **outer loop on ephemeral envs** (deploy/e2e/iterate/teardown + canary with auto-rollback) **unwatched**; humans gate prod / data / spend / security / irreversible — the unlock is the **reversibility primitives** (ephemeral envs + feature flags + auto-rollback). Graduates RFC-0041's deploy *flavor* into a proper outer loop; doctrine + reuse (`operational-safety` + `quality-engineer`), no new runtime; runs on the omnigent harness. Completes the **"company OS"**: product (discovery) → engineering (build) → SRE/ops (release). Evidence: inner/outer loop, ephemeral preview envs, shift-right/Charity Majors, Argo Rollouts, DORA. OQs: `release-lead`'s pack + agent shape | Open | 2026-06-25 | |
| [0050](0050-the-experience-pack.md) | **The `experience` pack** — child-1 of RFC-0048 (models its Decision 3). Renames **`design-craft → experience`** via the `contract-acquisition` precedent (rename dirs+manifests+guides+cross-links, frozen governance bridged by ADR-0038, **no install-time alias**; `0.1.1 → 0.2.0`); adds three **connective** pure-markdown UX skills — `map-journey` (journey map), `blueprint-service` (frontstage/backstage service blueprint — the screen↔service tie), `inventory-screens` (screen inventory + per-screen state matrix + **one per-screen brief per screen**) — all carrying the **platform/surface axis** (responsive-web · iOS · Android · cross-platform, grounded in HIG / Material 3 / responsive breakpoints, pointed-to never reprinted); **grounds `aesthetic-direction`** in persona+precedent+standards+platform and adds an evidence-grounded **taste mode to `design-critique`** (stays an interactive skill — the live-lens reviewer is `core`/`architect`'s seat, no new agent, ADR-0024 posture preserved); **wires `voice-and-microcopy`** (stays in `product-engineering`) to the screen inventory (GAP-C1); artifact paths resolve **config → default (`docs/design`) → discover-by-marker**, never hardcoded (RFC-0040). Zero new agents, no runtime. Picks up the cross-brief consistency-pass + low-fi-prototype forward idea (notes/07/09). Evidence: NN/g service blueprints, Patton/Torres, HIG/Material/MDN (all fetched) | Open | 2026-06-25 | |
| [0051](0051-the-self-coverage-gate.md) | **The self-coverage gate** — child 3 of [RFC-0048](0048-autonomous-product-team-operating-model.md) (Decision 5): build the floor-raiser that fixes the knowing-doing gap as a **`core` reference library + loop doctrine, the `operational-safety` shape — no new runtime, no new reviewer**. Seven step-modules (`domain-grounding` · `pre-mortem` · `taxonomy-walk` · `saturation-declaration` · `fresh-context-adversarial` · `scenario-variation` · `resolve-vs-surface`) + the **living resolve-vs-surface sample-bank** graduated out of `0048-notes/09`, routed by mode and by the axes the design crosses (never a flat march). **Non-skippable by a *different* mechanism than `operational-safety`** — it runs in the **loop controller's own context** as a named gate phase (not inlined into a subagent brief): three layers, strongest-to-floor — (1) a required gate phase in the consuming loop's doctrine (the plan-mode hard-state lesson), (2) a **coverage-record refusal** the done/converged checklist won't pass without (joining the existing reviewer-clean + doc-drift refusals at `work-loop` SKILL.md:675-713 — the harness-neutral teeth, doctrine + a mechanical artifact, not a runtime lock), (3) structural enforcement where the harness offers gates-outside-the-prompt (omnigent). **Fresh-context step reuses `adversarial-reviewer`/`devils-advocate`** (no fourth reviewer; CHARTER ceiling). **Right-sized light/full via RFC-0025** (light = `domain-grounding`+`resolve-vs-surface`+one bounded pass, escalate on surviving Blocker; full = all seven). Doesn't duplicate REVIEW (inventory-diff: the fresh-context step *is* the existing pass; four steps are net-new). **`work-loop` is the first consumer** (gate at REVIEW→DECIDE); **`discovery-loop` the second** when RFC-0048 D8 ships it (pre-G2 gate); seam specified here, wired there. Spike: the done-checklist already enforces non-skippable refusal items by mechanical record, no runtime — assumption survives. No foundation drift surfaced. OQs: skill name `self-coverage`; sample-bank projection mechanics | Open | 2026-06-25 | |
| [0052](0052-shared-prefix-aware-multi-adapter-install.md) | **Shared-prefix-aware multi-adapter install** — make the install identity the **footprint** (per-relpath + content SHA), not the adapter, so one pack coexists across adapters with disjoint footprints (fixes the adapter-blind "already installed" refusal), **co-owns** files shared at identical content **within one pack across its adapter rows**, and refuses on a same-path/different-content collision *or any cross-pack same-path claim* (`--force` → existing Tier-2 `.upstream` companion). Adds a `shared` prefix class (`.agents/skills/` relied on by codex, forward-ready for others; `.kiro/skills/` for {kiro-ide,kiro-cli}; claude-code island) with **Option A** routing for the whole `.agents/skills/` cohort — cursor/gemini/copilot skills move there, **superseding the skill-home decision of ADR-0015/0016/0013** (grounded in those tools' current docs; Gemini now *prefers* the alias). Agent/hook/command projection stays native. State schema **v0.3→v0.4** keyed `[pack.<name>.adapters.<adapter>]` with a hard cross-version refusal; ownership derived from footprints; uninstall reference-aware (capture-once last-owner, per-row path-jail); orphan scan keyed by pack-across-adapter-rows (the kiro-ide/kiro-cli `.md`/`.json` coexistence is the live worked example). Greenfield (no migration); rules/steering untouched (Kiro reads AGENTS.md). Prior art: dpkg `Replaces`/`--force-overwrite`, Nix store-path dedup, `npx skills` symlink-vs-copy. **Supersedes RFC-0012 Alternative #7** (erratum). | Accepted | 2026-06-25 | 2026-06-25 |
| [0053](0053-the-coordinator-contract.md) | **The coordinator contract** — child-5 of [RFC-0048](0048-autonomous-product-team-operating-model.md) (Decisions 7 + 8: the spike→RFC). **Runs the Decision-7 spike and writes the contract it validated.** Ships the upstream coordinator the RFC-0041/0049 way — **`discovery-lead` (agent) + `discovery-loop` (skill) in `product-engineering` + the typed sidecar *schema* in `core` — no new runtime engine**, reusing the existing reviewers in a design-artifact mode. An **empirical prototype** ([`0053-notes/`](0053-notes/)) ran the loop against the worked example ([`0048-notes/02`](0048-notes/02-worked-example-flow-trace.md)) on omnigent's storage form and **demonstrated the no-engine framing on one example + a reproducible ~60-line connectedness lint** (single operator, not replicated; the outer cap modelled not run — honest limits carried to spec as a second-example run). Specifies: the **typed sidecar** (blackboard · open-questions · traceability · decision-log — the connectedness verifier; store is the harness's, paths via RFC-0040 three-tier); the **gate state machine** (consent checkpoint/resume A1/A2 + rejection/recovery with **cascade-invalidation by traceability edges** O11); the **outer cap + cost budget** (O12 — stall-surfaces-to-human, no churn); the **solo/lens-team supervisor topology** (MAST-safe by topology, loop-scoped roster). A spec-stage security pass added **§ Security & integrity contract** ACs (verdict write-authority; append-only/attested decision-log; non-degradable security lens on a boundary; lens-write integrity; cascade circuit-breaker; reachability-not-presence lint). Seams: G3→`work-loop`, the self-coverage gate as pre-G2 (RFC-0051), the traceability lint (child-4), the backlog bridge. Reconciles D7/D8 spike-confirmed + the loop-scoped-roster + security extension back into RFC-0048; appends child-5 sample reads to note 09. No open questions (both mechanics calls resolved per recommendation: a forced-cap-hit second example is a spec gate, a live on-omnigent run is not; sidecar schema = a single `core` reference). | Open | 2026-06-26 | |

## Adding a new RFC

```bash
# Find the next number (portable across macOS, Linux, native Windows).
N=$(python3 .claude/skills/new-rfc/scripts/next-ordinal.py docs/rfc)
cp .claude/skills/new-rfc/assets/rfc.md docs/rfc/${N}-<kebab-title>.md
```

Or, in Claude Code, run `/new-rfc "<title>"` (defined in `.claude/skills/new-rfc/SKILL.md`).
