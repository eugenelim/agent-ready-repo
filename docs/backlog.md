# Backlog — anchor tombstone

This file is retained only as an anchor target for links in **Frozen RFCs** that
cannot be edited. All open work has migrated to `workspace.toml [backlog].open`.
To see open work, run `workspace-status` or read `workspace.toml`.

---

## `iac-terraform`

RFC-0065 links this anchor. Open work: see `workspace.toml [backlog]` entry `iac-terraform-azure-validation`.

---

## adapt-to-project — Shipped: AC4b transcripts deferred

RFC-0007 links this anchor. Open work: see `workspace.toml [backlog]` entry `adapt-to-project-ac4b-transcripts`.

---

## `extraction-tier0-and-output-contract`

RFC-0058 links this anchor. Open work: see `workspace.toml [backlog]` entries `extraction-image-pixel-bomb-guard` and `extraction-msg-realworld-sample`.

---

### credbroker-phase-2

RFC-0023 links this anchor. **Resolved 2026-06-10.** `credbroker 0.1.0` published to PyPI via OIDC Trusted Publishing.
