# Manual QA — project knowledge review integrations

Date: 2026-08-17

## Scope boundary

This session ends at the review-integration delivery gate. It exercises the
core reviewer family and architecture review only. Research, engineering and
operational workflows, adoption closeout, retention, index scaling, external
capture, and multi-project knowledge are documented downstream and were not
exercised.

Only redacted request/result classifications and pass/fail outcomes are
recorded. No knowledge body, reviewer scratch, rejected prompt text, capture
identifier, partition, raw artifact, source corpus, sensitive locator, or
user-specific path is retained here.

## Adopter-shaped projection journey

The real pack projection code rendered `architect-review` from its canonical
source for all seven declared adapters in isolated disposable roots. Each
projection contained the same `CQ-REVIEW` query shape, untrusted evidence
delimiter, independent-grounding rule, stable result gate, and behavior evals.
The forced core self-host projection also completed successfully and updated
only the planned work-loop, reviewer, eval, and marketplace outputs.

Redacted outcomes:

- core forced self-host write: **pass**;
- Claude Code architect projection: **pass**;
- Codex architect projection: **pass**;
- Copilot architect projection: **pass**;
- Kiro IDE architect projection: **pass**;
- Kiro CLI architect projection: **pass**;
- Cursor architect projection: **pass**;
- Gemini architect projection: **pass**;
- canonical/projected stable-gate marker parity: **pass**.

An initial install-route probe encountered the expected cross-scope safety rail
for an already installed user-scoped pack. A forced repo-scope fixture then
projected files but could not write its optional user-scope follow-up marker
under the managed permission profile. No existing user installation changed.
The adapter build API was therefore used as the supported isolated projection
oracle and completed for all declared adapters.

## Review behavior and authority

Construction tests and Tier-4 behavior fixtures exercise these review paths:

- relevant knowledge supplies candidate checks only: **pass**;
- no eligible topic supplies zero candidates: **pass**;
- consequential source-verification failure abstains: **pass**;
- missing provider emits the named unavailable skip with no fallback: **pass**;
- undeclared enquiry records the not-requested skip: **pass**;
- stale, quarantined, irrelevant, malformed, and privacy-refused evidence stays
  excluded or refused: **pass**;
- prompt injection cannot change instructions, permissions, scope, reviewer
  routing, checklist coverage, severity, verdict, or finding suppression:
  **pass**;
- a topic cannot corroborate itself or become normative evidence: **pass**;
- unchanged reruns reuse one envelope; material target/scope change requires a
  new explicit declaration: **pass**;
- ineligible, self-review-refused, partial, abandoned, invalid, or interrupted
  reviews do not claim the stable completion gate: **pass**;
- reviewers expose no project-knowledge capture, distillation, private-writer,
  journal-selection, fallback-storage, transcript-mining, or raw-corpus path:
  **pass**;
- reviewer tools, read-only posture, exact clean sentinel, severity models, and
  normative output ownership remain unchanged: **pass**.

## Zero-write evidence

Before and after the review-path fixtures, the project-knowledge write surface
was inspected through the published mode boundary and the changed reviewer
definitions. Review enquiry uses only the public `--enquire` mode. No review
path creates a capture request, receives or guesses a capture identifier,
selects a partition, invokes distillation, or writes fallback state. The
existing outer work-loop authoring capture and same-gate receipt-scoped
distillation tests remain the unchanged oracle.

Result: **pass — zero reviewer knowledge writes**.

## Automated verification

The final implementation gates produced these results:

- focused core review-integration construction tests: **9 passed**;
- focused architect review-integration construction tests: **6 passed**;
- combined final focused run: **15 passed**;
- existing project-knowledge suites: **169 passed**;
- existing work-loop suites: **468 passed, 5 skipped**;
- T1 anchor suites: **266 passed**;
- existing and focused architect suites: **10 passed with 33 subtests**;
- Ruff: **pass**;
- mypy: **pass — 118 source files**;
- forced self-host dry run and generated projection parity: **pass**;
- `git diff --check`: **pass**;
- catalogue verification and policy/build checks under `SKIP_SAST=1`: **pass**.

The live public `CQ-REVIEW` enquiry returned a valid bounded envelope with no
eligible topics, no verified sources, no opened journal, and no mutation path.
The workflow therefore supplied zero candidate checks and performed no second
query or knowledge write.

Security scanning was partially degraded by the managed execution environment.
Bandit's full gate and scanner self-tests passed. Dependency audit could not run
because the managed Python environment cannot bootstrap `ensurepip`; Semgrep
could not initialize its trust-anchor/X.509 authenticator. Neither unavailable
scanner is reported as green, and neither was retried after the bounded failure.

Specialist outcomes on the final exact diff:

- adversarial implementation review: **Clean — ready to commit.**
- security implementation review: **Clean — ready to commit.**
- quality implementation review: **Clean — ready to commit.**
- experience review: **named skip — reviewer unavailable**; no rendered visual
  surface changed.
- frontend review: **not triggered**; no primary HTML, CSS, or JavaScript output
  changed.

One quality finding narrowed an over-broad receipt assertion to the actual
forbidden capture-ID, workflow-receipt selection, direct-maintainer-pending,
capture, and distillation surfaces. The focused gates and all three reviewers
then returned clean. No additional knowledge capture was justified: the reusable
lesson is embodied directly in the shipped skill boundary, construction test,
and hostile-envelope eval rather than retained as separate observation residue.
