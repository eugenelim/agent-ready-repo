# Resolve-vs-surface disposition record

Opened at PLAN, closed at DECIDE. Every item is either **resolved** (a referent
in the repository, a probe, or a tool answered it) or **surfaced** (only the
human could answer it). The point of the record is that nothing sits in a third
category of "assumed and never checked".

## Domain-grounding check (PLAN, conditional)

**Not fired.** The conditional domain-grounding obligation applies when the build
rests on an ungrounded domain claim. Every load-bearing claim here is a fact
about this repository, and each was settled by a direct check rather than
inference:

| Claim | How it was grounded |
| --- | --- |
| Which transition paths start how many child Python processes | Read-only instrumentation probe over nine representative `(mode, state, event)` paths, 2026-08-17 |
| Which files are canonical vs projected | `md5` across the three copies of five scripts; `git check-ignore` exit 1 on both projections |
| Max external subprocess calls under the engine lock | Same probe (3 `git rev-parse`, one outside the lock in the `_locked` wrapper, two inside `cmd_transition`) |
| The sanctioned pattern for a stdlib-only skill-local helper | ADR-0074 Decision summary, read directly |
| That status parsing must stay single-source | ADR-0061, cited in `loop-cohort.py`'s `_lint_module` comment |
| What the lock-budget test actually asserts | `test_loop_concurrency.py::test_lock_hold_budget`, read directly |
| Which cohort internals the tests reach for | Grep across `packs/core/tests/skills/work-loop/` |

No external-domain claim (a standard, a third-party library contract, a platform
behavior) underpins this change, so there is nothing to ground outside the repo.

## Resolved without surfacing

| # | Question | Disposition |
| --- | --- | --- |
| R1 | Where does the canonical Core source live, and are the projections tracked? | Resolved by `md5` + `git check-ignore`. `packs/core/.apm/…` is canonical; `.claude/` and `.agents/` are tracked, byte-identical, regenerated by `make build-self`. |
| R2 | What is the exact before-state process topology? | Resolved by probe: max 3 child Python guards; `plan-locked` reaches 3 through a composed two-call guard; `done` is 1 because the schedule pre-check is exempt. |
| R3 | Is `MAX_SUBPROCESS_CALLS_UNDER_LOCK = 6` exact or conservative? | Resolved: conservative. Measured max under lock was 5. The new value 2 is exact, and T4 adds a source-derived reachability check so it cannot go stale again. |
| R4 | Does moving helpers out of `loop-cohort.py` break its tests? | Resolved by grep: tests reach `canonical_contract`, `sha256_canonical_contract`, `DEFAULTS`, `_validate_run_id` as module attributes. Mitigated by re-binding every moved name at module level (T1). |
| R5 | Do engine guard tests assert exact guard stderr text? | Resolved by reading the cases: they assert exit code and `engine-state.json` non-mutation. This is what makes the shed nested prefix safe. |
| R6 | What import strategy survives file-path invocation, the importlib harness, and a projected adopter tree? | Resolved: copy `_statelock()`'s cached importlib-by-path loader — the in-repo precedent whose docstring names this exact failure. |
| R7 | Should the callable layer raise or return on an expected refusal? | Resolved from the brief's own constraint (no tracebacks for expected validation failures) plus the CLIs' need for a success message: a `GuardResult` dataclass carrying `ok`, `reason`, `message`, `data`. |
| R8 | Can a cross-guard state snapshot be used? | Resolved as no, without needing to ask: the brief permits it only with proof it preserves behavior under concurrent mutation, and that proof is not in scope. Each guard reads fresh. |
| R9 | Where does path confinement belong? | Resolved: unchanged at the engine and CLI boundaries that already own it. The one confinement that is a validation rather than a CLI concern — `--file` within spec-dir — moves *into* the guard, so the engine gains it too. |
| R10 | Which initiative owns this work? | Resolved: `ini-002` Platform Core, the home of every prior work-loop runtime spec. Registered in `[work].queue`; `workspace-status` now reports `unapproved_spec`, the expected pre-approval finding. |

## Surfaced to the human

| # | Question | Why it could not be resolved | Answer |
| --- | --- | --- | --- |
| S1 | Rebase onto `origin/main`? | Base-freshness gate exited 1. History rewriting is the human's call even when it is a pure fast-forward. | Rebase. Done; gate now reports `head is current`. |
| S2 | Must `packs/core/pack.toml` bump? | Genuinely ambiguous in the repo: no policy in `CONVENTIONS.md`, no gate in `tools/repo/`, and recent history is split — #960 and #918 touched these scripts without a bump, #952/#924/#897 bumped. | Bump to `2.7.1`. |
| S3 | May the engine's own stderr shed the nested `loop-cohort: stop — ` marker that only existed because it captured a child's stderr? | A message-shape decision with no test or doc to appeal to. Either answer is defensible; the cost of preserving it is a CLI-shaped field in a layer the brief wants CLI-free. | Drop the nested prefix. Extended after review to cover the second nested marker, `check-spec-status: `, which the original question missed. |
| S4 | Fix the status-guard fail-open (`_read_md_status`'s `except ImportError: return None` → `_assert_status_legal` silently skips), or defer it? | A genuine behavior change, so it fails the bundled-fixes carve-out's own gate — the carve-out admits mechanical ride-alongs, not behavior changes. Not the agent's call. | Fix in this PR as AC8. |
| S5 | Route the engine's duplicate `_read_managed_json` through the shared reader, or declare it out of scope in AC3? | Both are defensible: the copy is byte-identical and behavior-neutral to merge, but it serves the engine's own state read, which is not a guard — so "one implementation of each guard" does not settle it either way. | Route it through the shared reader; AC3 now covers the safe reader as well as the guards. |
| S6 | Does refactoring `_validate_run_id` / `_assert_status_legal` trip the `Ask first` mutation-verb rail? | The rail as originally worded was ambiguous: six mutation verbs *call* these helpers, but no call site changes. The agent should not resolve its own governance rail. | Signed off, and the rail reworded to cover a mutation verb's **body or accepted arguments** so the ambiguity does not recur. |
| S8 | The hand-rolled cache-free loader from S7 generated four defects of its own across round 2 (frozen-dataclass failure, `__name__`→`SystemExit(0)`, three drifting copies, a `bandit` `exec` suppression). Keep it and pin every hazard, or withdraw it? | A security-vs-complexity tradeoff on a decision the agent had already made once, now visibly costing more than it closes. Both reviewers independently questioned it. Re-deciding unilaterally would be the agent marking its own homework. | **Withdrawn.** `exec_module` retained with `sys.dont_write_bytecode`; the pre-existing-`.pyc`-read residual is named and accepted in AC13. Supersedes S7. |
| S9 | Widening three cohort mutation verbs' `except` clauses so a `ValueError` from the new bounded reader refuses instead of tracebacking — in scope, or bound reads only on the guard path? | S6 signed off helper *signatures*, explicitly not mutation-verb *failure behavior*. `_schedule_run_impl` runs under the cohort state lock, so this is exactly the class of change the `Ask first` rail exists for. | Widen the three verbs, with a test each. Rail wording extended to record failure-behavior sign-off separately. |
| S7 | How far to carry the `__pycache__` hardening — the new module only, plus the parser loader it absorbs, or all four loaders including `_statelock.py`'s? | A scope judgment on a security fix, where the wider option touches files this change otherwise never opens. | New loader plus the absorbed `lint-spec-status.py` loader; `_statelock.py`'s two deferred to backlog with a recorded slug. |

| S10 | After three review rounds (12 → 14 → 4 blockers), continue to a fourth round, proceed to the approval gates, split the spec, or stop? | The `new-spec` convergence rule says a spec not clean by three passes may have a structural problem and should be surfaced rather than ground on. Whether the trend (round 3's blockers were all my own authoring errors, and one simplification retired several) counts as convergence or as a warning is a judgment about how much review is enough — not something the agent should decide about its own work. | Fix the four, then proceed to the gates without a round 4. Recorded as a deviation from full mode's iterate-to-`Clean` rule in the final report. |
| S11 | `check-spec-status.py --file` narrows from any `is_relative_to`-passing value to a single dot-free component, and a symlinked `spec.md` that `read_text()` follows today becomes a refusal. | The `Ask first` rail covered *widening* a CLI's accepted arguments, not narrowing — a gap the reviewer found. Narrowing a shipped CLI's inputs is a compatibility decision. | Accepted as named exceptions inside AC15, with `after` goldens so the change is asserted rather than discovered. |

| S12 | Round 4 found that the bounded reader changes the accepted-input surface of **four** cohort verbs, not the three S9 covered — `cmd_approve_plan` is a fifth `ValueError` site, holds the cohort lock, and has one hash call inside an `except` a `ValueError` escapes plus another that is unguarded *and writes its result*. | S9's sign-off was scoped to three named verbs. A fourth mutation verb's failure behavior, and a narrowing of its accepted artifacts, is squarely on the `Ask first` rail — and the agent should not extend its own sign-off. | Applied as AC12(iii), **pending ratification at the spec-approval gate**, where it is called out explicitly. The alternative — exempting `approve-plan`'s two call sites from the bounded reader — was rejected because it leaves an unbounded read under the cohort lock, the exact hazard the change exists to close. |

## Pre-EXECUTE review outcome

Three rounds of `adversarial-reviewer` + `security-reviewer`, run in parallel each
round against the persisted artifacts.

| Round | Blockers | Character |
| --- | --- | --- |
| 1 | 12 | Real gaps in the change — the child process was silently bounding reads and containing exceptions |
| 2 | 14 | Mostly defects in round 1's *mitigations* — the hand-rolled loader alone produced four |
| 3 | 4 | All four were errors in this spec's own text; one simplification retired several |
| 4 | 8 | Six of eight were defects in round 3's edits. One reviewer **falsified a change the other had prompted** — the semgrep "lost coverage" claim — and one real source defect neither had reached before: `cmd_approve_plan` as a fifth `ValueError` site under the cohort lock |

Every load-bearing claim was verified against source or by a read-only probe
before being acted on. **Three reviewer claims were incorrect** and were corrected
rather than absorbed:

1. `SyntaxError`'s parentage — stated as a `ValueError` subclass; its MRO is
   `(SyntaxError, Exception, BaseException, object)`. The conclusion (it escapes
   `except (ImportError, OSError)`) held.
2. `Path.resolve()` raising `RuntimeError` on a symlink loop — did not reproduce on
   3.13.13. Retained as cheap defence, recorded as unconfirmed rather than as a
   reproduced defect.
3. "Moving the confinement pattern loses semgrep coverage" (round-3 security C6) —
   **falsified in round 4** by the other reviewer: the rule's `paths.include` never
   listed `check-spec-status.py`. This one is the clearest argument for verifying
   rather than absorbing: acting on it uncritically wrote a false claim into AC9,
   and only a second independent lens caught it.

One reviewer-recommended design — a hand-rolled cache-free module loader — was
**withdrawn** after it generated more risk than it closed.

**Named deviation:** neither reviewer returned `Clean — ready to commit.` Full mode
normally iterates to that verdict. The human elected at S10 to proceed to the
approval gates instead, on the basis of the blocker trend and the character of
round 3's findings. Remaining Concerns and Nits are carried into the
implementation-stage diff review.

## Closed at DECIDE

<!-- Filled at DECIDE: every REVIEW finding and its apply/defer disposition. -->

_Open — the loop has not reached DECIDE._
