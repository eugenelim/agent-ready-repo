# Findings: Small determined (neutral-grammar)

- F1 — AC18 does not name its input set; AC21 puts `path` outside `untrusted_publisher_data` while AC14, AC27, and AC33 require that field to carry the publisher-chosen path, so the rules conflict, and AC18's refusal wording is circular for a diagnostic produced after refusal. Required outcome and constraints: the contract no longer both forbids rendering publisher-derived values and requires a diagnostic that renders one, and any resolution keeps the diagnostic path reachable at top level and safely rendered after a logical-path refusal.
- F2 — Plan task T2 has no code-side refusal fixture for the non-skill keys that T0b measured the schema as admitting. Required outcome and constraints: refusal of every non-skill key that the measured schema admits is independently demonstrated at the code boundary.
- F3 — The injectable clock/progress seam is a fourth bound-raising channel outside AC5's enumeration of flags, environment, and configuration. Required outcome and constraints: no supplied channel, including the injectable clock and progress seams, can raise either bound, and every supplied value is validated before it can tighten a bound.
- F4 — The fifth static assertion in plan task T5, which forbids `lstat`, `stat`, `fstat`, and `resolve` outside the carve-out, has no mutation fixture, and nothing falsifies AC34's refusal-only probe result. Required outcome and constraints: the static controls demonstrably fail if a forbidden filesystem call escapes the carve-out or the measured-path probe returns admission data rather than only a refusal decision.
- F5 — `--check` has no per-invocation aggregate bound, so N stored sources multiply the per-fetch cost N times; an owner decision is required. Required outcome and constraints: with owner approval, one `--check` invocation cannot exceed 90 seconds, 25 distinct resolutions, five in-flight resolutions, or 256 MiB downloaded.
- F6 — The AC35 verdict table was never committed even though its re-assertion trigger fired for both E14 shapes and E15 depth, and plan task T3 sets the bounds without carrying AC35 or AC36. Required outcome and constraints: committed corpus evidence covers both E14 shapes and E15 depth after their changes, and the bound-setting work remains traceable to both the corpus-admission obligation and the budget cost ceiling.
- F7 — Plan task T9 holds the AC4 differential fixture but omits AC4 from its AC line. Required outcome and constraints: the work that owns the stored-provenance differential evidence is traceable to the stored-provenance request-construction obligation.
