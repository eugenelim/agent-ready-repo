# Findings: Judgment dominated (fix-grammar)

- F1 — `validate_payload`, `compute_review_on`, and `is_due` catch `ZoneInfoNotFoundError` and `ValueError` but not the `OSError` raised by an over-long `timezone`, so a reachable public untrusted-input path can escape with a host path and errno instead of returning AC5's `unknown-timezone`, while the schema's 255-character bound remains unenforced. Fix: add a type and 255-character guard before `ZoneInfo` and catch `OSError` at all three seams so each returns its specified refusal code without a host path or errno.
- F2 — The spec's Follow-ons register cites pre-change line numbers, and three of its seven anchors name a different function than the surrounding prose does. Fix: update all seven Follow-ons anchors to the current code and correct the three function names to match their surrounding prose.
- F3 — `plan.md` cites eight `cooling.py` line numbers that have all drifted from the code they point at. Fix: replace the eight stale `cooling.py` line citations in `plan.md` with current symbol-level citations to their intended code.
- F4 — The `_close_work()` follow-on asserts that `enrol` wraps the dependency, which is false: `_resolve_destination` is called at `:692` while `enrol`'s `try` opens at `:695`, so a shipped document makes a false statement about shipped code. Fix: edit the `_close_work()` follow-on to state that `enrol` calls `_resolve_destination` before its `try` block and therefore does not wrap that dependency.
- F5 — `_resolve_destination`'s own `_close_work()` reach is a fifth uncaught path, and the Follow-ons register denies it while enumerating four. Fix: add `_resolve_destination` as the fifth uncaught `_close_work()` path and correct the Follow-ons count to five.
- F6 — Line-number citations in this spec have gone stale under an insertion three separate times, so the citation style itself, rather than any individual anchor, is what keeps failing. Fix: replace line-number citations throughout the spec with file-and-symbol citations that remain valid when unrelated lines are inserted.
