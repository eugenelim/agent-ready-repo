# Findings: Judgment dominated (neutral-grammar)

- F1 — `validate_payload`, `compute_review_on`, and `is_due` catch `ZoneInfoNotFoundError` and `ValueError` but not the `OSError` raised by an over-long `timezone`, so a reachable public untrusted-input path can escape with a host path and errno instead of returning AC5's `unknown-timezone`, while the schema's 255-character bound remains unenforced. Required outcome and constraints: every public timezone-resolving seam refuses non-string or over-255-character input before platform lookup, contains lookup `OSError`, returns its specified refusal code, and exposes no host path or errno.
- F2 — The spec's Follow-ons register cites pre-change line numbers, and three of its seven anchors name a different function than the surrounding prose does. Required outcome and constraints: all seven follow-on anchors identify the current code, and each named function agrees with the surrounding claim.
- F3 — `plan.md` cites eight `cooling.py` line numbers that have all drifted from the code they point at. Required outcome and constraints: all eight plan references remain attached to their intended `cooling.py` code when unrelated lines shift.
- F4 — The `_close_work()` follow-on asserts that `enrol` wraps the dependency, which is false: `_resolve_destination` is called at `:692` while `enrol`'s `try` opens at `:695`, so a shipped document makes a false statement about shipped code. Required outcome and constraints: the shipped record accurately reflects that `enrol` calls `_resolve_destination` before its `try` block and therefore does not contain that dependency's failure.
- F5 — `_resolve_destination`'s own `_close_work()` reach is a fifth uncaught path, and the Follow-ons register denies it while enumerating four. Required outcome and constraints: the shipped record accounts for five uncaught `_close_work()` paths, including the path through `_resolve_destination` itself.
- F6 — Line-number citations in this spec have gone stale under an insertion three separate times, so the citation style itself, rather than any individual anchor, is what keeps failing. Required outcome and constraints: every code citation remains unambiguous and valid when unrelated lines are inserted.
