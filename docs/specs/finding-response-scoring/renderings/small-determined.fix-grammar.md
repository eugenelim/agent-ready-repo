# Findings: Small determined (fix-grammar)

- F1 — AC18 does not name its input set; AC21 puts `path` outside `untrusted_publisher_data` while AC14, AC27, and AC33 require that field to carry the publisher-chosen path, so the rules conflict, and AC18's refusal wording is circular for a diagnostic produced after refusal. Fix: edit AC18 and AC21 to enumerate the publisher-derived output values, keep diagnostic `path` top-level, and require safe path rendering after a logical-path refusal.
- F2 — Plan task T2 has no code-side refusal fixture for the non-skill keys that T0b measured the schema as admitting. Fix: add T2 code-side refusal fixtures for every non-skill key that T0b showed the schema admits.
- F3 — The injectable clock/progress seam is a fourth bound-raising channel outside AC5's enumeration of flags, environment, and configuration. Fix: edit AC5 to include the injectable clock and progress seams among channels that may only tighten bounds after value validation.
- F4 — The fifth static assertion in plan task T5, which forbids `lstat`, `stat`, `fstat`, and `resolve` outside the carve-out, has no mutation fixture, and nothing falsifies AC34's refusal-only probe result. Fix: add T5 mutation fixtures that fail when a forbidden filesystem call escapes the carve-out or the AC34 probe returns admission data instead of only a refusal decision.
- F5 — `--check` has no per-invocation aggregate bound, so N stored sources multiply the per-fetch cost N times; an owner decision is required. Fix: edit AC22 after owner approval to cap each `--check` invocation at 90 seconds, 25 distinct resolutions, five in-flight resolutions, and 256 MiB downloaded.
- F6 — The AC35 verdict table was never committed even though its re-assertion trigger fired for both E14 shapes and E15 depth, and plan task T3 sets the bounds without carrying AC35 or AC36. Fix: add the regenerated AC35 verdict table for both E14 shapes and E15 depth, and add AC35 and AC36 to T3's criterion line.
- F7 — Plan task T9 holds the AC4 differential fixture but omits AC4 from its AC line. Fix: add AC4 to T9's criterion line.
