# Answer: small-determined (neutral-grammar)

## F1
Repair the contradiction without moving diagnostic paths out of their top-level field. AC18 will explicitly define its publisher-value input set as publisher metadata and other publisher text values, excluding a diagnostic `path` even when that path originated in the source tree. AC21 will retain top-level `path`; AC18 will require that it be escaped by the stated path-escaping rule before human rendering, and JSON will carry it as the diagnostic path rather than publisher data. The admission refusal may therefore name the offending path safely after the logical-path check, while no unescaped publisher value is rendered as a publisher field.

## F2
Repair the plan's T2 test commitment. Add one independent code-boundary refusal fixture for each non-skill key that T0b established the schema admits, rather than one aggregate fixture. This proves the direct-manifest subset rejects every such schema-admitted key and keeps the evidence at the boundary that implements the refusal.

## F3
Repair AC5 so its no-bound-raising rule expressly covers every supplied channel, including the injectable clock and progress seams. The seams will be validated before use and may not raise either Family-1 or Family-2 bound; only valid values that tighten a bound are accepted. This closes the unstated fourth channel while preserving the existing private, keyword-only seam design.

## F4
Repair T5's test plan with two mutation fixtures. One will introduce a forbidden filesystem call outside the AC34 carve-out and assert the static control fails. The other will mutate `probe_measured_path` to return admission data and assert the control fails because the probe may produce only a refusal decision. Together they demonstrate both the carve-out boundary and AC34's refusal-only result.

## F5
Approve the requested `--check` aggregate limits. AC22 will state that one invocation is bounded by a 90-second aggregate deadline, 25 distinct post-dedup resolutions, at most five in flight, and 256 MiB aggregate downloaded bytes, with limit and resolution failures rendered as `unknown` rather than raising the limits. This keeps a stored-source set from multiplying the per-fetch cost without changing the credential-free acquisition bounds.

## F6
Repair the plan and evidence obligations. T3 will commit and re-assert the AC35 corpus verdict table after the E14 and E15 changes, including both collection-root shapes, envelope-relative depth, and the required measurement columns. T3 will also cite AC35 and AC36 alongside the bound-setting work, so corpus admission evidence and the five-second/256-MiB cost ceiling remain traceable requirements rather than unstated follow-up work.

## F7
Repair T9's traceability line by adding AC4 to its implemented acceptance criteria. T9 already owns the stored-provenance differential fixture, so the plan must identify AC4 as the obligation that fixture proves.

## Acceptance-criteria count after these decisions
36, from a starting count of 36.
