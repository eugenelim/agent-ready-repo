# Transcript: large-mixed / neutral-grammar
Arm: neutral-grammar
Grammar: Required outcome and constraints:
Rendering: docs/specs/finding-response-scoring/renderings/large-mixed.neutral-grammar.md
Rendering-sha256: d4831310ac4f61547ccea36bf4a52a1bac1b26d1e9f9623a6403ff9f83d6aff3
Baseline: docs/specs/finding-response-scoring/cases/large-mixed.baseline.md
Answered: docs/specs/finding-response-scoring/transcripts/large-mixed.neutral-grammar.answered.md
Model: gpt-5.6-terra
Settings: model_reasoning_effort=medium, sandbox=workspace-write
Instructions: 25e14bc580c8bf9e9d49bb5537355411d9a4aaff954b28f0e719e1c19e844b0c

- F1 — repair — The authority metadata names RFC-0077 and AC3 consistently states only its same-or-ancestor rule.
- F2 — narrow-the-claim — AC4 claims only that pre-truncation match counts are non-increasing and no longer claims nested returned sets.
- F3 — repair — Enquiry has a separate matcher path while pending selection retains its existing matcher and journal-capacity behavior, with separate plan and test coverage.
- F4 — repair — The version-bump criterion requires the changelog entry, pack version, and plugin version in the same pull request.
- F5 — repair — T4 compares against an independently derived expected atom sequence and verifies that the sanctioned writer rejects duplicates.
- F6 — repair — T4 writes both migration measurements to one repository path established before migration.
- F7 — repair — The ranking contract and TDD table make `.` corpus-wide and least specific while normalizing specificity across supported scope forms.
- F8 — repair — T4 migrates every topic through `write_topic`, preserving validation and writer locking.
- F9 — repair — The migration derives valid repository-relative path atoms for every legacy value and stops underivable entries for review.
- F10 — repair — The revision gives each load-bearing value one authoritative statement and turns remaining occurrences into references or clearly evidentiary text.
- F11 — repair — The delivery record labels unfinished work as proposed and historical measurements as baseline evidence.
- F12 — cut-the-item — The unverifiable hand-grep comparison is removed, leaving the observable production-matcher success condition.
- F13 — repair — The acceptance criterion uses the contract term `corpus-wide` while leaving base reduction to implementation and tests.
- F14 — repair — T7 cites the pending-selection non-regression criterion and the full suite criterion it discharges.
- F15 — repair — T2 and T5 explicitly use TDD, and T6 gains a TDD Testing Strategy row with independently failing exclusion cases.
