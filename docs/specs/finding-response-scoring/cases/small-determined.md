# Case: Small determined
Source: docs/specs/direct-skill-repository-installation/notes/reviews/preexecute-security-adjudication.md
Baseline: docs/specs/finding-response-scoring/cases/small-determined.baseline.md

- F1 — AC18 does not name its input set; AC21 puts `path` outside `untrusted_publisher_data` while AC14, AC27, and AC33 require that field to carry the publisher-chosen path, so the rules conflict, and AC18's refusal wording is circular for a diagnostic produced after refusal.
- F2 — Plan task T2 has no code-side refusal fixture for the non-skill keys that T0b measured the schema as admitting.
- F3 — The injectable clock/progress seam is a fourth bound-raising channel outside AC5's enumeration of flags, environment, and configuration.
- F4 — The fifth static assertion in plan task T5, which forbids `lstat`, `stat`, `fstat`, and `resolve` outside the carve-out, has no mutation fixture, and nothing falsifies AC34's refusal-only probe result.
- F5 — `--check` has no per-invocation aggregate bound, so N stored sources multiply the per-fetch cost N times; an owner decision is required.
- F6 — The AC35 verdict table was never committed even though its re-assertion trigger fired for both E14 shapes and E15 depth, and plan task T3 sets the bounds without carrying AC35 or AC36.
- F7 — Plan task T9 holds the AC4 differential fixture but omits AC4 from its AC line.
