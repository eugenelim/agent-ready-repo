# First-run dispositions: large-mixed / fix-grammar

The classified dispositions of the first run, listed only. The original header
block is deliberately removed: it named paths that now hold the second run's
artifacts, so keeping it would have implied provenance this directory cannot
support.

- F1 — repair — The authority record names RFC-0077 and AC3 unambiguously defines equal-or-descendant query matching without the reverse match.
- F2 — narrow-the-claim — AC4 promises only the measurable non-increasing pre-truncation match count instead of nested returned sets that its check cannot establish.
- F3 — repair — Enquiry uses a dedicated matcher while pending processing retains its matcher and both callers receive independent tests.
- F4 — repair — The version-bump criterion requires its patch-version changelog entry in the same pull request.
- F5 — repair — T4 compares against hand-authored ordered atoms and tests that the sanctioned writer refuses duplicate input.
- F6 — repair — T4 names the exact durable path for the AC9 before-and-after record.
- F7 — repair — The contract gives dot and every empty-base glob the same corpus-wide lowest specificity.
- F8 — repair — T4 routes every topic mutation through write_topic with its validation and lock.
- F9 — repair — The migration converts comma-joined and glob values into valid repository-relative path atoms and records each derivation.
- F10 — repair — Each load-bearing fact has one normative location and all repetitions become references.
- F11 — repair — The specification describes unimplemented work as proposed work or baseline evidence rather than completed history.
- F12 — narrow-the-claim — The success claim is limited to the observable production-matcher condition directly reached by the integration test.
- F13 — repair — AC6 uses the defined corpus-wide contract term instead of a plan-owned implementation phrase.
- F14 — repair — T7 cites AC11 and AC12 as the obligations it actually verifies.
- F15 — repair — T2, T5, and T6 have explicit verification modes and the Testing Strategy covers T6 behavior.
