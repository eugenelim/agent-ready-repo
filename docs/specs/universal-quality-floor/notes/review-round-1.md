# Adversarial review — round 1

## Blockers

**1. Spec ships with all 15 Acceptance Criteria unchecked.** `docs/specs/universal-quality-floor/spec.md:101`. Every AC is still `- [ ]`. Fix: flip each met criterion to `- [x]`.

**2. Spec Status not advanced to Shipped.** `docs/specs/universal-quality-floor/spec.md:3`. Status still `Approved`. Fix: set Status: Shipped.

**3. Plan Status not advanced.** `docs/specs/universal-quality-floor/plan.md:4`. Status still `Drafting`. Fix: set plan Status: Done.

## Concerns

**4. Plan T4 Touches / AC11 location divergence undocumented.** `docs/specs/universal-quality-floor/plan.md:145`. Governance note landed in AGENTS.local.md not the seed (correct call); plan still reads as seed. Fix: add a plan changelog note recording the move.
