# Specs

> Feature specifications and implementation plans. See
> [`../CONVENTIONS.md`](../CONVENTIONS.md#4-specs-and-plans--docsspecsfeature)
> for the spec / plan distinction and lifecycle.

Each feature gets a directory:

```
docs/specs/<feature>/
├── spec.md      ← the contract (objective, boundaries, testing strategy, acceptance criteria): what this feature does
├── plan.md      ← the strategy + construction tests: how we'll build it
└── notes/       ← (optional) research, sketches, rejected approaches
```

## Active specs

<!-- Update this list as features are added. -->

| Spec | Status | Constrained by | Notes |
| --- | --- | --- | --- |
| [`workspace-journey-guides-and-planning-doctrine/`](workspace-journey-guides-and-planning-doctrine/spec.md) | Shipped | RFC-0064 Amendment #3 (e)(f) | **RFC-0064 P1 Orient + Capture — guides + phase-slice doctrine.** 6 new Diátaxis guides (`docs/guides/core/`): `tutorials/your-first-workspace.md`, `how-to/start-a-project.md`, `how-to/orient-at-session-start.md`, `how-to/capture-work.md`, `reference/workspace-toml-schema.md`, `explanation/two-room-model.md`. 3 prose edits: CONVENTIONS.md `### Phase-slice planning` doctrine in two sections; `new-rfc` SKILL.md `## After acceptance` roadmap-sequencing guidance; `receive-brief` SKILL.md shippable-slice definition extended to include guide. `make build-self FORCE=1` gate. Mixed-shaped; goal-based checks + manual Diátaxis spot-check. 13 ACs / 11 tasks. Spec-mode review Clean (adversarial 3 passes → 0). |
| [`author-brief-docs/`](author-brief-docs/spec.md) | Shipped | RFC-0064 | **RFC-0064 P3 — docs for `author-brief`.** Docs-only. Three deliverables: (1) new `docs/guides/core/how-to/intake-an-external-brief.md` — task-oriented guide covering the full `author-brief` flow (ingest → identify → elicit → confirm slug → create + queue → handoff), decision table naming `author-brief` / `receive-brief` / `new-spec` entry points, all three `workspace.toml` branch outcomes, explicit boundary statement, Next-step link; (2) `docs/guides/core/reference/product-brief-fields.md` — attribution fix (opening paragraph no longer attributes brief creation solely to `receive-brief`) + `Rabbit holes` and `Status` rows added to body-sections table + new DoR gate section defining four eligibility fields (Outcome, Appetite, Rabbit holes ≥1, Spec map skeleton ≥1) framed as "required to reach Ready"; (3) `receive-brief` how-to decision table patched to name `author-brief` as peer entry point; `docs/guides/core/README.md` updated. Mixed-shaped; goal-based greps + manual read-through. 9 ACs / 2 tasks. Spec-mode review Clean (adversarial 4 passes → 0). |
| [`m2-lean-canvas-and-initiative-brief/`](m2-lean-canvas-and-initiative-brief/spec.md) | Shipped | RFC-0064 | **RFC-0064 M2.6 — `lean-canvas` PE skill.** Prompt-only skill in the `product-engineering` pack. Elicits initiative slug; presents simple (5-box: Problem, UVP, Solution, Customer Segments, Key Metrics) or full (9-box adapted, adding Channels, Value Created, Cost Estimate, Organizational Buy-In) Lean Canvas mode; reads upstream shaping artifacts from `<output_dir>/shaping/<ini-slug>/` to pre-populate fields; produces one file — `docs/product/initiatives/<ini-slug>.md` combining `_template.md` structure with a `## Value Proposition` section inserted before `## Scope`. Three-tier output-path config (realpath, reject `..`, overwrite guard). Degrades under "Step 5 readiness" and "Step 6 readiness" notes when `place-bet` or `map-capabilities` is absent. Mode locked at user confirmation. 11 ACs / 5 tasks. Shipped (PR #596). |
| [`m2-frame-situation/`](m2-frame-situation/spec.md) | Shipped | RFC-0064 | **RFC-0064 M2.1 — `frame-situation` PE skill.** Prompt-only skill in the `product-engineering` pack. Classifies a raw initiative/capability-level signal into a typed finding (`opportunity`/`risk`/`gap`/`threat`/`emergent-capability`); assesses Wardley maturity for up to three capability areas (Genesis/Custom-built/Product/Commodity) with evidence and strategic implication; recommends a PE six-step shaping entry point. Writes a typed `situation-framing.md` artifact to `<output_dir>/shaping/<slug>/` (three-tier config, realpath, reject `..`); degrades when `identify-opportunities` is absent; suggests a `workspace.toml` shaping-queue entry without writing it. Feature-scoped requests redirected to `frame-intent`. 12 ACs / 4 tasks. Shipped (PR #591). |
| [`workspace-status-next-actions/`](workspace-status-next-actions/spec.md) | Shipped | RFC-0064; depends on `spec-A-workspace-status-rename` (Shipped) | **workspace-status closing section.** Adds explicit `### 6. Next-actions` step to `workspace-status` SKILL.md (before `## See also`). Surfaces ≤4 numbered choices derived from Step 2 DAG state: active spec, next unblocked queue item, first actionable shaping item, always "start new work" (`new-spec`·`new-rfc`·`new-adr`·`capture-work`). When ≥2 work items unblocked: ASCII dependency graph precedes choices; harness-appropriate parallel-session numbered choice follows (Claude Code → `claude --bg`; others → prose). Trigger list unchanged. Service-shaped; goal-based grep + manual QA. 10 ACs / 2 tasks. |
| [`workspace-status-queue-reconciliation/`](workspace-status-queue-reconciliation/spec.md) | Shipped | RFC-0064 | **workspace-status reliability — reconciliation step.** Adds a Reconciliation block to `workspace-status` that surfaces three inconsistency classes between spec.md `Status:` fields and `workspace.toml` queue/active/shipped lists: (1) untracked live specs (Approved/Implementing, absent from all lists); (2) stale queue/active entries (spec shows Shipped/Archived — offers cleanup with confirmation); (3) prematurely-shipped entries (in shipped, spec shows live status). Block appears before initiative output; clean workspace shows no block. 13 ACs / 2 tasks. Spec-mode review Clean (adversarial 7 passes → 0). |
| [`product-strategy-pack/`](product-strategy-pack/spec.md) | Shipped | RFC-0063, ADR-0053, ADR-0024, ADR-0030, RFC-0037 | **RFC-0063 — `product-strategy` pack v0.1.0.** New user-scope, pure-markdown pack. 9 skills: `run-swot`, `run-porters-five-forces`, `run-pestle-analysis`, `run-bcg-matrix`, `run-okr-cascade`, `write-prfaq`, `synthesize-stakeholder-research`, `define-ux-strategy`, `define-content-strategy`. OKR cascade → PE-pack `frame-situation` cross-pack routing contract. `check-workspace` routing updated. ADR-0053 records D1–D10. Growth strategy (OQ1) and experience mapping (OQ2) deferred. |
| [`m2-place-bet/`](m2-place-bet/spec.md) | Shipped | RFC-0064 | **RFC-0064 M2.4 — `place-bet` PE skill.** Step 5 of the PE six-step shaping sequence. Reads the diverge-solutions artifact if present; offers to run `diverge-solutions` first when absent (naming the tradeoff), then degrades gracefully to free-form input. Accepts any prior options source. Reads `validation-notes.md` opportunistically. Emits `bet.md` with full betting table: option, option-source, confidence, appetite, rationale, risks-accepted, assumptions, kill-condition (optional), next-step. Config-driven three-tier path resolution. Prompt-only skill; SKILL.md <100 lines; no engine/script. Ships with Diátaxis how-to guide. 12 ACs / 4 tasks. Spec-mode review: 1B+5C+3N → 0 (adversarial). |
| [`m2-map-capabilities/`](m2-map-capabilities/spec.md) | Shipped | RFC-0064 | **RFC-0064 M2.5 — `map-capabilities` PE skill.** Terminal step (step 6) of the PE six-step shaping sequence; produces the Capability Map artifact that anchors M3–M6 spec-writing. Reads `bet.md` (required anchor; degrades with "reduced traceability" note when absent); reads `situation-framing.md` opportunistically. Elicits product vision; proposes candidate L1 domains; annotates capabilities with 7 fields: stable id (kebab-case), name, description, Wardley stage (Genesis/Custom-built/Product/Commodity), strategic criticality (Differentiating/Parity/Utility), disposition (Build/Buy/Partner/Adopt), dependencies. Flags Commodity+Differentiating pairs as "strategic tension" before finalizing. Emits a "Suggested build sequence" (Build-disposition only, dependency-ordered). Three-tier config-driven path resolution; realpath, reject `..`. Suggests workspace.toml transition (does not write). Ships with template asset, worked example (two runs, no-collision), and Diátaxis how-to guide. 16 ACs / 5 tasks. Spec-mode review: 3B+3C+2N → 0 (adversarial). |
| [`m2-identify-opportunities/`](m2-identify-opportunities/spec.md) | Shipped | RFC-0064 | **RFC-0064 M2 — `identify-opportunities` PE skill.** Step 2 of the six-step shaping sequence. New prompt-only skill in the PE pack: surfaces functional/emotional/social jobs exhaustively, scores each via the Ulwick formula (`importance + max(importance − satisfaction, 0)`), emits a ranked `opportunity-assessment.md` artifact under `<output_dir>/shaping/<slug>/`. Free-form input primary; reads `situation-framing.md` opportunistically when present. Ships with `assets/opportunity-assessment-template.md`, a worked example, and a Diátaxis how-to guide. Three-tier write-path resolution; degrade branch when `diverge-solutions` absent. Mixed-shaped; goal-based + manual QA. 14 ACs / 4 tasks. |
| [`m2-frame-intent-jtbd/`](m2-frame-intent-jtbd/spec.md) | Shipped | RFC-0064 | **RFC-0064 M2.7 — JTBD enrichment for `frame-intent`.** Additive modification to the shipped `frame-intent` skill: step 5 gains explicit three-tier JTBD elicitation (functional job, emotional job, social job, struggling moment); `intent-template.md` Opportunity section gains four optional structured sub-fields; new `references/jtbd-job-categories.md` grounds vocabulary shared with `identify-opportunities`. Boundary preserved: no Ulwick scoring in `frame-intent`. Guide `shape-a-feature-intent.md` updated with JTBD subsection; PE pack bumped to v0.12.0. Service-shaped; goal-based greps + manual QA. 12 ACs / 7 tasks. Spec-mode review Clean (adversarial 4 passes → 0). |
| [`portfolio-first-run-pilot-governance-extras/`](portfolio-first-run-pilot-governance-extras/spec.md) | Shipped | RFC-0064 Amendment #4 | **RFC-0064 Amendment #4 — governance-extras preview-confirm write pilot.** Adds `docs/guides/governance-extras/tutorials/governance-extras-first-session.md`: a step-by-step walkthrough of the `new-adr` preview-confirm write gate (decision framing → ADR content preview → target path preview → confirm/stop/revise → recovery). Adds `tutorial` field to `[pack.first-value]` in `pack.toml`; bumps governance-extras to 0.8.2. Completes the "preview-confirm repository write" archetype pilot (Decision 4 of Amendment #4). Gates: `lint-first-value-contract` (validates tutorial path exists); `build-check` passes. 7 ACs / light mode. |
| [`m6-role-journey-guides/`](m6-role-journey-guides/spec.md) | Shipped | RFC-0064 (M6 AC line 529; Amendment #3 P5) | **RFC-0064 P5 Adopt — role journey guide.** Single Diátaxis explanation-quadrant guide at `docs/guides/core/explanation/role-journeys.md` covering PM/strategist/PE (altitudes 0–1: direction-setting, shaping, tracker intake), Engineer (altitude 2: workspace orientation + work-loop + initiative/ad-hoc distinction), and headless Agent (altitude 2: cold-start, autonomous execution, ship signal). Derived from five source journey maps in `docs/product/journeys/`. Swarm extension deferred (`role-journey-agent-swarm-section`). `docs/guides/core/README.md` + `docs/guides/README.md` By-role table updated. Mixed-shaped; goal-based greps + manual persona-read QA. 11 ACs / 5 tasks. Spec-mode review Clean (adversarial 4 passes → 0). |
| [`agentbundle-first-value-handoff/`](agentbundle-first-value-handoff/spec.md) | Draft | RFC-0064 Amendment #4; spec/portfolio-pack-first-value-contract | **RFC-0064 Amendment #4 — install first-value handoff.** Additive change to `agentbundle install` stdout: after the `installed:` line(s), emits a first-value block drawn from `[pack.first-value]` in `pack.toml`. Level B packs: `Verify:`, `Try:`, `Expected:`, and optional `Next:` labels. Level A packs: `Verify:` only. No first-value section: output unchanged. `installed:` line format and all automation contracts preserved. Dry-run, upgrade, and profile-install paths unaffected. 7 ACs / 4 tasks / light mode. |
| [`m3-experience-design-rename/`](m3-experience-design-rename/spec.md) | Draft | RFC-0064 | **RFC-0064 M3 — rename `experience` pack to `experience-design` at v1.0.0.** Pack directory rename; `pack.toml` + `plugin.json` name/version/display_name/docs-link updates; no skill renames (all 18 are function-named); operative cross-pack reference sweep across `work-loop`, `new-spec`, `discovery-loop`, `voice-and-microcopy`, and `product-engineering/README.md`; guide directory rename + slug-named guide file rename + guide prose + `docs/guides/README.md`; all platform-site surfaces: `site/mkdocs.yml`, `tools/build-site.py`, `web/src/content/packs/`, `web/src/content/journeys/`, `web/src/pages/journeys/index.astro`; root `README.md`; new `AGENTS.md` migration guide; RFC-0064 body edit recording alias-assessment outcome; `build-self`. `experience-reviewer` agent name unchanged (functional). `agentbundle-layout.toml` has no `[experience]` section — none to protect. Zero agentbundle test updates needed. Integration-shaped; goal-based checks only. 18 ACs / 7 tasks. Spec-mode adversarial review pending. |
| [`experience-pack-0.6.0/`](experience-pack-0.6.0/spec.md) | Shipped | RFC-0066, ADR-0038, ADR-0024 | **RFC-0066 — experience pack surface-genre uplift.** 9 skill renames (map-customer-journey→journey-mapping et al.), 1 screen-brief template update (surface-genre field + Genre-specific notes), 1 new Define-phase skill (design-principles), 6 new genre-specific skills (conversion/documentation/analytical/marketplace/informational/workspace-design), 7 extensions across 6 existing skills, web/ marketing site sweep, docs/guides/experience/ content pass, journey doc update, ADR-0052, changelog, RFC row. Pack 0.5.0→0.6.0. Goal-based checks + manual QA. 17 ACs / 9 tasks. Spec-mode review Clean (adversarial 4 passes → 0). |
| [`m5-github-brief-intake/`](m5-github-brief-intake/spec.md) | Shipped | RFC-0019 | **RFC-0064 P4 — `github-brief-intake` skill in a new `github` pack.** Pure prose-choreography adapter (mirrors `jira-brief-intake` shape): pulls a GitHub Milestone + its issues via `gh` CLI, maps to a Shape B product brief (milestone title → Outcome draft; issues → `US-n (#NNN)` stories, closed annotated `[closed]`; milestone `html_url` → `Epic:`), writes to `docs/product/briefs/<slug>.md`, and hands off to `receive-brief` by name. Three-way auth model: authenticated → proceed; unauthenticated + public → note posture; unauthenticated + 404 → ambiguous message (private vs not-found indistinguishable). Graceful degradation when `receive-brief` absent. Bounded post-intake write-back (comment/label/close — never body edits, per user confirmation). New `github` pack (user-scope-default, non-projected; gate = `lint-packs` + `validate` + `build` + package pytest). Ships with Diátaxis how-to guide (P4 phase-slice doctrine). 9 skill ACs (AC-S1–S9) + pack / guide / cross-cutting ACs / 4 tasks. Spec-mode review: 2C+3N → 0 (adversarial 3 passes). |
| [`m3-desk-research-rename/`](m3-desk-research-rename/spec.md) | Draft | RFC-0064 | **RFC-0064 M3 — rename `research` pack to `desk-research` at v1.0.0.** Pack directory rename; `pack.toml` + `plugin.json` name/version/display_name/docs-link updates; five skill directory renames (`research` → `desk-research`, `research-project-*` → `desk-research-project-*`); operative cross-pack reference sweep across `check-workspace`, `init-project`, `contract-acquisition`, `frame-domain`, `discovery-loop`, `frame-intent` references, and `frame-domain` examples; guide directory rename + guide prose + slug-named guide file renames; new `AGENTS.md` migration guide in renamed pack; RFC-0064 body edit recording alias-assessment outcome; integration test updates; `build-self`. `workspace.toml` `type = "research"` and `agentbundle-layout.toml [research]` section key unchanged (activity-type identifiers). Integration-shaped; goal-based checks only. 14 ACs / 8 tasks. Spec-mode review Clean (adversarial 2B+3C+2N→0). |
| [`m1-brief-queue/`](m1-brief-queue/spec.md) | Implementing | RFC-0064 | **RFC-0064 Batch 4 — brief queue end-to-end.** Three deliverables: (1) Brief template gains four DoR fields: `Status` (hand-set; Draft→Ready→Executing→Shipped), `Rabbit holes` (design traps; ≥1 required for Ready gate), `Instrumentation` (measurement mechanism, distinct from Success metrics), `## Design artifacts` (upstream shaping artifact links). Fields are additive and retroactively optional. (2) `receive-brief` extended: checks DoR gate before stamping Ready; sets `Status: Ready` in brief frontmatter; moves brief path from `[brief_queue].draft → ready` in `workspace.toml` via comment-preserving edit; handles idempotent/dedupe/missing-from-draft cases; degrades on absent/unparseable/no-`brief_queue`. (3) `author-brief` new skill: ingests unstructured external input; elicits missing DoR fields; checks for slug collision; creates brief file with `Status: Draft`; prompts on multi-initiative `workspace.toml`; appends path to `["<slug>".brief_queue].draft` via comment-preserving edit; degrades gracefully on all no-write cases. Integration-shaped; goal-based + manual QA. 13 ACs / 3 tasks. Spec-mode review Clean (adversarial 1B+4C+5N→0). |
| [`m1-governance-integration/`](m1-governance-integration/spec.md) | Implementing | RFC-0064 | **RFC-0064 Batch 5 — governance integration + shaping home.** Three integrated deliverables in one PR: (1) `new-rfc` Accepted path gains a workspace queue-write step — prompts "Add implementation specs to `workspace.toml` queue?", helps append `{path, needs}` entries to `["<initiative>".work].queue`, degrades gracefully if `workspace.toml` absent; (2) four `docs/product/` subdirectory seeds — `projects/_template.md` (project index template), `findings/` (register schema seed ahead of M3's register population), `initiatives/_template.md` (initiative brief template), `shaping/` left untouched (already seeded in Batch 2); (3) `docs/product/workspace-toml-deps.md` — reference doc for the D7 dependency model: inline `{path/slug, needs}` format, three cross-queue prefixes (`work:`/`shape:`/`brief:`) + two shaping-type prefixes (`research:`/`strategy:`), cross-initiative prefix, `check-workspace` as display surface, `work-loop` DAG enforcement deferred to post-M1 milestone. Pure content change — no code, no new dependency, no build step. Integration-shaped; goal-based + manual QA. 3 ACs / 6 tasks. Safe to ship any time after Batch 1; `new-rfc` queue-write is a no-op until Batch 2 lands. |
| [`catalogue-runtime-inventory/`](catalogue-runtime-inventory/spec.md) | Draft | ADR-0049, ADR-0021, RFC-0060 | **RFC-0060 implementation — the `agentbundle show <pack>` CLI verb.** Resolves a pack in the active catalogue, walks its `.apm/` tree at call time, and prints the pack's metadata + full sorted skills/agents inventory (`--format json` for tooling). **Derived live** (ADR-0049): persists nothing, changes no schema, touches no `pack.toml`/`plugin.json`/`marketplace.json` — the Claude manifest `skills`/`agents` fields are functional load-paths, not a name inventory (RFC-0060 Evidence), and are ADR-0021 "not ours to own." Full untagged inventory (`show core` lists all skills, not the `[pack.evals].skills` subset). Honest state-differentiated degrade on an unresolvable catalogue: an *installed* pack recovers its inventory from the install state file's projected paths (`source: installed-state`); a *not-installed* pack errors cleanly. One shared `.apm/` walk primitive consumed by both the command and `build/lint_packs.py`. Reuses `resolve_catalogue`/`_discover_pack_dirs`/`PackState`/`projected_paths`. No new dependency. 10 ACs / 5 tasks. Spec/plan PR now; the build is the gated follow-on `work-loop` run. |
| [`platform-site/`](platform-site/spec.md) | Implementing | aesthetic-direction.md, design-system-foundations.md | **Marketing-anchored platform site — Astro at `/` + MkDocs reference at `/docs/`, one GitHub Pages deploy.** Phase 0 (amber-gold CSS token swap) done except global primary-button override. Phase 1 (Astro scaffold + 9-section homepage + CI pipeline) blocked on `web/` RFC. Phase 2 = 14 pack pages + 3 core journey pages. Phases 3–4 = 11 remaining journey pages + SEO (all content-gated). Full site inventory in `spec.md` § Site inventory. |
| [`extraction-tier0-and-output-contract/`](extraction-tier0-and-output-contract/spec.md) | Shipped | RFC-0058, ADR-0045, ADR-0034, RFC-0007 | **RFC-0058 floor-first slice — the no-ML Tier-0 floor + unified output contract + D7 formats.** Re-architects `file-to-markdown` so a locked-down (ML-banned) environment has a working path: Tier-0 digital-PDF text via `pypdf` (the one sanctioned new dep), Office (docx/xlsx/pptx) via ordinary parsers degrading to stdlib `zipfile`+XML, and D7 cheap-win formats (HTML/EPUB/CSV-TSV/ODT-ODS-ODP/`.eml`) — all no-ML. **D3 unified output contract** is additive-not-breaking: keep every key the image branch emits today (byte-stable), add top-level `contract-version` + `tier`, and have both output shapes (`convert.py` document+image-via-Docling, `reconcile.py` diagram branch) adopt it via one shared `contract.py` builder — the existing hand-rolled emitter kept (no PyYAML) and made injection-safe by escaping (incl. newlines), so extracted content can't forge/truncate the block. Sparse-text self-assessment marks `requires-review` + names Tier 1 as the escalation target (Tier-1 rasterization is the deferred `extraction-general-image-mode` spec). Docling stays the unchanged Tier 2. Defensive parsing on untrusted input — XXE-safe parser allowlist, multi-axis decompression-bomb guard, path-join guard, realpath output-path confinement, and a coarse resource ceiling. Pack 0.2.3 → 0.3.0. 13 ACs / 8 tasks. **`msg-to-markdown` (Node.js) is out of scope** — its contract adoption is a follow-on Python-port slice (backlog `extraction-msg-to-markdown-python-contract`), not a JS re-implementation that would fork the contract; D4/D5/D6 deferred to later RFC-0058 slices. **Shipped** — converters 0.3.0 (PR #480); D4 + Tier-1 is the `extraction-general-image-mode` slice. |
| [`extraction-general-image-mode/`](extraction-general-image-mode/spec.md) | Shipped | RFC-0058, ADR-0045, ADR-0034, RFC-0007 | **RFC-0058 second slice — D4 general image/PDF-page text/table mode + Tier-1 rasterization.** Extends the *existing agent-orchestrated image branch* (no new engine): a sixth `reconcile.py` strategy (`text-table`) routes the non-diagram case (prose, table, form, receipt, scan) through an in-session **agent-vision** read emitting Markdown prose + tables, and a new `rasterize_pdf.py` renders a scanned/image-only PDF page-by-page into that read path — the concrete path the floor's `escalation-target: 1-agent-vision` points at. Reuses `split_image.py` tiling + `reconcile.py`'s translate→dedup→order pipeline (with a **new general-mode keying function** — prose/rows lack the diagram `name` IoU keys on) + the `contract.py` builder; emits `tier: "1-agent-vision"` + `content-category: general-text-table`. **Load-bearing new dep: `pdf2image` (MIT)** — pip-on-demand behind `--check` with a pinned floor version + SCA-gap note; wraps a separate-process system Poppler (GPL, no propagation); **`pymupdf` rejected (AGPL)**, `pypdfium2` (Apache/BSD, self-contained) flagged as a follow-on if the Poppler prerequisite bites. Security boundary: rasterized/extracted text is **untrusted data** — the read reference delimits it (`<document_content>…`) and directs transcribe-not-obey (OWASP LLM01; the body is *not* escaped — containment is leading-block-only/first-fence). Honest confidence/`requires-review`; **cross-checked** against the `pypdf` text layer (whole-document Jaccard overlap, pinned threshold) to bound hallucination. Adds `safe_io.confine` to `reconcile.py`'s currently-unconfined writes + confines the rasterizer work dir; multi-axis rasterization ceiling (DPI + cumulative bytes + a calibrated page cap, not the floor's 5000). Tier 1 = "no *new* egress, not no egress"; Tier 3 unreachable. Pack 0.3.0 → 0.4.0. 12 ACs / 6 tasks. Spec-mode review Clean (adversarial 8→3→0; security 6→1→0); implementation review Clean (adversarial 6→0, + security-reviewer + quality-engineer). D5/D6 deferred to `extraction-higher-tiers`. **Shipped** — converters 0.4.0. |
| [`extraction-higher-tiers/`](extraction-higher-tiers/spec.md) | Draft | RFC-0058, ADR-0045, ADR-0034, RFC-0007 | **RFC-0058 third & final slice — D5 opt-in higher-fidelity + D6 structure/chunking; the data-egress-boundary spec.** Three opt-in, off-by-default capabilities bolt onto `file-to-markdown` with the default one-command path unchanged. **(1) Docling enrichment** on the Tier-2 path — formulas→LaTeX (`do_formula_enrichment`), code (`do_code_enrichment`), figure classification/captioning (`do_picture_classification`/`do_picture_description`) — forced **local-model-only**: `enable_remote_services` never set / no remote-VLM option (an attribute-level guard catches renamed symbols), closing the covert-egress-inside-Tier-2 path. "vision-model reads" folds into local captioning — no new higher-fidelity vision mechanism beyond the shipped Tier-1. **(2) Tier-3 managed-API OCR** = **option B, transport-free**: the skill ships **no HTTP client / vendor SDK** and makes no call; it provides the declaration-gated interface (stamps `tier: "3-managed-api"` onto adopter-obtained OCR text), the egress-declaration schema (`{endpoint-allowlist, residency-region}`), guardrails, and doctrine — the adopter's transport does the call. Tier 3 is **never auto-reached** (`contract.TIER_3` constructed only in `tier3.assemble_tier3` via explicit `--tier3`; `dispatch()` routes only Tiers 0–2). Declaration validation rejects wildcard/scheme-less catch-alls **and** loopback/link-local/private/metadata-IP targets (SSRF-adjacent), rejects unknown fields (no auth material), echoes endpoint (comma-joined scalar) + region through the injection-safe emitter; Tier-3 output defaults `requires-review: true`. Retention/no-train + transport-binding + redaction-out-of-scope are recorded adopter controls in the Tier-3 grounding doc; the optional pre-egress redaction hook (RFC Open-Q3) is deferred to backlog. Enriched captions treated as untrusted model content (contract-non-forgery). **(3) D6 chunking** — Docling `HybridChunker` emitted **as-is** to a `<basename>.chunks.jsonl` sidecar (full contract field set as JSON, not the YAML emitter); section-aware Markdown below Tier 2; no neutral chunk schema (Open-Q1). No new pack-declared dep (enrichment models / `docling-core[chunking]` tokenizer / vendor all adopter-provisioned; ADR-0034). Pack 0.4.0 → 0.5.0. 13 ACs / 6 tasks. Spec-mode review Clean (adversarial 2→0; security 2→0), `security-reviewer` gates spec + diff. |
| [`extraction-msg-to-markdown-python-contract/`](extraction-msg-to-markdown-python-contract/spec.md) | Draft | RFC-0058 (Open-Q2), ADR-0045, ADR-0034, RFC-0007, ADR-TBD | **RFC-0058 Open-Q2 slice — port `msg-to-markdown` from Node.js to Python so it adopts the unified output contract via `file-to-markdown`'s shared `contract.py` builder** (deferred from the floor spec because a Node skill can't import the Python builder, and a JS fork would break the single-builder property). Swaps the `@nicecode/msg-reader` npm dep + Node runtime for a **permissive** pure-Python `.msg` reader — default `msg-parser` (BSD, `olefile`-based), fallback `olefile`+hand-rolled MAPI; **`extract-msg` excluded on license (GPL)** — pip-on-demand behind `--check` with a pinned floor + SCA-gap note, recorded in a new ADR. The shared modules (`contract.py` + `safe_io.py`) are **vendored by copy with a byte-identical drift-guard test** (drift *detected*, not structurally prevented — a documented weakening, since there's no cross-skill shared-lib mechanism); the HTML reducer is **re-implemented** (the floor's is an inline class), so no `file-to-markdown/` source is touched. **Folds in the deferred richer `.eml`/MIME** (multipart, nested `message/rfc822`, richer headers) through one internal email-render path, with a cross-path frontmatter-parity check against the floor's unchanged flat `.eml` route. Untrusted-input posture: a `.msg` is an **OLE2/CFBF file, not a zip**, so the skill enforces its **own** resource wrap (cumulative-decompressed + per-stream/LZFu-RTF + OLE2 stream-count caps) around the stale reader; **closes the Node attachment-filename traversal sink** (basename via both path flavors + `safe_io.confine`); bounds embedded-message recursion by pinned depth/count; no XML-payload parse; no egress. **Content-parity check** vs. a captured Node baseline over a generated corpus + a numbered real-world sample (BCC a pre-declared allowed difference). Pack 0.4.0 → 0.5.0. 12 ACs / 8 tasks. Spec-mode review Clean (adversarial 3B+4C+2N; security 2B+4C+1N — both re-reviewed after revision). Spec/plan PR now; implementation is the gated follow-on. |
| [`architect-diagram-product-types/`](architect-diagram-product-types/spec.md) | Shipped | none | **Adds three new Mermaid diagram types to `architect-diagram`** — `timeline` (roadmap/chronology), `quadrantChart` (2×2 prioritization/positioning), and `mindmap` (hierarchical decomposition) — as first-class, intent-routed options joining the existing C4/sequence/state/ER/flowchart set. Each gets its own on-demand `references/mermaid-<kind>.md` syntax file, a row in `notation-routing.md`, a rubric section, and a per-type complexity budget. Because all three are *newer* Mermaid grammars with uneven enterprise-wiki rendering, they carry the same **rendering-support caveat the skill already applies to `architecture-beta`** — flowchart/C4 stay the defaults. Also sharpens the diagram rubric with explicit per-type node/accent/edge budgets. Mermaid-only + additive: no new dep/module/dir; the editorial SVG/HTML output path (pyramid/venn/org-chart) is explicitly **deferred to `docs/backlog.md`** for a future RFC. Every reference example is `mmdc`-parse-verified. architect 0.9.0→0.10.0. 9 ACs / 7 tasks. Spec-mode review Clean (8→0); implementation review Clean (adversarial + quality-engineer). |
| [`install-state-visibility/`](install-state-visibility/spec.md) | Shipped | RFC-0052, ADR-0039 | **agentbundle CLI install/upgrade state visibility (CLI 0.10.0).** A new read-only `list-installed` command (pack · adapter · scope · installed · latest · status across both scopes; up-to-date check on by default, degrades to `unknown` offline with `--no-check`/`--offline`; opt-in `--check-drift`), plus three upgrade-messaging fixes: per-adapter versions in the multi-adapter disambiguator (also `diff`/`uninstall`); an honest recap that prints `re-applied: … (already current)` instead of `upgraded: X -> X` for a same-version re-apply (with companion count when local edits are preserved); and an upfront drift notice before a whole-pack confirm. Row-scoped drift via `PackState.file_sha` (not `safety.classify`); no state-schema bump, no new dep/module. Bumped agentbundle 0.9.0 → 0.10.0 (+ README + changelog). 14 ACs / 7 tasks. Spec-mode review Clean (6→1→0); implementation review Clean (6→1→0). |
| [`discovery-loop/`](discovery-loop/spec.md) | Shipped | RFC-0053, RFC-0048, RFC-0051, RFC-0049, RFC-0050, RFC-0041, RFC-0040, RFC-0025, RFC-0019, RFC-0036, ADR-0022, ADR-0042, ADR-0031 | **RFC-0053 implementing spec (Accepted 2026-06-30) — child 5 of the RFC-0048 series; the single implementing spec the RFC's § Follow-on names.** The upstream **discovery loop**: a `discovery-lead` **agent** + a `discovery-loop` **skill** + a carried, `schema_version`-stamped **sidecar schema** (`references/`, in the skill — **not** `core`) + a carried **plan-tree template asset**, all in `product-engineering`, shipping the no-engine coordinator contract (D1) as content the harness runs. Owns: the typed sidecar (D2 — blackboard / open-questions / traceability / decision-log / meta, three partitioned status namespaces, per-verdict cross-namespace write-set, one-tree-per-initiative forest, `_state/` vs committed-durable, per-round/gate checkpoint to the harness store not main); the gate state machine (D3 — consent-gate pause + option card, rejection/recovery cascade-invalidation, the **typed verdict set** approve / approve-with-constraint / redirect / explore-alternatives / abandon / park / extend-override, the impact-before-blast + no-jumping-ahead guards, two-tier persistence, the resume design); the bounds (D4 — round cap + cost budget per-initiative + per-node, concentration + depth/breadth, every bound a pause-and-confirm/override gate); the supervisor topology (D5 — solo ↔ lens-team, blackboard-coordinated never chat-to-consensus, the loop-scoped `discovery-threat-reviewer` / `discovery-reliability-reviewer` roster, CHARTER ceiling stays a code-review cap); the D6 exploratory scaffold (two thin PE skills `explore-options` + `plan-validation`, the `de-risk-intent` / `decompose-intent` extensions, provisional-spine emission — *converged ≠ validated*). Carries the **Security & integrity contract as hard ACs** (verdict write-authority via an agent-untokened harness-attested channel; append-only attested decision log; non-degradable security lens; lens-write integrity; cascade circuit-breaker; `reversibility-class` enum; slot data-classification). Wires the seams (G3 → `work-loop`; the self-coverage gate as the full-seven-module pre-G2 phase per RFC-0051; the traceability slot consumed by child-4's lint with its **root→leaf reachability** dependency named; the backlog bridge) and **owns DRIFT-G** — the `new-spec` `Discovery:` up-edge header + `type:` markers (spec-format only, `CONVENTIONS.md` § 4) then the traceability `--strict` flip, producer-before-consumer, **resolving RFC-0048 acceptance blocker #4**. Folds in traditional requirements (BRD/PRD/SRS/RTM crosswalk as skill guidance; `receive-brief` / `frame-intent` ingest; the traceability slot **is** the RTM; enterprise-format emit rides the RFC-0036 converters). Gates on a full **Diátaxis guide set** (adoption risk is as real as build risk) and an **empirical validation run** forcing the modelled-not-run **concentration-bound + pause-at-bound-resume** path on a ≥2-level recursive tree against the actual discovery reviewers. **No engine/scheduler/service/solver, no new reviewer beyond the roster, no `core` bump, no `CONVENTIONS.md` operating-model section.** ~40 ACs / 10 tasks across the phases. Spec **Shipped** — RFC-0053 implemented; the `product-engineering` discovery-loop pack is built. |
| [`experience-pack/`](experience-pack/spec.md) | Shipped | RFC-0050, ADR-0038, ADR-0042, ADR-0030, ADR-0028, RFC-0040, RFC-0037, RFC-0033, ADR-0024 | **RFC-0050 implementing spec (Accepted 2026-06-29) — D1–D10, one implementing spec.** Grows the design/UX seat into the **`experience` pack**: renames `design-craft → experience` (dirs/manifests/guides/cross-links + `0.1.1 → 0.2.0` + marketplace re-aggregation + the agnosticism CI lint `lint-design-craft-agnostic.py → lint-experience-agnostic.py` retargeted to `packs/experience/`, env `DESIGN_CRAFT_ROOT → EXPERIENCE_ROOT`, RFC-0033 docstring + `(design-craft-pack AC8)` step tag **left pinned**; **no install-time alias**; frozen governance bridged by the already-Accepted **ADR-0038**, not edited) (D1). Adds the connective trio **`map-customer-journey` / `blueprint-service` / `map-screen-flow`** (outcome → journey → screen → backing service; per-screen briefs via the `0048-notes/07` template; cross-brief consistency pass + low-fi-prototype/steel-thread; D2/D3/D4), the inside-out **`map-internal-process`** (APQC L3→L4, as-is/to-be + delta, SIPOC + swimlane + pain/waste; D9), and the behavioral-pillar craft skill **`interaction-design`** (feedback/timing, input/forms, component state machines, motion/micro-animations, navigation-as-behavior, gesture, cognitive-law fit; references onboarding/search patterns; enriches the per-screen brief, no own artifact; D10). **Promotes the whole three-part `quality-floor.md`** (handle-all-states + accessibility + reduced-motion) to a pack-shared reference (D2). Grounds **`aesthetic-direction`** (persona + precedent + standards + platform) and adds a **taste mode** to **`design-critique`** (stays an interactive authoring-time skill; D4/D5). Wires **`voice-and-microcopy`** (stays in `product-engineering`; PE **`0.7.0 → 0.8.0`**, the RFC's `0.6.0→0.7.0` superseded by an intervening bump) to the screen inventory + cross-links (D5/GAP-C1). Ships the forked-context **`experience-reviewer`** agent (collision-hardened name, design-time-only `description` cue, review list carries the full quality floor incl. accessibility; decided within **ADR-0042**; amends RFC-0048:236; D7). Adds the **`[experience]` layout table** (`parent = "docs/design"`; D6), the **optional design-tool handover** (instructions not pixels; D8), and the eval surface (trigger + Tier-4 judge per RFC-0037). **Pure-markdown method + manifests + one CI-lint rename — no runtime/hook/validator, no values tables, no pixels** (RFC-0033/ADR-0024 guardrails unchanged). User-scope-default: re-aggregates `marketplace.json`, not projected to this tree. Goal-based + manual-QA. **Builds the pack only** — not the RFC-0048 discovery loop / traceability lint / a user-research producer. 14 tasks; spec/plan PR now, the build the gated follow-on. |
| [`adr-template-right-sizing/`](adr-template-right-sizing/spec.md) | Shipped | RFC-0056, RFC-0038, ADR-0027 | **RFC-0056 implementation (track 2) — the gated follow-on of the `new-adr` critique track 1 deferred.** Adds three **optional, lean** fields to the ADR template that track-1 foreclosed because they change the format ADR-0027 froze: a first-screen `## Decision summary` block (R2/D1, optional-deletable, length-keyed), a structured `**Revisit if:**` trigger canonical in Consequences and mirrored in the summary (R6/D2, renames the ad-hoc `Neutral / to revisit` bullet; `stable — no foreseeable trigger` a valid value), and a `Mode / Signal / Owner` sub-structure for the existing optional Confirmation with `Mode: none` an explicit value (R7/D3). Each sourced to a lean precedent (Y-statement / Nygard / MADR 4.0) — **no MADR-full** (no per-option pros/cons, no options-first ordering), **no CONVENTIONS § 2 change** (status vocabulary unchanged, D5). Authors the three format-dependent usability evals track 1 left un-wireable + syncs the how-to guide. Recorded via the RFC-0055 vehicle: a dated, Approver-signed `## Errata` on the frozen RFC-0038 + a follow-on ADR-0041 that **extends** ADR-0027 (`Related:`, not `Supersedes:`) and dogfoods the three fields; RFC-0056↔ADR-0041 linked via RFC-0056 `## Errata` (default) rather than a frozen-body edit. Prose/template/JSON only — no code, no dependency/module/dir. Approver decisions: ships under **`0.4.0`** (no bump, 0.4.0 released carrying track-2); RFC-0056↔ADR-0041 link recorded by filling RFC-0056's `## Follow-on artifacts` placeholder in place (no RFC-0056 errata). Goal-based + manual-QA. 13 ACs / 8 tasks; spec/plan PR now, T1–T8 the implementing PR. Spec-mode adversarial review converged Clean (5 findings → 1 → 0). || [`rfc-correction-convention/`](rfc-correction-convention/spec.md) | Shipped | RFC-0055 | **RFC-0055 implementation — the RFC amendment & errata convention.** Adds a single, lifecycle-keyed convention for recording post-publication RFC corrections to the `new-rfc` skill: **Errata** for Frozen RFCs (Accepted/Rejected) / **Amendments** for in-flight Open RFCs (D1); an optional, threshold-gated two-layer structure — authoritative *Current state* over a dated *History / audit trail*, current-state wins (D2); append-only with no per-entry ritual, an optional Amendments-only in-place reword (`*(Superseded: …)*`), and whole-RFC replacement out of scope / recorded as an Errata entry naming the superseding RFC (D3). The **skill is the sole source of truth** — `SKILL.md` (procedure) + `assets/rfc.md` (an optional, clearly-conditional commented scaffold); **no `CONVENTIONS.md` change** (D4) and **forward-only**, no retrofit of the existing 24 sections (D5). Repo-only how-to dogfood note rides along; `governance-extras` 0.4.0 changelog entry, no version bump (packs are usable unreleased). Prose-only — no executable behavior, no dependency/module/dir. Goal-based + one manual-QA dogfood walk against RFC-0048 / PR #430. 9 ACs / 5 tasks; one PR. || [`new-rfc-readability/`](new-rfc-readability/spec.md) | Shipped | RFC-0014 | **Human-readability polish for the `new-rfc` skill (B-narrow).** Three RFC-0014-clean refinements, no spine/flow change: a **body-as-argument / proof→`NNNN-notes/`** split rule; a reviewer-friendly **`REVIEW READINESS` handoff summary** that keeps every pre-handoff gate check but links the heavy proof instead of dumping it; and **short, identifying RFC titles** (explanation lives in "The ask"). `governance-extras` 0.3.1→0.3.2. The richer changes the source critique proposed — a decision-weight field, a reviewer-brief section, decisions-as-table, a shape phase — each reverse or revise a frozen RFC-0014 decision (esp. the decision-weight field RFC-0014 explicitly cut as ceremony) and are **deferred to a follow-on RFC**. 8 ACs / 5 tasks; one PR. |
| [`new-rfc-two-humans/`](new-rfc-two-humans/spec.md) | Shipped | RFC-0054, RFC-0014 | **RFC-0054 implementation — the deferred half of the `new-rfc` human-consumption work.** Four changes to the `new-rfc` skill + `assets/rfc.md` template, each implementing one accepted RFC-0054 decision and framed around the skill's **two humans** (author / reviewer): **D1** a `Decision weight: light \| standard \| heavy` header field that right-sizes research depth + the pre-handoff gate (an author-applied prose heuristic off `work-loop`'s risk triggers, default `standard` — **no engine**), reversing the structural right-sizing field RFC-0014 cut; **D2** a top-of-doc `## Reviewer brief` orientation section above "The ask" (the chat-only `REVIEW READINESS` summary stays chat-only — B-narrow's back door stays closed); **D3** decisions rendered as a table; **D4** a guided shape/intake step before the research gate (offer-don't-force). The **load-bearing constraint**: weight-based trimming reduces research depth + draft size, **never** a mandated gate check — all five RFC-0014 pre-handoff checks are clause-pinned to survive at every tier. Resolves both RFC open questions per their recommended defaults. Prose + template only (no lint/hook/engine/dep/dir). `governance-extras` 0.3.2 → 0.4.0; how-to guide + Tier-4 eval (three output-observable assertions) + changelog ride along. 9 ACs / 5 tasks; one PR. Spec-mode review converged Clean over 3 passes. || [`new-rfc-fresh-context/`](new-rfc-fresh-context/spec.md) | Shipped | RFC-0014, RFC-0054 | **Third `new-rfc` readability wave — reads from zero context + decidable-in-chat decisions.** Four additive, frozen-decision-clean refinements to the skill (no spine/flow/section-set change): (1) a **fresh-context drafting principle** — gloss every project-coined term / acronym / sibling-RFC back-reference on first use, inline (not a new section), so an RFC reads cold (RFC-0053 is the exhibit — it had to be hand-patched with glosses after drafting); (2) a **decidable-in-chat decision handoff** — expand step 4's `RESEARCH FINDINGS` chat block so each decision carries question + options-with-trade-offs + recommendation-with-why + cost-of-each, decidable without opening a file; (3) a **no-context readability check** in the pre-handoff gate — a *generic* context-denied subagent dispatch (only the RFC text, told not to read project docs/sibling RFCs) that lists unresolvable terms, additive to the mandatory `adversarial-reviewer`, result/skip recorded in `REVIEW READINESS`; (4) two new anti-patterns. Eval: a fresh-context assertion on the existing eval + a new `evals.json` entry for the handoff (`eval_queries.json` untouched). `governance-extras` 0.4.0 → 0.5.0; how-to guide + changelog ride along. Prose + eval only — no code/dep/module/dir. 9 ACs / 4 tasks; one PR. Spec-mode review converged Clean (2 blockers → 2 nits → 0). |
| [`shared-prefix-aware-multi-adapter-install/`](shared-prefix-aware-multi-adapter-install/spec.md) | Shipped | RFC-0052, ADR-0039, ADR-0040, ADR-0002 | **RFC-0052 implementation spec.** Makes the unit of install identity the **footprint** — the relpaths a `(pack, adapter, scope)` install writes, each content-SHA'd — not the pack name, so one pack installs for multiple adapters at one scope (fixes the adapter-blind "already installed" gate). State schema **v0.3 → v0.4** keyed `[pack.<name>.adapters.<adapter>]` with a **hard cross-version refusal** (read + write). Adds a **`shared` prefix class** (`.agents/skills/` cohort {codex,cursor,gemini,copilot}; `.kiro/skills/` cohort {kiro-ide,kiro-cli}) and routes cohort skills to `.agents/skills/` (ADR-0040, supersedes the skill-home sub-decision of ADR-0013/0015/0016). **Intra-pack co-ownership** (same-pack + same-SHA → one copy, removed only at last owner); a same-path/different-SHA or any cross-pack collision **refuses** with named paths, `--force` → existing Tier-2 `safety.write_companion` `.upstream`. Ownership **derived not stored**; every per-file reader (`classify`/`projected_paths`/`file_sha`/`scan_for_pack_artifacts`) moves off first-owner. **Governance-docs PR** (this spec/plan + ADR-0039/0040 + RFC-0012 erratum); T1–T7 are the implementing PR. 26 ACs / 7 tasks. || [`frame-domain/`](frame-domain/spec.md) | Shipped | RFC-0048 | **The frame-domain primitive (RFC-0048 Decision 4 / child 2).** A new prompt-only skill `frame-domain` in `product-engineering` that produces two typed artifacts grounding a product in its real-world domain *before* any screen/service/architecture is drawn — the biggest correctness lever against domain-hallucination and over-scoping. **Domain Framing** (canonical filename `domain-framing.md` + frontmatter `type: domain-framing`): a **real-world-activity half** (how the activity is really done · best practice · naive-failure modes, grounded by wrapping `research` applied mode) and a brownfield **current-system half** (reverse-engineered via `decision-archaeology` + architecture extraction). **Scope Boundary** (canonical filename `scope-boundary.md` + frontmatter `type: scope-boundary`): the MVP appetite + **out-of-scope register** (each excluded capability + its appetite reason) — the upstream G1.5 scope-creep guard the brief inherits/refines at G3. Each carries a stable marker and resolves its write path config → default → discover-by-marker; ungrounded findings surface as named residual assumptions, never silent assertions. Standalone-useful; non-skippable gate-enforcement deferred to the coordinator (child 5). Prompt-only (Charter Principle 3). 10 ACs / 4 tasks; one PR. |
| [`traceability-lint/`](traceability-lint/spec.md) | Shipped | RFC-0048, RFC-0040, RFC-0019, RFC-0025, RFC-0030, ADR-0022, RFC-0049 | **RFC-0048 child-4 — the traceability lint.** A stdlib-only `core` `work-loop` skill script (`lint-traceability.py`, projected to every adapter like `lint-spec-status.py`) that detects **structural orphans** along the chain `outcome → opportunity → capability → screen → action → service → contract → spec → component` — a node present but with no producer above (backward orphan = scope-creep signal) or consumer below (forward orphan). Generalizes `receive-brief`'s `lint-brief-coverage.py` from the single brief↔spec edge **in one repo** to the nine-layer chain **across repos** — because the loops span repos (`work-loop` per-module; `discovery-loop`/release-loop same-or-cross-module). Crossings are handled **by convention, not by path**: every node carries a stable, location-independent id (marker slug / Backstage `kind:namespace/name` / `contract@version`) in a conventional pointer field (`Discovery:`/`Brief:`/`parent-intent:`, `Component:`), reusing the value-stream cross-repo mechanism (ADR-0022: reference-by-version + read-only courier snapshot, the rollup row schema, `unknown / not-yet-catalogued`) rather than inventing one. Each edge endpoint resolves to one of **three** states — local / **satisfied-by-reference** (a well-formed pinned cross-repo pointer — never a dangling edge) / unresolvable (reported `unknown / not-yet-catalogued`, never fatal). Nodes in two realizations: file-backed (`screen`/`contract`/`spec`/`component`) and container-embedded (`outcome`/`opportunity`/`capability` in the intent ladder, `action` in the journey, `service` in the blueprint). Defect classes: structural orphan (informational; exit 1 under `--strict`, the G2/CI "traceability closed" gate, O6), **dangling edge** (missing-local-target/malformed, exit 1 always), **cycle** (exit 1). Two run postures (single-repo + meta-repo/federated). Marker/path resolution is three-tier (config → default → discover-by-marker). **Semantic scope-creep is out of scope** — deferred to the coordinator spike (O10), the human call at G1.5; grounded in the structural-vs-semantic traceability split (SoK / ReqToCode / RTM). The `Discovery:` header (O8) + frontmatter `type:` markers + the sidecar schema are out-of-scope dependencies (RFC-gated); drift ships warn-only until pinned. Wide prior-art research in `notes/`. 18 ACs / 8 tasks. Shipped (`core`; + the root→leaf reachability pass). |
| [`release-loop/`](release-loop/spec.md) | Shipped | RFC-0049, RFC-0048, RFC-0053, RFC-0041, ADR-0031, ADR-0018, RFC-0025, RFC-0040 | **RFC-0049 implementing spec (RFC Accepted 2026-06-30; spec Shipped) — the deployed e2e outer loop + the minimum-regret deploy carve.** A new opt-in **`release-engineering` pack** holding a **`release-lead`** agent (the SRE/ops outer-loop supervisor, a peer of `work-loop`'s supervisor + `discovery-lead`) + a **`release-loop`** skill: deploy the integrated whole to an **ephemeral env** → e2e → observe telemetry → feed findings back to `work-loop`'s inner loop (no human relay) → redeploy → **converge by policy** (canary SLOs + e2e coverage of the changed surface + flake < 2%), then stop at the **human gate** for prod ship (G5). Resolves RFC-0049's **OQ1** (pack home → dedicated opt-in `release-engineering` pack, not `core`; `delivery` rejected — work-loop already owns "the delivery track") + **OQ2** (agent shape → distinct `release-lead` agent, not a `work-loop` mode). **Minimum-regret carve:** autonomous on ephemeral (deploy/e2e/iterate/teardown + canary auto-rollback); human at first real users/data, data migrations, spend>threshold, security boundaries, anything irreversible-beyond-MTTR, and prod. **Reuses** `operational-safety` (REVIEW modules + `cloud-implementation-craft`) + `quality-engineer` + `security-reviewer` + the RFC-0053 sidecar schema (consumed by convention; carried in `product-engineering`'s `discovery-loop`, **not** `core`); **no new engine, no new reviewer** (ADR-0031 idiom); runs on the omnigent harness. AC10 carries the **security/integrity contract** (controls a–i + the AC7 artifact-provenance control): verdict write-authority, harness-delegated attested decision-log with an anchored hash-chain tip, non-degradable security lens, telemetry-as-data with an `untrusted:` marking discipline, oscillation circuit-breaker, teardown, deploy-credential tiering (the four-broker `credbroker` boundary), env-isolation precondition, and sidecar data-classification. Reconciles drift into RFC-0048 (§ Amendments 2026-06-30) + RFC-0049 (OQ1/OQ2 resolved, now Accepted); the follow-on **ADR-0044** (inner/outer split + carve) is authored. Shipped in this PR: the `release-engineering` pack (`release-lead` agent + `release-loop` skill), all 15 ACs checked. 15 ACs / 6 tasks. |
| [`self-coverage-gate/`](self-coverage-gate/spec.md) | Shipped | RFC-0051, RFC-0048, ADR-0042, RFC-0041, ADR-0031, RFC-0025 | **RFC-0051 child-3 — the `work-loop` thin slice of the self-coverage gate.** Wires self-coverage's **cross-loop seam** (resolve-vs-surface + a non-skippable coverage record) into `work-loop` as a **skill-resident** slice — **no `docs/CONVENTIONS.md` edit** (operating-model doctrine relocated into the loop skills, RFC-0048 § Amendments 2026-06-29). `work-loop`'s doctrine **names its existing passes as the gate's steps** (REVIEW = fresh-context-adversarial; PLAN assumption trio + declined-pattern register = pre-mortem hook; `Surface` + DECIDE apply/defer = resolve-vs-surface bones) and adds only the net-new at spec time / DECIDE: a **resolve-vs-surface disposition record** + a **conditional domain-grounding** check, under the existing light/full mode (RFC-0025, no second knob), plus **one** end-of-session-checklist refusal item (the record). Seeds a **self-contained** `references/self-coverage/resolve-vs-surface-sample-bank.md` from `0048-notes/09` (inline append-only discipline, no CONVENTIONS dependency). **No heavy seven-module battery under `work-loop`** (that's `discovery-loop`'s, RFC-0053), **no new reviewer / no fourth core-loop lens** (ADR-0042), no runtime lock, no new dependency. Projects via `make build-self`; bumps `core` + changelog. Goal-based + one dogfood manual-QA (produce this spec's own coverage record). 12 ACs / 6 tasks. Shipped. |
| [`windows-build-gate-chain/`](windows-build-gate-chain/spec.md) | Shipped | none | **Make-free cross-platform self-host gate chain.** Adds a repo-native script `python tools/build_gate_chain.py` with two subcommands — `build-self` (lint-packs → self) and `build-check` (lint-packs → build → check → pre-pr-catalogue → lint-spec-status → brief-coverage) — so a Windows contributor runs the whole gate in one command without `make`/bash. It calls the existing `cmd_lint_packs`/`cmd_build`/`cmd_check`/`cmd_self` handlers in-process and spawns repo scripts via `sys.executable` (pure stdlib, no shell); each chain stops at the first failing step and propagates its exit code. It self-bootstraps `sys.path` + `chdir`s to repo root (one command, no env setup). Deliberately **not** an `agentbundle` package subcommand — `build-check` spawns repo-only scripts that never ship to adopters, so the published CLI stays unchanged (no release). The `Makefile` targets route through it so the step lists live once and can't drift; the Windows-incompatible SAST leg stays Makefile-only (appended after `build-check`, `SKIP_SAST` preserved). Direct follow-on to `windows-build-self-entry`. 8 ACs / 4 tasks; one PR. |
| [`infra-grounding/`](infra-grounding/spec.md) | Shipped | ADR-0034, RFC-0044, ADR-0031, RFC-0041, ADR-0023, ADR-0018, RFC-0029, RFC-0025 | **RFC-0044 implementation spec — grounds the infra inner loop in platform reality across six MECE failure families (Author→Provision→Verify→Debug).** Ships **doctrine + reference prose only**: one new protocol-shaped `core` skill **`infra-contract-acquisition`** (a tiered, **tool-keyed not vendor-keyed** oracle protocol — T0 detect → T1 toolchain validate/plan/schema-slice → T2 detect-and-recommend a curated platform skill → T3 docs → Final: the runtime data-plane probe — that **declares its oracle tier + confidence** and degrades across the strong→weak spectrum, with an EXECUTE contract-grounding gate "acquire before you guess"); one new `operational-safety` module **`cloud-implementation-craft`** loaded into the **implementer's EXECUTE brief** (sufficient-permissions / propagation / cold-start-retry / ordering / terminal-failed-state); a **fifth P1 preflight artifact** (durable-credentials-once) + terminal-failed-state convergence + the **reusable-script corollary** (P); **phased oracle fidelity** (V1); a **readiness-aware data-plane probe** refining RFC-0041 P2 (V2); a **symptom→layer log playbook** strengthening the existing `observability-and-smoke` (D, no new module); and `quality-engineer` infra-flavored wiring that **re-derives the contract independently** (Delivery — **no new reviewer / agent**; a dedicated `infra-contract-reviewer` deferred behind an evidence trigger). **No executable tooling, no per-vendor KB** (Principle 1/3). The four-way carve (`infra-contract-acquisition` ↔ `cloud-implementation-craft` ↔ `security-checklists` ↔ the CSPM scanner) is stated. **Governance-docs PR** (ADR-0034 + spec/plan); T1–T9 + the `core` bump + changelog are the implementing PR. 15 ACs / 9 tasks. || [`upgrade-derive-version-confirm/`](upgrade-derive-version-confirm/spec.md) | Shipped | none | **`agentbundle upgrade` UX, breaking.** Removes the `required` `--to` flag (it asserted a version that was never validated against the catalogue's actual `pack.toml`, and there is no version-history store — the catalogue is a single git snapshot). The upgrade target is now derived from the resolved catalogue's `[pack] version`; the command shows `installed → target` and confirms interactively before writing, with `--yes` to skip and a clean non-TTY refusal (never blocks on `input()`). The success recap names both versions. `--dry-run` resolves silently and previews. Docs: PyPI README (`upgrade` usage) + a new root-README Quick start. No new dependency; Tier-1/2/3 write contract, path-jail, and hook-wiring reconciliation untouched. 11 ACs / 3 tasks; one PR. agentbundle version bump surfaced as a release decision. |
| [`consolidated-pack-layout/`](consolidated-pack-layout/spec.md) | Shipped | RFC-0040, ADR-0030, ADR-0029, ADR-0021, RFC-0035, RFC-0038 | **RFC-0040 implementation spec — wires all three consumers in one spec.** Replaces the research-only `research-layout.toml` with one namespaced **`agentbundle-layout.toml`** (one `[<pack>]` table each for `research` / `architect` / `product-engineering`; single `parent` key = a **base** under which each unit of work gets its own topic-named folder). Two locations, **repo-root overrides user-profile per table**; `parent` **anchored by the file's location** (repo-relative for the repo file, absolute for the user file), resolved to a full absolute path and **surfaced before the first write**. Reading stays **prompt-only** (Charter P3 — no engine/index/daemon); the **only** code is an installer **`_append_layout_section`** step (append-if-exists / **never-create** / never-overwrite, peer to `_append_install_marker`) sourcing its default from a **scope-keyed `[pack.layout]`** `pack.toml` table (ADR-0021), serialised via `config._emit_basic_string`, written via `safety.write_jailed`. The file is **adopter-owned, never shipped**; the shipped artifacts are each pack's `references/agentbundle-layout.md` schema doc + the within-pack `[pack.layout]` default. `architect` changes from a loose file to a **per-effort folder**; `product-engineering` stays file-per-slug (briefs stay pinned). `research` migrates by a **clean rename, no alias** (undistributed), 0.4.0→0.5.0. Four **prose-enforced security ACs** (confine + reject `..` + surface; realpath; repo-sourced-out-of-tree = untrusted-origin Ask-first; injection-safe + never-overwrite construction tests). `.gitignore` housekeeping (this repo only). **Governance-docs PR** (ADR + spec/plan); T1–T6 are the implementing PR. 18 ACs / 6 tasks. || [`research-typed-artifacts/`](research-typed-artifacts/spec.md) | Shipped | RFC-0039, ADR-0029, RFC-0038 | **RFC-0039 Decision 2 — the smaller half, lands first.** Renames episodic `research` outputs from the generic `research.md` to topic-typed `<topic-slug>-<type>.md` (e.g. `oauth-pkce-survey.md`), with the canonical type vocabulary (`survey` / `fact-check` / `counterpoints` / `comparison-matrix` / `shortlist` / `blueprint` / `hypotheses`) carried once in the `research` SKILL.md and the `<topic-slug>-` prefix applied across all seven artifact-producing skills. Quick mode stays inline (no file). `research.md` retained as a **one-release forward-only legacy alias** (RFC-0038); the nine live consumers migrate and the **frozen `research-pack/spec.md` is amended** (CONVENTIONS § 4 shipped-spec-as-reference-material; applied-mode amendment precedent), not left stale. Prompt-only (Charter P3): the name is produced by the agent, never code; no new dependency / top-level dir / dual-write. `research` 0.2.0→0.3.0. 7 ACs / 6 tasks. Precondition for `research-project-mode` (bare-name-inside-folder). |
| [`research-project-mode/`](research-project-mode/spec.md) | Shipped | RFC-0039, ADR-0029, RFC-0035, RFC-0034 | **RFC-0039 Decisions 1/3/4/5/6/7 — the lifecycle axis.** Adds a stateful, multi-week **project** mode orthogonal to the depth axis, as **four prompt-only skills** (`research-project-start` / `-digest` / `-synthesize` / `-check`) on the existing `research` pack. A three-layer folder (`sources/` raw → `synthesis-matrix.md` + `memos.md` **digest middle layer**, the capability the pack lacked → typed synthesis) with **emergent/constructed columns** (not fixed pillars), a **soft revisable working hypothesis** (no refuse-without-a-claim gate), **scratch / out-of-repo by default** via an adopter-created `research-layout.toml` (RFC-0035 precedent; never the committed repo tree), **additive Admiralty provenance axes** (optional `reliability` + `credibility` informing the unchanged GRADE + ≥3-source rail), and a single-file self-contained `<topic-slug>-brief.md` governance handoff (answer-first, no cross-links, `## Known unknowns`). `-check` is a **passive** stop-signal (read-by-eye saturation; never advances `phase`; may write only `verdict_status` — RFC-0039 OQ1 resolved). **Prompt-only is a hard boundary** (no engine/index/counter/metric; AC8 guards it). The seven existing skills are **reused as phase ops, not rewritten**. Adds the RFC `NNNN-notes/` companion convention to CONVENTIONS (authorized by RFC-0039 D7). `research` 0.3.0→0.4.0. 14 ACs / 11 tasks. **Depends on** `research-typed-artifacts`. |
| [`pack-activation-evals/`](pack-activation-evals/spec.md) | Shipped | RFC-0037, ADR-0028 | **RFC-0037 Tier-A implementation spec.** Adds repeatable, empirical **activation/selection** testing per skill — the one property the catalogue's all-structural gates never check. Adopts the agentskills.io trigger-eval convention: per-skill `evals/eval_queries.json` (flat `[{query, should_trigger}]`, ~8–10 each way, near-miss negatives); `pack.toml` gains `[pack.evals].skills` as the coverage allowlist; a new `tools/run-pack-evals.py` projects a pack in isolation and runs each query through the convention's `claude -p … --output-format json --allowed-tools Skill` detector, computing a `trigger_rate` over 3 runs graded at 0.5 (intra-pack exclusivity comes free). Runs in a new **schedule + dispatch, report-only** `pack-evals.yml` consuming an `ANTHROPIC_API_KEY` secret — never on the PR path; `make build-check` stays structural. **No new dependency** (stdlib + `tomllib` + the `claude` CLI), no new top-level dir, runner is catalogue-internal (Principle 3). Code preconditions: relax `lint-skill-spec.py` (`check_evals`) to accept an `eval_queries.json`-only skill + a coverage lint over `[pack.evals].skills`. First cut covers `core` (excluding the reviewer-internal `security-checklists`) + `converters`. **Headless-only**, measuring **claude-code as the reference harness** — a proxy for the byte-identical `description:` projected to every adapter; GUI-only IDEs (Kiro IDE, Cursor IDE) are out of scope (no headless surface; an in-editor `.kiro.hook` driver was rejected as runtime infra), and codex/copilot/cursor-agent/gemini are additive headless detectors later behind a `Detector` seam. **Tier B** (output-quality grading) + the report-only→gate promotion + train/validation split are deferred. 22 ACs / 8 tasks (T8 follow-on). **Ships in two PRs:** this spec/plan + **ADR-0028** (Accepted) PR, then the implementing PR (T1–T7). || [`credential-setup-credbroker-guard/`](credential-setup-credbroker-guard/spec.md) | Shipped | RFC-0023 | **Tier-1 detect-and-guide guard.** `credential-setup`'s `setup.py` imports `credbroker` eagerly at module top; when the package is unresolvable it now prints a clear ASCII stderr message naming the editable-from-checkout install (`pip install -e ./packages/credbroker`) and exits the already-documented code `3` — no traceback. The except arm narrows to `ModuleNotFoundError` whose `.name` is exactly `credbroker` and re-raises anything else (a broken submodule / wrong version is a different fault), and never echoes the exception (no `sys.path`/home-dir leak). Rides along: fixes the prose drift that still calls `credentials_shim` the `auth: creds` resolver (`pack.toml`/`plugin.json`/`README` → `credbroker`; RFC-0023; `credentials_shim.py` kept as the `sso-broker` companion loader, frozen `docs/rfc/0013` untouched), bumps the pack `0.1.2`→`0.1.3` + regens `marketplace.json`, adds the `SKILL.md` exit-code line + a changelog entry, and wires the previously-ungated `test_setup.py` into the credbroker CI job. 10 ACs / 4 tasks. |
| [`pack-profiles/`](pack-profiles/spec.md) | Shipped | RFC-0034, ADR-0025, RFC-0004, RFC-0011, RFC-0001 | **RFC-0034 implementation spec.** `agentbundle install --profile <name>` + `list-profiles` install a curated, **single-scope** `profiles/<name>.toml` (catalogue root, id = filename stem, declares `scope = user\|repo`) in one command — a user-scope role toolkit (`solution-architect`) or a repo-scope setup bundle (`full-ceremony`), **never mixed**. Thin orchestrator over the existing per-pack resolve→check→write path: ordered deps-first, all-pre-flight-before-any-write, one adapter pinned per batch, already-installed skipped, `install_route="profile"`. One small `install.py` change (batch-aware param on `validate_dependencies_required`), one `.py` lint (scope-homogeneity + dep-completeness + order-validity), a `profile.schema.json`. **No** state entity, **no** state-schema bump, **no** adapter-contract bump, **no** marketplace.json/build/self-host change. 15 ACs / 6 tasks. Depends on RFC-0034, ADR-0025. || [`design-craft-pack/`](design-craft-pack/spec.md) | Shipped | RFC-0033, RFC-0007, RFC-0004, RFC-0032, ADR-0024 | **RFC-0033 v1.** A new opt-in, **user-scope** `design-craft` pack for interaction/visual designers — authors of upstream **design intent** the build consumes (the design-side twin of `product-engineering`'s seam). Ships **four pure-markdown skills** — `aesthetic-direction`, `design-system-foundations`, `layout-and-information-architecture`, `design-critique` (`<100`-line SKILL.md + `references/`) — plus a shared **`quality-floor` checklist** (handle-all-states + accessibility floor + reduced-motion *principle*) referenced by the authoring skills and applied by `design-critique` (the design-side `architect-review`, a **skill**, not the RFC-0032 subagent twin). **Strictly framework-agnostic**: zero React/Vue/Tailwind/CSS, no Framer Motion, **no values tables** (px/ms/hex/easing/token sets) — method only, per RFC-0033's Guardrail A (point to WCAG / W3C Design Tokens, never reprint values) and Guardrail B (wayfinding/motion as *concepts*, never ARIA/CSS/`@media`). Enforced by a **pack-scoped agnosticism lint** (`tools/lint-design-craft-agnostic.py`, Python for Windows portability) + self-test, wired into both CI workflows — the RFC-0007 grep-enforcement pattern, **not** promoted to a repo-wide convention. `default-scope = "user"`, `allowed-scopes = ["user","repo"]`, `[pack.adapter-contract] 0.12` (matches `research`), all seven adapters; aggregates into `marketplace.json`, not projected to this tree (so `build-self` not required). No `seeds/` (RFC-0004 Rail A; templates ride as skill `assets/`). Habits-not-infra: **no hook/engine/in-pack-validator/reviewer-subagent/new-top-level-dir**; the `design-reviewer` subagent twin is RFC-0033 OQ#2 (a later RFC). Goal-based + manual-QA verification (no TDD — no logic). **Ships in two PRs:** this spec/plan PR, then the build PR (T1–T8) that lands the pack, the lint, guides, and the **ADR** (ADR ships with the build, per the RFC-0030 / RFC-0032 precedent). |
| [`architect-design-reviewer/`](architect-design-reviewer/spec.md) | Shipped | RFC-0032, ADR-0023 | **RFC-0032 implementation.** Ships a read-only, forked-context **`design-reviewer`** subagent in the architect pack (`packs/architect/.apm/agents/design-reviewer.md`) — the convergence loop's *preferred* fresh-context reviewer rung, previously delegated to "the harness's review subagent where it has one" with the explicitly-weaker cold-re-read floor as fallback. Self-contained single `.md` (agents bundle no `references/`) inlining `architect-review`'s verdict scheme / severity glossary / mechanical-judgment taxonomy / genre routing (byte-faithful at authoring time; note points at the skill's `references/` as fuller source); covers **both** review modes; `tools: Read, Grep, Glob` (read-only = "flag, never rewrite" as a tool boundary); findings-only output. Coexists with the `architect-review` skill (cf. `new-spec`+`adversarial-reviewer`); convergence loop stays a **soft** dependency. Wires the convergence-loop rung + revises the README "Subagents — NOT in this pack" exclusion + updates the architect guides; co-lands an **ADR** pinning the charter-ceiling reading ("three reviewers is the ceiling" scopes the core code-review lenses, not opt-in design-side review — no charter edit). architect **0.5.3→0.6.0**; no adapter-contract bump (agents admitted since v0.7); projects across all 7 adapters. 13 ACs / 5 tasks; one PR. Spec-mode adversarial review converged Clean over 2 passes. |
| [`enriched-pack-manifest/`](enriched-pack-manifest/spec.md) | Shipped | RFC-0031, ADR-0021, ADR-0020, RFC-0011, RFC-0001 | **RFC-0031 first spec.** Enriches `pack.toml` with optional first-class metadata (`readme`/`license`/`[[maintainers]]`/`[pack.links]`/`categories`/`keywords`/`display_name`) + a `[pack.metadata.<tool>]` hatch; projects the cleanly-projectable subset into `plugin.json`/`marketplace.json` entries (relaxing `additionalProperties:false` on **both** plugin-manifest schemas) and each pack's README into the dist routes; **declare-only** `@catalogue/pack` identity (ADR-0021); soft warn-not-error `categories` vocab; adapter-contract 0.13→0.14 (optional fields, no legacy migration). Doc surface: README link-out, PyPI READMEs, new `docs/architecture/pack-manifest.md`, and the per-pack `docs/guides/<pack>/` migration (ADR-0020, tasks T12/T13). Populates all **12** packs incl. `product-engineering`. 13 tasks; spec-mode review converged Clean. Depends on RFC-0031. || [`value-stream-meta-repo/`](value-stream-meta-repo/spec.md) | Shipped | RFC-0030, ADR-0022, ADR-0019 | **RFC-0030 phase 2 (the business-unit cross-component value-stream layer).** Extends the `product-engineering` pack (0.1.0→0.2.0) with one new pure-markdown skill **`align-value-stream`** (owns the **value-stream meta-repo** — a coordinating repo with no app code: the Backstage Domain→System→Component→API **federated catalog**, the elicited **contract-authority home**, the shared-contract handoff, the C4/bounded-context architecture seam, and the **cross-component delivery rollup** — unified by *currency*) plus a **business-unit slicing branch** on `decompose-intent` (a feature intent → one `core` brief per component, each carrying `parent-intent:` + a version-pinned **contract reference + courier snapshot** + a `providesApi`/`consumesApi` role; the two skills cross-reference). The **only `core` change** is one additive optional `parent-intent:` brief field (distinct from `Epic:`). The rollup is a **markdown snapshot** (one row per slice → its brief → status + pointer; absent-source rows show `unknown / not-yet-catalogued`, never silently delivered) — **no runtime hub, no live API, no validator, no new subagent**. Hard limits stated honestly (no atomic cross-repo commit, no shared release train, snapshot-not-live). One rollup seed; the federated `catalog-info.yaml` ships as a `references/` sample, not a projected seed. `monorepo-extras` structuring referenced, not restated; wire-contract maturity reuses the existing `Contract:` seam. **ADR-0022** (Accepted) records the decisions; RFC-0030 § Errata records the spec+ADR (not new-RFC) vehicle for open-question #2. 12 ACs / 7 tasks; one PR. Spec-mode adversarial review converged Clean over 3 passes (3→2→0 blockers). |
| [`architect-knowledge-surfaces/`](architect-knowledge-surfaces/spec.md) | Shipped | none | Adds enterprise knowledge-surface awareness to the `architect-design` skill. A new progressive-disclosure reference (`references/knowledge-surfaces.md`) carries an 8-area MECE knowledge taxonomy (domain, landscape, interfaces, operational, standards, patterns, decisions, in-flight) + a **harness-agnostic detection** mechanism (discover retrieval surfaces from the session's tools/CLIs; no hardcoded names). One **conditional** step in `architect-design/SKILL.md` loads the reference only when a surface is detected, and **degrades gracefully** (ask user + lower confidence) when absent — reusing the existing compose-with-`research` framing. No knowledge server/RAG engine, no registry/shared-config, no cross-pack dependency (Route B rejected). architect 0.2.0→0.3.0. architect-review/diagram + a product-engineering sibling deferred (`backlog.md`). |
| [`architect-review-knowledge-surfaces/`](architect-review-knowledge-surfaces/spec.md) | Shipped | none | **Review-side counterpart of `architect-knowledge-surfaces`.** Adds knowledge-surface awareness to `architect-review` as a **verification lens** (not a consult guide): the review **checks that a design was grounded** and flags any landscape/standards/in-flight/interface claim asserted as fact without grounding (no cited surface and no "unverified — confirm" marker), plus any available surface the design ignored — it does **not** redesign or consult-to-author. Reuses the **same 8-area MECE taxonomy + modality×space axis** as the shared canonical core (duplicated per-skill; only the trigger column, lens paragraph, and detection/degrade framing change), in a new `architect-review/references/knowledge-surfaces.md`. One **conditional** step in `architect-review/SKILL.md` loads it only when the artifact asserts grounding-relevant claims; harness-agnostic detection (no hardcoded tools, public web excluded); three honesty rails recast (name-what-checked / never-fabricate-a-ground-truth / one-source-is-weak). No registry/shared-config/cross-pack dependency. architect 0.3.0→0.4.0. `architect-diagram` + the product-engineering sibling remain deferred (`backlog.md`). |
| [`architect-diagram-knowledge-surfaces/`](architect-diagram-knowledge-surfaces/spec.md) | Shipped | none | **Third and final architect-skill sibling of `architect-knowledge-surfaces`/`architect-review-knowledge-surfaces`.** Adds knowledge-surface awareness to `architect-diagram` as an **as-is-drawing consult lens** — like design (consults), not review (verifies) — but scoped to the **descriptive current-system facets only** (areas 2/3/4, the seam) and **only in document + update mode** (never design = the hypothetical; never review = routes to `architect-review`). **Mode-scoping is the load-bearing constraint.** Extends "read the repo" to "read the landscape" so beyond-repo boxes/arrows/edge-labels are grounded instead of guessed, strengthening the skill's existing never-fabricate-names anti-pattern. Reuses the **same 8-area MECE taxonomy + modality×space axis** as the shared canonical core (kept byte-identical across the three full-taxonomy architect copies; only the trigger column, lens paragraph, and detection/degrade framing change), in a new `architect-diagram/references/knowledge-surfaces.md`; the new copy is **registered in `tools/lint-knowledge-surface-parity.py`** (the #302 drift guard, extended here + its self-test) so the core is mechanically guarded. One **conditional** step in `architect-diagram/SKILL.md` loads it only when mode ∈ {document, update} **and** a surface is reachable; harness-agnostic detection (no hardcoded tools, public web excluded); three honesty rails recast for drawing (name-what-drew-from / `<unnamed>`-or-ask / flag-a-contradicted-edge). No registry/shared-config/cross-pack dependency. architect 0.4.2→0.5.0. **Completes the knowledge-surface line** — design/review/diagram and the `product-engineering` sibling (#302) have all shipped. 14 ACs / 6 tasks; one PR. Spec-mode review converged Clean. |
| [`product-engineering-knowledge-surfaces/`](product-engineering-knowledge-surfaces/spec.md) | Shipped | none | **Product-engineering counterpart of `architect-knowledge-surfaces` (PR #297) — same mechanism, problem-framing lens.** A new progressive-disclosure reference (`frame-intent/references/knowledge-surfaces.md`) carries a **strict four-area subset** of architect's eight — (1) business domain & meaning [PRIMARY], (8) in-flight & roadmap [PRIMARY], (2) current landscape [brownfield-only], (4) operational reality [light] — and **deliberately omits** the four solution-design areas (3/5/6/7 = architect's lens) so product-engineering stays in problem space. Keeps a **shared-canonical-core anchor** naming architect's reference as canonical so the copies don't diverge. **Harness-agnostic detection** (no hardcoded tool names) + the same three honesty rails. One **conditional** step in `frame-intent/SKILL.md` loads the reference only when a surface is detected and **degrades gracefully** (ask + lower confidence into the intent's `Assumptions`) when absent — generic compose/degrade framing (frame-intent does **not** compose with `research`). The current-landscape area wires into the existing brownfield maturity gate (`current-state-inputs.md`). Pins the detection audit home in the intent template's `## Assumptions` and — symmetrically — architect's `concept.md` (architect 0.4.1→**0.4.2**), guarded by a new `tools/lint-knowledge-surface-parity.py` CI gate over **all three** copies (architect-design canonical, architect-review, frame-intent). No registry/shared-config, no cross-pack dependency (Route B rejected). product-engineering 0.2.1→**0.3.0** (atop #300's enriched `pack.toml`). |
| [`cursor-full-parity/`](cursor-full-parity/spec.md) | Shipped | RFC-0026, ADR-0015 | **RFC-0026 implementation.** Adds a native `cursor` distribution adapter projecting all five catalogue primitives to Cursor's `.cursor/*` paths at repo + user scope — `skill`→`.cursor/skills/<n>/` (direct-directory), `agent`→`.cursor/agents/<n>.md` (direct-file + `cursor-agent-frontmatter-v0.11`, `tools` dropped + `readonly` derived for non-mutating agents), `hook-body`→`.cursor/hooks/<n>.{sh,py}`, `hook-wiring`→`.cursor/hooks.json` (merge-json, event-remapped, `version:1`), `command`→`.cursor/commands/<n>.md` (first-class, second adapter after Claude Code). **No new projection mode, no `adapter.schema.json` change** — contract v0.10→v0.11 (`[adapter.cursor]` block + version bump only, dual-copy). Same `.cursor/` prefix at both scopes (claude-code/codex pattern, **no** rewrite fn — Open Q1). Readonly predicate = declared tools with none of `{Edit,Write,MultiEdit,NotebookEdit}` (Open Q2). Hook-event map keyed on Claude-PascalCase source names (erratum vs RFC's `agentSpawn` spelling). **Distribution-only** (not in `SELF_HOST_ADAPTERS`). 24 ACs / 7 tasks; one PR. |
| [`adapter-support-accuracy/`](adapter-support-accuracy/spec.md) | Shipped | RFC-0022, RFC-0024 | Corrects six documentation-accuracy defects in the adapter support matrix (`docs/guides/_shared/reference/adapter-support.md`): the Kiro IDE hook three-way split (bodies + `.kiro.hook` events project, only agent-embedded wiring drops), the Kiro/Codex/Copilot slash-command framing (the dropped surface is slash-invocation, not reusable prompt content — that lives on as skills), and the Copilot repo-scope hook caveat (reframed "regressed" → trust/prompt-mode-gated, re-verified against copilot-cli 1.0.61). Verified against `docs/contracts/adapter.toml`; repo-owned docs only, no contract change. |
| [`projection-dry-run/`](projection-dry-run/spec.md) | Shipped | none | Adds a read-only `--dry-run` to `install` + `upgrade`: previews the per-file projection plan (action + tier + target path, with Tier-2 `.upstream.<ext>` companion lines) to stdout and writes nothing — reusing the existing tier classifiers (`_classify_for_install` / `safety.classify`), not forking them. Exits 0 even with Tier-2 collisions (informational; `diff` stays the drift gate), non-zero only if the read-only pre-flight would itself fail. Refuses `--dry-run --force` (install Step 3c cleanup is destructive). Ships a Diátaxis how-to + CLI reference update. v1 = `install`/`upgrade` only; `--json`, dual-scope-via-force, and other write verbs deferred. || [`codex-agent-config-projection/`](codex-agent-config-projection/spec.md) | Shipped | dropped-primitives-coverage, distribution-adapters | Corrects Codex agent TOML projection so source `model:` aliases become OpenAI model IDs and source `tools:` declarations reduce to documented Codex config keys instead of silently dropping or inventing a generic `tools` array. |
| [`product-brief-intake/`](product-brief-intake/spec.md) | Shipped | RFC-0019, ADR-0009 | **RFC-0019 spec 1 of 2.** Adds a `brief` artifact at `docs/product/briefs/<slug>.md` (core seed) + a `receive-brief` skill that elicits load-bearing fields (never mandates a schema), decomposes a multi-feature brief into feature-sized slices by the shippability test, and executes each through `new-spec` → `work-loop`, stamping a `Brief:` back-link. Two shapes (no-stories / story-list with `Satisfies: US-n`); optional `Epic:` pointer for own-the-slice cross-repo coordination. Coverage is **auto-rolled-up** by a new lint (reads `Status:` via `Brief:` back-links; no-op when no brief; untracked-not-error on missing back-link) wired into `make build-check` (Open Q1 resolved → build-check, not advisory). `examples/` ships both shapes, labelled as examples (Q3). CONVENTIONS seed amendment (briefs altitude + `Brief:` field) lands in the implementing PR. 18 ACs / 10 tasks; one PR. No new top-level dir; no cross-pack import. Does **not** depend on `lld-aware-spec-plan` (chains `new-spec` only). |
| [`lld-aware-spec-plan/`](lld-aware-spec-plan/spec.md) | Shipped | RFC-0019, ADR-0009 | **RFC-0019 spec 2 of 2.** Enriches the spec/plan templates to carry a low-level design without baking a stack. `spec.md` gains a light stack-neutral `Shape:` field (`ui\|service\|data\|integration\|mixed`) + AC guidance (UI state/trigger/outcome → AC; NFR-with-a-bar → AC); **no LLD body in the spec**. `plan.md` gains a `## Design (LLD)` section before `## Tasks` built from **ten stack-neutral categories** (optional, shape-pruned) + an **expanded `## Rollout`** covering infra / external integration / deployment sequencing. `new-spec` + `receive-brief` gain a shape/stack-**derivation** step: read `docs/architecture/reference.md` (RFC-0020) and conform when present, else degrade to repo detection + elicitation — the seam that lets RFC-0019/0020 land in either order. Template ships category *names* only (grep-gated, Principle 1). All additive (37 prior specs stay valid). 16 ACs / 8 tasks; one PR. T4 (derivation inside `receive-brief`) carries a cross-spec dep on `product-brief-intake/T4`. |
| [`reference-architecture/`](reference-architecture/spec.md) | Shipped | RFC-0020, ADR-0010, ADR-0009 | **RFC-0020 implementation.** Ships the **producer** side of the normative `docs/architecture/reference.md` golden path (the consumer — `new-spec`/`plan.md` Shape/Stack derivation — already shipped via RFC-0019). Three build deliverables: (1) an **arc42 template asset** (4 sections — Constraints §2 / Solution strategy §4 / Building-block view §5 / Crosscutting concepts §8) at `packs/core/.apm/skills/adapt-to-project/assets/reference.md`, instantiated on demand like `spec.md`/`plan.md`, **never a core seed** (D3 — pre-seeding would *guarantee* a stack-pack collision); (2) an **`adapt-to-project` Class-3 harvest** path that detects the stack/components/standards and *proposes a draft* (repo-scope path-jail, per-finding accept/edit/decline, never authoritative pre-confirmation — D5, no new skill); (3) the **documented stack-pack delivery contract** (a pack ships a filled `reference.md` as an ordinary seed; sole-producer ⇒ no collision; two-producer/atop-adopter ⇒ existing `.upstream` + merge; never ships `overview.md`; **no bundler override field** — D6 §Delivery), pinned by a characterization test of `_project_seeds`' collision `ValueError`. Plus the **CONVENTIONS document-hierarchy** diagram edit (seed → projection) seating `reference.md` beside `overview.md`, and four repo-owned `docs/guides/` quadrant guides. Core stays stack-neutral (Principle 1). **Greenfield authoring (`init-project`) is RFC-0021, out of scope.** 9 ACs / 5 tasks (T1, T2, T4 disjoint → one wave; T3 after T1; T5 last). Spec-mode adversarial review converged Clean in 3 passes. **ADR-0010** (Accepted) records RFC-0020's decisions. Depends on RFC-0020, ADR-0010, ADR-0009; **does not** depend on `lld-aware-spec-plan` (the consumer seam already lands either order). |
| [`spec-contract-seam/`](spec-contract-seam/spec.md) | Shipped | RFC-0017 | **Stage 2** of RFC-0017 (depends on shipped Stage 1): wires contract authoring into the spec loop and gives contracts a durable home. `new-spec` gains a **conditional, contract-type-agnostic `4b` seam** — detect an interface surface *and its type* (REST/OpenAPI, events/AsyncAPI, proto, GraphQL, schemas), locate/create the contract at `contracts/<type>/`, delegate to the type's authoring skill if in the roster else direct-edit + note, link via a `- **Contract:**` header, point the plan's tests at it; non-API features run untouched. **Convention-first** (D7): the `contracts/<type>/` location is the load-bearing anchor; the type→location→skill registry in `new-spec/references/contract-types.md` routes *any* type (events included) and only the skill lookup degrades. Bidirectional traceability (forward `Contract:` header; backward `x-spec` / `REGISTRY.md`) kept honest by a **warn-only traceability invariant added to `lint-spec-status.py`** (extends, not a new script; no-ops where no `contracts/` tree). `adapt-to-project` Class 3 gains contract-relocation + a **narrow carve-out** to its "never add a top-level directory" anti-pattern (package clause byte-identical). CONVENTIONS seed + ADR-0008 record the decisions. v1 bundles only the OpenAPI authoring skill; adopter-facing skill content carries no catalogue RFC numbers. **This repo ships the mechanism only — no `contracts/` tree created here.** 16 ACs / 5 tasks. Spec-mode + impl adversarial review converged Clean. Depends on RFC-0017, `pluggable-api-standards` (Stage 1). |
| [`greenfield-inception/`](greenfield-inception/spec.md) | Shipped | RFC-0021, ADR-0011 | **RFC-0021 implementation.** Ships the **greenfield front-door** — `init-project`, a new `core` skill that is the twin of `adapt-to-project` (brownfield). It turns an idea into a structured repo by **composing** existing skills, never reinventing: a **trigger gate** (throwaways/scripts skip it → scaffold directly; fires only for real stack/structure/tooling decisions — D3); a **value gate over fed-in discovery** (`research`-pack output / a provided PRD / a `receive-brief` brief — `init-project` *consumes* discovery, never performs it) that emits the first **brief** (RFC-0019); a **foundation step** recording the stack as an **ADR + `reference.md`** authored from the arc42 template asset RFC-0020 already shipped (the greenfield population path ADR-0010 D4 names); a **walking skeleton** authored via `new-spec` and *built by* `work-loop` (Open Q1 resolved → orchestrate, don't execute — ADR-0011 D6), kept-and-minimal not a throwaway; then **handoff** to the normal `brief → spec → LLD → work-loop` loop. Fluid-not-waterfall + scoped handoffs (BMAD-borrowed; autonomous multi-agent *engine* declined — D4 / AP2). **No new code, no TDD surface** — goal-based lint/file/projection + manual-QA walk. CONVENTIONS seed amendment (the two front-doors) + three repo-owned `docs/guides/` quadrant guides ride along. 14 ACs / 4 tasks (T1, T2 disjoint → one wave; T4 after T1; T3 last); one PR. No new top-level dir; no cross-pack import. **ADR-0011** (Accepted) records RFC-0021's Decisions 1–4 + Open Q1. Depends on RFC-0021, ADR-0011; soft ordering on RFC-0019/0020 (all Shipped). || [`event-contract-engine/`](event-contract-engine/spec.md) | Shipped | RFC-0018, ADR-0008 | RFC-0018's named follow-on (closes RFC-0017 D4): ships `event-contract`, a **second skill in the `contracts` pack**, as a standard-driven engine in `api-contract`'s method+data shape. Emits **AsyncAPI 3.1.0** into `contracts/asyncapi/` under a swappable event-design standard across **two axes** — (A) the event-design ruleset (**Zalando** ch. 19–21 default, anchored on shared `[#NNN]` numbers) and (B) the message envelope as the reserved `components.envelope` manifest key (**CloudEvents 1.0.2** default, swappable for AWS-native / Avro / bare JSON Schema). Wires into the spec loop by filling the one stub `asyncapi` row in `core`'s seam **plus** a D8 producer-vs-consumer detection refinement (produce/own → author; consume in-repo → reference; consume external → no contract). Open Q1 (drift vs. `api-contract`'s `events.md`) resolved by `[#NNN]` anchoring + a drift-by-number quality gate — `events.md` left byte-identical (D6). The single executable gate is the golden example validating under `npx @asyncapi/cli` (output captured in the PR; absent toolchain blocks merge). No new ADR; no CONVENTIONS change; **mechanism only — no `contracts/` tree created here.** `contracts` is user-scope, so the skill is **not** self-host-projected (only the `core` edit projects) — an RFC-0018 § Errata records that + the D7 reconciliation. 19 ACs / 9 tasks; one PR. Spec-mode adversarial review converged Clean (7 → 1 → 0 findings). Depends on RFC-0018, ADR-0008. |
| [`copilot-full-parity/`](copilot-full-parity/spec.md) | Shipped | RFC-0024, ADR-0013 | **RFC-0024 implementation.** Makes `copilot` a full-parity, **user-scope-capable** adapter — the codex `dropped`→first-class move (`dropped-primitives-coverage`) re-run for Copilot's native `.github/{agents,hooks}` + `~/.copilot/` surfaces. Contract **v0.9 → v0.10** (dual-copy `_data/` + `docs/contracts/`): copilot `agent` flips `dropped`→new **`copilot-agent-md`** mode (markdown → `.agent.md`; `model` dropped, `target` omitted, `tools` pass-through gated by a **fail-closed allow-list**), `hook-wiring` flips `dropped`→new **`copilot-hooks-json`** mode (one self-contained `<name>.json` per wiring — deliberately **not** `merge-json`, since Copilot reads every `*.json`; frozen six-event name map, fail-closed on unmapped), `hook-body` moves `tools/hooks/`→`.github/hooks/`, `skill` gains a `~/.copilot/instructions/` user target, and `command` **stays dropped** (copilot-cli#618/#1113 — warning rail keeps it visible). Copilot gains `[adapter.copilot.scope].user` (+ prefixes) so `user_scope_capable_adapters_from_contract()` includes it with **no `scope.py` edit**; but `install.py` needs a `_render_for_user_scope` copilot branch **and** a post-render `.github/`→`.copilot/` prefix rewrite (the divergent-prefix seam — surfaced by adversarial review; claude-code rewrites only hooks, copilot's whole prefix diverges). Validation surface = the two subagent-bearing packs: `research` (+`copilot` in `allowed-adapters`, v0.8→v0.10) + `core` (v0.8→v0.10) — **not** an all-pack bump (RFC-0022 precedent). **Open Q4 settled** (two live CLI 1.0.59 runs): Copilot exposes no web tool to custom agents, so `WebFetch`/`WebSearch` are documented-degradation for `research` — **later corrected by [`copilot-skills-and-web`](copilot-skills-and-web/spec.md) (RFC-0024 § Errata E1): web *is* supported on the Copilot CLI + app** (the 1.0.59 probe was confounded); only the cloud agent lacks it. Synthetic user-scope hook pack covers the validation gap no shipped pack exercises; live-CLI T1–T8 smoke gates merge (recorded in the spec changelog). 30 ACs / 11 tasks; one PR per RFC-0004 atomicity. Spec-mode adversarial review converged Clean (4 blockers → 0 → 3 accuracy fixes). Amends `distribution-adapters/spec.md` (v0.9→v0.10 Changelog). **`skill` surface superseded** by `copilot-skills-and-web` (instruction-file → first-class SKILL.md). Depends on RFC-0024, ADR-0013. |
| [`credentialed-cli-exit-code-contract/`](credentialed-cli-exit-code-contract/spec.md) | Shipped | RFC-0006, RFC-0013, ADR-0003 | **Step 1 of the credential-handling hardening** (the A+B pip-installed-manager pivot is a separate later RFC). Harmonizes the exit-code taxonomy across the 5 credentialed CLIs (jira, jira-align, confluence-publisher, confluence-crawler, figma) to a **banded** `0 OK · 1 ERROR (functional) · 2 USER_ACTION`, with `3–9` reserved for the credential/auth band (room for new auth codes). Adds a top-level `except Exception` catch-all so `Tier2HardFailError` (keychain hard-fail) and an unprojected shim (`ModuleNotFoundError`) stop escaping as uncaught tracebacks. Adopts authsome's *philosophy* (single mapper, map-every-exception-at-top, coarse code + message — `utils.py:197`) but defers its *numbers* to the A+B pip-manager (forcing them now would break `author-a-skill.md`'s `check → 2` contract). Functional failures fold into `1`; credential/auth gets the band. Adds the message-first "When a request fails" body to all 5 SKILL.md + corrects the guide's `Tier2HardFailError → 3` drift to `→ 1`. Accepted observable breaks (0.1.0 packs): `jira`-family `SERVER 3→1`, confluence-crawler usage `2→1` (confluence-publisher already `0/1/2`). Shim untouched (byte-equivalence gate). 7 ACs / 6 tasks (T2–T6 depend on T1). |

| [`credbroker-user-scope/`](credbroker-user-scope/spec.md) | Shipped | RFC-0023 (amends), RFC-0013 | **Closes the user-scope gap the credbroker migration opened.** A no-repo `--scope user` install resolves credentials at **env Tier-1 only** today (consumers `import credbroker`, but user-scope install delivers no credbroker and runs no pip; OS-keyring/dotfile/vault silently fall away) — the same unbuilt rail that leaves `sso-broker.py` undelivered to `~/.agentbundle/bin/`. Fix = **one consumer contract (`import credbroker`) + a `sys.path` precedence stack**, fed by three delivery layers in cost/value order: (1) **foundation** — consumer bootstrap appends `~/.agentbundle/lib` at *lowest* precedence + a `release-credbroker.yml` builds/validates the wheel (publish gated); (2) **vendored floor** — a drift-gated `.apm/user-libs/` primitive vendors `packages/credbroker/credbroker/`, and a **new install-time user-scope delivery step** writes it → `~/.agentbundle/lib/credbroker/` **and** `adapter-root-bins/*.py` → `~/.agentbundle/bin/` (closing the latent `sso-broker` gap on the same rail); (3) **PyPI** — wired, manual maintainer publish. A pip-installed `credbroker[crypto]` (PyPI / internal mirror / local wheel — **no-PyPI corporate path**) always wins over the floor (vault enabled); the floor (stdlib base, plaintext Tier-3) is the zero-pip guarantee. Amends RFC-0023 (layered model resolves its deferred no-repo problem). 8 ACs / 5 tasks. Stacked on `credbroker` (Shipped). |
| [`work-loop-light-mode/`](work-loop-light-mode/spec.md) | Shipped | RFC-0025, ADR-0014 | **ADR-0014/RFC-0025 implementation.** Adds a risk-gated `work-loop` **light mode** (lean inline persisted spec + single bounded adversarial pass + no `loop-cohort` state machine; single logical task) as the **adopter-wide default**, replacing the `>1 file` escalation rule with ADR-0014's seven-trigger set; full mode unchanged. Vehicle = a `work-loop` SKILL.md mode branch + optional `new-spec` template sections — **no new executable code, skill, or artifact type**. Self-hosting nuance: the work-loop skill + new-spec templates + `docs/CONVENTIONS.md` (override-rescued) project via `make build-self`; root `AGENTS.md` is Manual (`EXCLUDED_PATTERNS`) — edited directly with its seed synced. Light mode keeps `lint-spec-status.py`, drops only the state machine. 9 ACs / 5 tasks; spec-mode adversarial review converged Clean (2 blockers → 1 blocker/4 concerns → clean). **Implementation is a separate follow-up PR.** Depends on RFC-0025, ADR-0014. || [`work-loop-context-hygiene/`](work-loop-context-hygiene/spec.md) | Shipped | none | **Subtraction-shaped context-hygiene pass on `work-loop`.** Cuts resident-context noise without weakening any verification surface (gates, the adversarial iterate-to-Clean loop, fingerprint stasis, the quality-engineer floor, the iteration cap). Three coordinated edits under `packs/core/.apm/`: (1) **orchestrator-side distillation** — after `loop-cohort review record` persists fingerprints, the orchestrator drops the full reviewer report from resident context and re-reads a finding from the on-disk report only when a FIX needs it (the on-disk report + `state.json` fingerprints are the durable record; the next REVIEW pass regenerates findings); (2) **reviewer-contract tightening** — the three reviewer agents return *only* the findings block (or `Clean — ready to commit.`), no pre-findings methodology recap/narration, finding format untouched (parser-safe); (3) a lean **`## Context hygiene`** SKILL.md section codifying three evidence-ranked "do this, because" levers, each with a portable no-subagent floor (reference-read reduction first — biggest window draw, delegate where the agent supports it; compact at task boundaries with a preserve hint — invariant-anchored to externalized memory + cross-referencing Unattended loops, not a bare hedge; narrowest gate during FIX, full suite before finish), plus an explicit "reduce, never lossily transform" guardrail (`Edit` needs exact bytes; skeleton maps for orientation only). Agent-agnostic — `/compact` is the sole named instance. No token-count AC by design. 10 ACs / 4 tasks; spec-mode adversarial review converged Clean (2 blockers/4 concerns/2 nits → 1 concern/1 nit → clean). Skill-behavior change ships via spec+PR (not RFC-gated). || [`universal-quality-floor/`](universal-quality-floor/spec.md) | Shipped | none | **Raises the bundle's default quality floor by doctrine so agent output clears strict external static-analysis gates (e.g. SonarQube "platinum" + CI-only coverage) without per-repo detection or bundled tooling.** Three coordinated, stack-agnostic, doctrine-only changes in `packs/core`: (1) **`quality-engineer` sharpening** — four universal-smell findings added to Maintainability (bounded *reducible* complexity → split, distinguished from #23's *comment-the-irreducible*; nesting depth → idiom-appropriate guard clauses/pattern-matching, not mandated early-`return`; duplicated production blocks past the rule-of-three, tests exempt/DAMP; magic-literals-of-non-obvious-origin + fn/param bloat, scoped to *align* with `adversarial-reviewer`'s threshold-suppression carve-out, threshold-free), reframed as "approximates a strict static-analysis gate whether or not one is wired"; plus a Test-Design preface promoting the **mutation-testing mindset** ("a test must be able to fail") as the Goodhart-safe stand-in for a coverage number (existing "do not demand 100% coverage" retained). Pressure-tested across 9 stacks (detekt + SonarJS both use SonarSource's cognitive-complexity metric). (2) **Simplify pass** in `work-loop` EXECUTE/REVIEW — a deliberate reduce-the-diff step (new code only, tests DAMP, no adjacent cleanup) so there's less code to smell/review; **harness-agnostic doctrine**, Claude Code's native `/simplify` an optional accelerant, never a dependency. (3) **Light-mode carve-out** — retain the `quality-engineer` pass when the **adopter declares** (in their AGENTS.md) a strict external gate the local loop can't run; **policy, not detection**. Begins migrating mode *mechanics* out of the projected `CONVENTIONS.md` (edit the **seed** `packs/core/seeds/docs/CONVENTIONS.md`; risk-trigger block left byte-identical across all 4 copies — verified) into `work-loop` as the opinionated owner, **owner-approved in lieu of an `update-conventions` RFC** (full CONVENTIONS retirement deferred to its own RFC). Bumps core pack `0.2.0`→`0.3.0` + codifies a new `AGENTS.local.md` rule (non-cosmetic pack updates must bump the pack version). 15 ACs / 6 tasks; one PR. Spec-mode adversarial review: 9 findings (0 blockers) → all resolved. || [`gemini-full-parity/`](gemini-full-parity/spec.md) | Shipped | RFC-0027, ADR-0016 | **RFC-0027 implementation.** Adds the native `gemini` distribution adapter — projects all five primitives to `.gemini/*` (+ `~/.gemini/*` via the install-time prefix rewrite), the `copilot`/`cursor` full-parity pattern. Higher fidelity than Cursor: agent `tools:` allowlist **kept and name-mapped** (`Read→read_file`, `Edit→replace`, `Bash→run_shell_command`, …; `MultiEdit→replace` for parity) and a **tier-preserving** model map (`opus→gemini-2.5-pro`/`sonnet→gemini-2.5-flash`/`haiku→gemini-2.5-flash-lite`). One new mode **`gemini-command-toml`** (Markdown→TOML `prompt`, `$ARGUMENTS`→`{{args}}`, **fail-closed** on non-single-injection). Hook-wiring **and** the `AGENTS.md` `context.fileName` bridge land in a **single** managed-merge into `.gemini/settings.json`; hook-event map keyed on PascalCase source events (mirrors `copilot-hooks-json`'s `_EVENT_MAP`, **not** the Kiro `agent-event-vocabulary` — the same erratum Cursor's RFC needed), zero drops, fail-closed. `gemini` opted into the 7 explicit-`allowed-adapters` packs (the 4 line-less packs already admit any shipped adapter at repo scope — left untouched to avoid narrowing). Distribution-only (**not** in `SELF_HOST_ADAPTERS`). Support-matrix gains a dedicated Gemini row (was stale "Universal layer"); root `AGENTS.md` reader line refined directly (`EXCLUDED_PATTERNS`) + seed synced. **Coordination:** stacks on the **merged** Cursor adapter (RFC-0026 / ADR-0015, #273) — contract **v0.11 → v0.12**, **ADR-0016**; rebased + reconciled to Cursor's hand-maintained-site touch-list. 15 ACs / 7 tasks; one PR. Spec-mode adversarial review converged Clean. Depends on RFC-0027, ADR-0016. || [`copilot-skills-and-web/`](copilot-skills-and-web/spec.md) | Shipped | RFC-0024 (§ Errata), ADR-0013 (§ Errata), RFC-0009 | **Expand-support fix: the Copilot adapter caught up to the shipped CLI + app.** Two coupled changes, one PR, contract **v0.11 → v0.12** (atop RFC-0026 cursor's v0.11; dual-copy `_data/` + `docs/contracts/`). **(1) Skill flip:** Copilot shipped first-class Agent Skills (distinct from custom instructions), so `skill` flips `instruction-file` → `direct-directory` — a passthrough of our canonical `.apm/skills/<name>/SKILL.md` to `.github/skills/<name>/SKILL.md` (repo) / `~/.copilot/skills/<name>/SKILL.md` (user, via the `install.py` prefix-rewrite map). Reuses the existing `direct-directory` projector (copytree + symlink-defence + skill-bounded orphan-sweep, mirroring codex); the orphaned `copilot-instruction` frontmatter-default and the `.github/instructions/` / `.copilot/instructions/` scope prefixes are retired. No adapter uses `instruction-file` after this. The copilot orphan-scan asymmetry is gone (skills are now a scannable directory tree), so `_ADAPTERS_WHERE_GOV_ORPHAN_SCAN_FIRES` gains `copilot`. **(2) Web correction (docs/comments-only, zero behaviour change):** `WebFetch`/`WebSearch` already pass through and Copilot resolves them to its `web` tool on the CLI + app (only the cloud agent lacks it) — the false "no web tool / research degradation" wording is corrected across code comments + ~6 docs; no tool-name mapping added. **Governance:** both reversals are recorded as Approver-signed `§ Errata` on RFC-0024 + ADR-0013 (append-only; the spec-mode reviewer judged the erratum the right vehicle over a follow-on RFC — flip-on-upstream-support with RFC-0009 precedent). `research` + `core` bump pack contract v0.10 → v0.12 (not all-pack). 11 ACs / 7 tasks; one PR. Spec-mode adversarial review converged Clean (3 passes: 4 blockers → +4 blockers → 0). Amends `distribution-adapters/spec.md` (v0.11→v0.12 Changelog); supersedes-in-part `copilot-full-parity/`. Depends on RFC-0024, ADR-0013. |
| [`security-reviewer-shift-left/`](security-reviewer-shift-left/spec.md) | Shipped | RFC-0029, ADR-0018, ADR-0017, ADR-0014/RFC-0025 | **RFC-0029 implementation.** Strengthens the `security-reviewer` along three axes with **no adapter-contract change**: (1) a new core-pack **`security-checklists` skill** of ten boundary-keyed `references/<module>.md` modules (OWASP 2025 axis + cross-cutting), each naming its standard+version and tagging checks `tool`/`hybrid`/`reason`; (2) a **spec-stage secure-design mode** added to `security-reviewer.md` (mirrors `adversarial-reviewer`'s spec-stage structure) + a **trim** of the deep per-domain checklists to the universal method, with the awareness anchor bumped **2021 → 2025**; (3) **work-loop wiring** — net-new pre-EXECUTE dispatch of the spec-stage reviewer on the security-boundary trigger, plus orchestrator-side **module-inlining** (boundary→module routing table; depth never bloats the agent). Plus language-agnostic three-bucket tool delegation (Tier-1 declare/detect/fail-clean on scanner absence), `AGENTS.md` blessed-helpers customization point (**no new `security.md` file** — D5), adopter-clean shipped skill (mechanism only), and the RFC's CONVENTIONS touch landing **atomically with the wiring** (no forward-claim). 13 ACs / 6 tasks; one PR; core pack version bump. **ADR-0018** (Accepted) records the decision. Depends on RFC-0029, ADR-0018. || [`llm-agent-agentic-boundary-extension/`](llm-agent-agentic-boundary-extension/spec.md) | Shipped | RFC-0029, ADR-0032, agentic-well-architected-overlay | **Closes the deferred `llm-agent-module-agentic-boundary-extension` backlog item (ADR-0032).** Extends the `security-checklists` `llm-agent` control module (the orchestrator-inlined depth the `security-reviewer` reasons from) with three control-altitude checks for agentic security boundaries the agentic overlay routes out today — **execution isolation & blast radius**, **inter-agent identity/privilege propagation**, and **memory poisoning** — anchored on the **OWASP Agentic Top 10** (tool misuse / identity-privilege abuse / memory poisoning) plus **LLM04** (added to the module's Standards line, previously omitted), alongside the existing LLM01/02/03/05/06/10. Keeps the module's shape intact: the `tool`/`hybrid`/`reason` legend, the spec-stage proactive-control section, the established-helper-bypass section, and the `work-loop` boundary→module routing-table `llm-agent` row. **Cross-pack reciprocal updates:** softens the now-stale "reach beyond the module's current checks" prose in both byte-identical `architect` `lens-genai-agentic.md` copies (architect 0.8.0→0.8.1), rewrites the overlay's `coverage-parity.md` to map the three to named module checks, and closes the overlay's deferred AC + removes the backlog entry in one commit. core 0.4.13→0.4.14 (projected). Goal-based + manual-QA (the `security-reviewer` itself is the consumer). 15 ACs / 5 tasks; one PR. |
| [`well-architected-cloud/`](well-architected-cloud/spec.md) | Shipped | none | **Cloud well-architected, two-sided, in the `architect` pack — Track 1 only (Track 2 enterprise *process* excluded; only its quality *techniques* fold in).** `architect-design` gains a light **concept-first stage** (one-page `assets/concept.md`, arc42-canvas spirit — *not* the heavy design doc), a **well-architected-by-construction** pass (universal-5 + Sustainability pillars achieved per provider **AWS/Azure/GCP/generic-primitives(Hetzner)/local-first** — primitives carry a "capability-gaps-you-must-fill" checklist, local-first carries the "local→production delta" + graduation path, no toolchain prescription), an **agent-free convergence loop** (review → auto-resolve *mechanical* findings → re-review → surface only *judgment* findings; bounded + stasis escape; reviewer-independence via the harness's fresh-context facility w/ a cold-read floor; degrades to embedded rubric when `architect-review` absent), and a **leading-edge / novel-domain path** (`leading-edge-domains.md` — detect novelty → optionally compose with the user-scope `research` skill `applied`/`deep` for grey-lit + GRADE confidence → ad-hoc lens → carry source+confidence → degrade when absent; ships **method, not domain content**). `architect-review` gains a **WA/lens mode** (concern + workload-class lenses incl. GenAI/agentic, orthogonal to artifact-type routing) emitting a risk register + improvement plan + risk-acceptance + non-risks, every finding **mechanical-vs-judgment tagged** (the loop's signal, with an operationally-decidable test); standalone **review-only mode** preserved. Quality substance mined from **ATAM** (quality-attribute scenarios, tradeoff/sensitivity points, utility-tree-lite) + ARB question banks; **CASA/ASVS** named as a downstream assessability gate (referenced, *not* reproduced — control verification routed to `security-checklists`/`security-reviewer`). Pure-markdown, user-scope, **no subagents / no `loop-cohort` / no script / no new pack** (distribution model projects path-not-content, so the loop lives inline). Refs duplicated per-skill (no shared dir); every SKILL.md kept <100 lines. Pack 0.1.0→0.2.0 + `.claude-plugin/marketplace.json` regen via `make build-self`. In-charter (README defers EA-platform, not cloud WA) → spec, not RFC. Background in `.context/architect-pack-enterprise-modes-research.md`. 39 ACs / 7 tasks; one PR. Spec-mode adversarial review converged over 3 passes (6 → 4 → 2 findings, no blockers — all folded in). || [`atlassian-sso-cookie/`](atlassian-sso-cookie/spec.md) | Shipped | RFC-0035, RFC-0013, RFC-0023, ADR-0026 | **RFC-0035 implementation.** Wires the atlassian `jira` (read paths) + `confluence-crawler` skills to SSO-cookie auth against **Atlassian Data Center** behind corporate SSO (token-blocked). The missing piece is a **consumer-side SSO resolver**: `credbroker` gains a platform-agnostic SSO family — `load_sso_cookies(profile)` (subprocess-invokes the **unchanged** `sso-broker.py` engine, fail-closed on any non-exit-0) + reusable validation primitives (https-only, root-relative endpoint, cookie-domain capture/send confinement) — bumping `credbroker` 0.1.1→0.2.0, **no new user-lib** (RFC-0023 retired in-pack shared-module projection; `credbroker` is the single consumer-resolution home). Each skill ships a placeholder-shaped `references/sso-config.toml` (one `auth_default` switch; upstream `creds`, enterprise pre-bakes `sso-cookie` + flavor + base_url into a pack customization), a config loader/selector, and a setup helper that seeds the runtime profile by driving `sso-broker register` from the file. Clients gain a cookie-auth path: jar attached, **no `Authorization` header**, proxy/trust-store wired against actual httpx 0.28.1 behavior (`trust_env=True`; the RFC's "httpx ignores proxy/CA env" premise corrected), reads enforced by a **GET/HEAD allowlist at the `_request` chokepoint** (covers the `raw()` escape hatch; writes refused before the wire pending XSRF). Frontmatter flips to `auth: sso-cookie`; `lint_credentialed_skills.py` amended so a dual-auth (`sso-cookie`+`creds`) skill carries **both** phrase sets and a `credbroker` SSO import satisfies broker-invocation (mirrors RFC-0023's `creds` move); a new structural `sso-config.toml` lint pins the upstream placeholder shape (TOML-key, not substring). Token users with no SSO config are byte-identical. Cloud, writes, `confluence-publisher`, `jira-align` out of scope. Ships **Experimental** until a live-DC read transcript lands (one AC deferred). Governance: **ADR-0026** + a second RFC-0013 § Errata (consumer-surface addition), authored once design is adversarial-clean. 19 ACs / 9 tasks. || [`product-engineering-pack/`](product-engineering-pack/spec.md) | Shipped | RFC-0030, ADR-0019, ADR-0008 | **RFC-0030 v1 (app/solo + single-component; BU cross-component value-stream layer is phase 2).** A new opt-in, user-scope `product-engineering` pack — three pure-markdown skills `frame-intent` / `de-risk-intent` / `decompose-intent` (<100-line SKILL.md + `references/`), the recursive level-tagged `intent` seed + a worked example, and Diátaxis guides. One global **Scale** axis (app↔BU, resolved at intake infer→confirm→ask) + per-intent **maturity** / **reversibility** / choosable **prototype-approach** (`prototype-led`↔`validate-first`) modes baked into the skills. **v1 touches `core` not at all** — a feature-level intent at app scale *is* an ordinary `core` brief (`receive-brief` is level-agnostic for its own repo; the brief carries no `level:`; `parent-intent:` provenance deferred to phase 2, refining RFC-0030 §5 per ADR-0019 corollary 2). Contract maturity behavioral@intent→interaction@brief→wire@spec **reuses the existing `Contract:` seam** (ADR-0008), no new contract machinery. Habits-not-infra: **no hook/engine/validator/subagent/new top-level dir** (3-reviewer ceiling). Goal-based + manual-QA verification (no TDD — no logic). 11 ACs / 7 tasks (T3/T4/T5 parallel after T1,T2; T6 after; T7 last); built via work-loop. **ADR-0019** (Accepted) records the decisions. Spec-mode adversarial review converged Clean. || [`markdown-to-office-publishing/`](markdown-to-office-publishing/spec.md) | Shipped | RFC-0036, RFC-0007 | **RFC-0036 implementation.** Adds three skills to the existing user-scope `converters` pack — `markdown-to-docx` / `markdown-to-pptx` / `markdown-to-xlsx` — that **fill a user-provided branded Office template** (Word/PowerPoint/Excel) from a Markdown artifact, completing the pack's Office round-trip (today only Office→Markdown). **Template-fill, not convert** (only it preserves a user-owned brand); **three independent script sets** (RFC Open Q1, no shared engine); each **Tier-1** (declare/detect/fail-clean import-probe `--check` on `docxtpl`/`python-pptx`/`openpyxl`, no auto-install) mirroring `file-to-markdown`. **Inspect-then-map**: enumerate the template's fill-points (docx `get_undeclared_template_variables()` / pptx layout placeholders by `idx` / xlsx named ranges + tables) → map Markdown (front-matter→scalars, H1/H2→sections/slides/sheets, lists→loops/rows, tables→row-loops/table-placeholder/data-region); guide-the-user when docx/xlsx have no fill-points (pptx exempt). Template flow detect→confirm/elicit→explicit no-template opt-out → **library-default** unbranded output (RFC Open Q3; no bundled brand). Security additions from the spec-stage pass: **output-path confinement** (realpath + path-component containment, not string-prefix), docx **`autoescape=True`** at `render()` + escaping test, explicit **trusted-author** template trust posture (SSTI/XXE/zip-bomb scoped accepted out-of-scope, consistent with the pack's local-files-trusted stance), canonical-package + version-floor + outside-SCA dependency note. Per-skill **TDD** units (manifest enumeration / Markdown→model mapping / `--check` / path confinement) + one **end-to-end integration test per format** (re-open the produced file, assert filled values) **CI-wired by explicit per-path lines** (atlassian precedent) + behavioral `evals/evals.json` for activation. Pack **0.1.2→0.2.0** (adapter-contract stays 0.8; RFC Open Q2), README/`description` refreshed, `marketplace.json` regen via `make build`. Docs follow-on (how-to + reference + changelog) land in the implementing PR. 19 ACs / 5 tasks (T1/T2/T3 independent → one wave; T4/T5 join). Spec-stage adversarial + security review converged Clean over 2 passes. **Implementation is a separate follow-up PR.** Depends on RFC-0036. || [`lf-line-endings/`](lf-line-endings/spec.md) | Shipped | none | **CLI emits LF on every platform.** The `agentbundle` CLI's text-mode writers used `Path.write_text(...)` / text-mode `open(...)` / `tempfile.NamedTemporaryFile(mode="w")` with no `newline=`, so on Windows every generated artifact (adapter projections incl. Kiro, the composed `AGENTS.md`, the self-host tree, merged `.claude/settings.json`, hooks, TOML/JSON) drifted to CRLF — diverging from macOS/Linux output and the Windows-clean white-label fork goal. Fix is two complementary layers: a repo-root **`.gitattributes`** (`* text=auto eol=lf` + binary overrides, added to `EXCLUDED_PATTERNS` so self-host treats it as repo-owned) normalizing at the commit boundary, and **`newline="\n"` at all 24 text-mode writers** so output is LF at the source independent of git config. A stdlib **AST guard test** fails CI if any future non-test text writer omits `newline=` (chosen over a substring grep, which both false-positives on multi-line `write_text` and misses multi-line `NamedTemporaryFile`). Inline kwarg, no shared helper (declined — one constant kwarg isn't shared logic). Not RFC-gated: a correctness convergence of Windows output to the POSIX behavior the contract always implied (CONVENTIONS § 3 bugfix carve-out). 6 ACs / 4 tasks; one PR. Spec-mode adversarial review converged Clean over 2 passes (2 blockers — the missed `NamedTemporaryFile` writers + AST-scope gap — / 3 concerns → clean). |

| [`render-proof/`](render-proof/spec.md) | Approved | none | **New `render-proof` skill in the `converters` pack.** Renders a markdown file to a single self-contained offline HTML proof artifact via `renderToStaticMarkup` + `dangerouslySetInnerHTML` (Risk #1 fallback: `A2uiSurface` is SSR-incompatible; `MessageProcessor` called for pipeline integrity). Markdown pipeline: markdown-it + `markdown-it-task-lists` + Shiki syntax highlighting (github-dark) + DOMPurify with a four-step `uponSanitizeAttribute` CSS-sanitization hook (nonce-sentinel + canonical-restore + `none`-placeholder). Path confinement: `fs.realpathSync` on both input and cwd + component containment. CLI: `node scripts/render-proof.js <input.md> [--output <file.html>]`. Pack 0.6.1 → 0.7.0. 13 ACs / 6 tasks. Spec-mode adversarial review converged Clean (17 passes). |
| [`m2-diverge-solutions/`](m2-diverge-solutions/spec.md) | Shipped | RFC-0064 (M2.3), RFC-0064 § Known Unknowns resolved 2026-07-18 | **RFC-0064 M2.3 — `diverge-solutions` PE skill (step 3 of the six-step shaping sequence).** Prompt-only skill that takes an opportunity (step-2 artifact or free-form description) and generates ≥3 structured, comparable solution options that `place-bet` (step 5) can reason against. Emits `solution-options.md` at `<output_dir>/shaping/<slug>/`; each option carries name, approach, key bets (1–3), trade-offs, and status (`recommended` \| `parked` \| `rejected`); the skill sets exactly one to `recommended` and retains all others. Degrades when `identify-opportunities` (step 2) artifact is absent: offers to run it first; if user proceeds, includes a "Step 2 readiness" section. Redirects feature-scope to `explore-options` (altitude mismatch) and freeform-brainstorm requests to `explore-options` (output-contract difference). Ships with how-to guide `generate-solution-options.md`. SKILL.md < 100 lines (Charter Principle 3). `explore-options` vs `diverge-solutions` boundary: coexist, different output contracts — freeform/no-minimum vs formal/≥3-structured (resolved 2026-07-18). 11 ACs / 4 tasks; spec-mode adversarial review converged Clean (2 passes). |
| [`m1-work-queue/`](m1-work-queue/spec.md) | Implementing | RFC-0064 | **RFC-0064 Batch 3 (M1.7) — work queue end-to-end.** Extends `work-loop` with two prose touch points: (1) a step 0 orientation block that reads `workspace.toml` at session start and surfaces the active initiative name, milestone, and `[work].active` spec path before PLAN begins; (2) a ship-step edit instruction that moves the current spec path from `[work].active` to `[work].shipped` in `workspace.toml` and surfaces a `roadmap.md` update reminder. Degrades gracefully when `workspace.toml` is absent — no error, no behavioral change. Vehicle = SKILL.md prose additions only (no new executable code, skill, or artifact type); projected via `make build-self`. 9 ACs / 4 tasks. |

## Shipped specs (archived)

<!-- Once a feature is shipped, move its row here. The spec stays in place
     as documentation of the feature's contract. -->

| Spec | Status | Constrained by | Notes |
| --- | --- | --- | --- |
| [`distribution-adapters/`](distribution-adapters/spec.md) | Shipped | RFC-0001 | F-spec + F-build: adapter contract, build pipeline, four reference adapters, pack.toml/plugin.json schemas, Tier-1/2/3 model. Format source-of-truth for the other two. |
| [`self-hosting/`](self-hosting/spec.md) | Shipped | RFC-0001, RFC-0002 | `make build-self` + `make build-check` gate; this repo eats its own dog food. Phase 1 closed AC1a, AC1b, AC2–AC7, AC9–AC17 — adapter-driven `.apm/` primitives, seed projection, marketplace aggregation, CLAUDE.md symlink, drift source-naming, info-level unclassified, fail-fast on missing discovery, branch protection registered, first real edit landed through pipeline. Phase 2 (follow-up) covers AC8 — AGENTS.md body+footer composition (needs Codex multi-pack aggregation fix in the sibling distribution-adapters spec) — and LF/mode/symlink comparison-rule strengthening. Depends on `distribution-adapters`. |
| [`agent-spec-cli/`](agent-spec-cli/spec.md) | Shipped | RFC-0001, RFC-0003 | `agentbundle` CLI at `packages/agentbundle/`. Library-first; stdlib only; zipapp distribution; eleven subcommands incl. `upgrade` with per-primitive granularity. Depends on `distribution-adapters`. |
| [`adapt-to-project/`](adapt-to-project/spec.md) | Shipped | RFC-0001, RFC-0003 | LLM-driven counterpart to `agentbundle adapt`: walks adopters through the four RFC-0001 classes of change (substitution, `.upstream.<ext>` companion merges, discovery+restructuring, within-layout consolidation). Unifies `.adapt-discovery.toml` schema to `[markers]` + `[[findings.*]]` across CLI and self-host; lights up `[pack.dependencies.required]` enforcement on install; adds install→adapt nudge via `.adapt-install-marker.toml` + session-start hook. AC4b transcripts deferred — see ROADMAP. Depends on `agent-spec-cli`, `distribution-adapters`. |
| [`user-scope-hooks/`](user-scope-hooks/spec.md) | Shipped | RFC-0005 | RFC-0005 implementation contract: `user-merge-json` (Claude Code user scope) + `merge-into-agent-json` (Kiro both scopes — closes RFC-0001 Open Q1); v0.3 state schema with `hook-wiring-owned`; `--force-merge` and `reconcile --scope user`. 13 tasks; T10/T11 amend the sibling specs. Depends on `agent-spec-cli`, `distribution-adapters`. |
| [`apm-install-route-parity/`](apm-install-route-parity/spec.md) | Shipped | RFC-0010 | F-apm-derivation + F-apm-install-marker: per-pack `SessionStart` writer drops `.adapt-install-marker.toml` on first install via APM's HookIntegrator, projected through `dist/apm/<pack>/.apm/hooks/install-marker.{json,py}`. Reuses the canonical writer template from `claude-plugins-install-route` with two additive changes: a `required=True` `--install-route {claude-plugins,apm}` argparse flag (fast-fails on absence so a build-pipeline regression that drops the flag from the projected hook command is caught immediately) and a data-directory portability shim (`${CLAUDE_PLUGIN_DATA}` → `${PLUGIN_ROOT}/.data` → `${CURSOR_PLUGIN_ROOT}/.data` → exit 0). Scope detection under APM is by writer's own projected path (cwd → repo; `$HOME` → user) with `.resolve()` on both sides. Adapter contract v0.4 → v0.5 (`"apm"` appended to `install-routes`); marker schema gains `"apm"` as a permitted `install-route` value. Coverage is four-of-seven APM targets (Claude Code mechanically asserted; Copilot, Cursor, Gemini deferred to AC17 manual-QA matrix per first-firing characterisation); three uncovered targets (Codex, OpenCode, Windsurf) use the documented `agentbundle adapt --scope <project\|user>` manual fallback. T1–T12 implementation landed 2026-05-25; live APM install end-to-end deferred per AC17 manual-QA matrix. 18 ACs / 12 tasks. Amends `claude-plugins-install-route/spec.md` (AC1 + AC9 in-PR edits), `adapt-to-project/spec.md` (1 new AC + proactive cache-scan branch extends to `apm_modules/`), and `distribution-adapters/spec.md` (APM-route conformance cases + v0.4→v0.5 Changelog). Depends on `claude-plugins-install-route`, `distribution-adapters`, `adapt-to-project`. |
| [`repo-scope-per-adapter-projection/`](repo-scope-per-adapter-projection/spec.md) | Shipped | RFC-0012 | Lifts RFC-0011's adapter resolution to **repo scope**: `agentbundle install --scope repo --adapter <X> .` lands the pack at `<repo>/.<ide>/skills/` (per-IDE direct write) instead of the dist-tree shape (`<repo>/claude-plugins/<pack>/`, `<repo>/apm/<pack>/`). Contract v0.6 → v0.7: every adapter declares `allowed-prefixes.repo`; Copilot gets a scope table for the first time. `_resolve_user_scope_target_adapter` renames to `_resolve_target_adapter` with a `scope` kwarg; the lookup gains six steps (0–5) with scope-branching at steps 0, 4, 5; repo scope **does not probe** `<repo>/.<ide>/` (load-bearing asymmetry pin). `DEFAULT_USER_SCOPE_ADAPTER` → `DEFAULT_ADAPTER` rename (single-flip enterprise-rebrand surface); one-release deprecation alias. `--emit-install-routes` opt-in restores the dist-tree producer for catalogue-publishing workflows; handler-level mutex with `--adapter` at repo scope; the user-scope-only `--adapter` binding is **removed**. Eight packs bump v0.6/v0.2 → v0.7 (four user-scope-capable + four repo-only; the latter is load-bearing per RFC-0012 Drawback #7). New `safety.scan_for_pack_artifacts` helper closes the projection-vs-state crash-window with an orphan-projection refusal + `--force` clean-and-retry. Three-trigger in-band detection (shape-mismatch / adapter-disagreement / orphan recovery) carries adopters across the v0.6 → v0.7 transition. 37 ACs / 11 tasks; one PR per RFC-0004 atomicity. Amends `pack-allowed-adapters/spec.md` (scope-conditional widening of AC15), `agent-spec-cli/spec.md` (`--emit-install-routes` + `--adapter` at both scopes), and `distribution-adapters/spec.md` (v0.6 → v0.7 conformance + repo-scope per-IDE cases). Depends on RFC-0012 / RFC-0011 / RFC-0009 / RFC-0008 / RFC-0010 / RFC-0004. |
| [`dropped-primitives-coverage/`](dropped-primitives-coverage/spec.md) | Shipped | RFC-0009, RFC-0005, RFC-0012, ADR-0002 | Closes the silent-degradation footgun (adapters whose contracts mark some primitive types as `mode = "dropped"` produce partial installs without telling the adopter) AND fixes contract drift (codex contract claims `agent` + `hook-wiring` are dropped, but upstream codex has native `.codex/agents/<name>.toml` subagents and `<repo>/.codex/hooks.json` hooks since v0.117.0 April 2026). Contract v0.7 → v0.8 with codex `agent` projection upgraded to a new `codex-agent-toml` mode (markdown → TOML serialisation per a new `codex-agent-frontmatter-v0.8` mapping) and codex `hook-wiring` upgraded to existing `merge-json` mode targeting `.codex/hooks.json` (schema matches claude-code's settings.json `hooks` table — same `event → matcher group → handlers` shape). Codex `command`, kiro `command`, and copilot's `agent` / `command` / `hook-wiring` remain dropped (no upstream projection target) — contract-driven warning rail emits a pinned stderr line per install enumerating dropped primitives + projecting compatible ones; rail fires at both `--scope repo` and `--scope user`; trigger is dynamically computed from the contract (no hardcoded adapter literal). Eight packs bump v0.7 → v0.8 (RFC-0004 atomicity); a documented two-step `uninstall + install` path covers v0.7-era codex adopters who got silent agent/hook-wiring drops. 22 ACs / 10 tasks; one PR per RFC-0004 atomicity. Amends `distribution-adapters/spec.md` (v0.7 → v0.8 Changelog entry). Depends on RFC-0009 / RFC-0005 / RFC-0012 / ADR-0002. |
| [`incompatible-hook-event-drop/`](incompatible-hook-event-drop/spec.md) | Shipped | `dropped-primitives-coverage` (Shipped), RFC-0005 | Extends the shipped `dropped-primitives-coverage` warning rail with per-file event-level granularity. Today `agentbundle validate packs/core` exits 1 because `core`'s `session-start.toml` declares `[[hooks.SessionStart]]` and Kiro's `agent-event-vocabulary` doesn't list SessionStart — the tool refuses the whole pack rather than dropping just the one incompatible file. Fix in two parts: (a) drop the incompatible file, install everything else (skills + agents + other compatible hook-wirings still project to Kiro); (b) tell the adopter clearly what got dropped — install-time three-clause warning to stderr (primitive-type clause + event-level clause + closing clause; degrades byte-identically to PR #156's wording when event list is empty), validate-time `info:` line to stdout with exit 0. New shared module `commands/_drop_warning.py` houses the enumerator + formatter; install.py + validate.py both import. `check_kiro_attach_to_agent` and `check_kiro_event_vocabulary` keep their pure-function shape; validate.py discriminates missing-attach (compat, swallowed) from unknown-agent / explicit empty-string (correctness, kept refusal) from input data, not refusal-string parsing. Install-vs-validate parse-fail asymmetry intentional and Boundaries-enforced: validate refuses on parse-fail (correctness); install enumerates as drop entry (visible in warning). Security rails (symlink under `.apm/hook-wiring/` or `.apm/agents/`) keep exit-1 unchanged. T1–T8 landed 2026-05-26. 17 ACs / 8 tasks; one PR. Four rounds of adversarial spec review converged Clean (18 → 7 → 3 → 1 fresh findings, no stasis). Depends on `dropped-primitives-coverage` (Shipped), RFC-0005. |
| [`core-install-seed-delivery/`](core-install-seed-delivery/spec.md) | Shipped | RFC-0001, RFC-0008, RFC-0010, RFC-0012 | Fixes issue #190. `agentbundle install` never delivered the `core` pack's governance seeds (`AGENTS.md`, `docs/CHARTER.md`, `docs/CONVENTIONS.md`, …); the dist build never copied `seeds/` into the APM or Claude-plugin artifacts (RFC-0001 §595 mapping never implemented); a first install over pre-existing files at projection paths was misclassified as an interrupted-install orphan and `--force` `unlink()`d them with no companion; and a fresh install recorded a phantom `unresolved-markers = ["name"]` from a documentation example. Option-2 fix: CLI `install` delivers + state-tracks seeds (shared Tier-1/2/3 helper with `scaffold`); orphan-recovery filters by the current projection so colliding adopter files get `*.upstream.<ext>` companions while genuine non-projection crumbs stay guarded (message reworded); `--force` help documents file removal; the marker scanner ignores code-fenced markers; the per-pack APM + Claude-plugin recipes ship `seeds/` inside the artifact; README + file-safety-contract reconciled + an Approver-signed RFC-0001 `## Errata` records that auto-placement is CLI-route-only. 13 ACs / 6 tasks; one PR. Depends on RFC-0001 / RFC-0008 / RFC-0010 / RFC-0012. |
| [`claude-plugins-install-route/`](claude-plugins-install-route/spec.md) | Shipped | RFC-0008 | F-claude-plugin-derivation + F-claude-plugin-install-marker: per-pack `SessionStart` writer drops `.adapt-install-marker.toml` on first install via Claude-plugins; build derives `pack.toml`, `install-marker.py`, and the `hooks.SessionStart` block into each `.claude-plugin/`. Adapter contract v0.3 → v0.4 (`install-routes` on `[adapter."claude-code"]` — present in today's v0.8 contract); marker schema gains optional `install-route`; `unresolved-markers` / `new-companions` relaxed to optional. Amends `adapt-to-project/spec.md` (3 new ACs + proactive cache-scan branch on the skill body) and `distribution-adapters/spec.md` (conformance suite cases per route). Depends on `distribution-adapters`, `adapt-to-project`. |
| [`codex-native-skills/`](codex-native-skills/spec.md) | Shipped | RFC-0009 | Flips Codex `skill` projection from `managed-block-inline` (one-line descriptions in `AGENTS.md`) to `direct-directory` (full skill bodies at `.agents/skills/<name>/SKILL.md`). Pairs the contract flip with: a one-shot migration strip on first install (removes the legacy `<!-- agent-skills:start/end -->` block in `AGENTS.md`); a uniform `project_packs(pack_paths, ...)` entry point across `codex`/`claude-code`/`kiro` (all three present in `build/adapters/`); a shared `sweep_orphans` helper (`build/projections/direct_directory.py`) that removes stale `<name>/` directories under each adapter's projected skill dir after every multi-pack call; deterministic last-wins for same-name skill collisions across packs (Q2 lean); cross-cutting orphan cleanup across all three direct-directory adapters (Q3 lean); unconditional migration strip (Q4 lean). 9 tasks; T8 amends `distribution-adapters/spec.md`; T9 lands the `tools/lint-agents-md.py` warning + changelog entry. Depends on `distribution-adapters`. |
| [`agentbundle-wheel-release/`](agentbundle-wheel-release/spec.md) | Shipped | RFC-0003 | Publishes the `agentbundle` Python wheel to a registry so adopters can `pip install agentbundle` without cloning. Closes RFC-0003 §F-cli-dist path #2 in two variants — PyPI (Trusted Publisher OIDC, secrets-free) and corp Artifactory (scaffolded; step-level guard requires all three Artifactory secrets — `ARTIFACTORY_URL`/`ARTIFACTORY_USER`/`ARTIFACTORY_TOKEN` — non-empty before publishing; awaiting AC14 first-firing once the corp configures the secrets). Shipped as `.github/workflows/release-agentbundle.yml` with the three jobs (build-and-smoke, publish-pypi, publish-artifactory). 14 ACs, 7 tasks across PR-A (T1–T6, infrastructure) and PR-B (T7, README route-3 update sequenced after first publish). Out-of-band: PyPI account + Pending Publisher (~10 min) before first tag. Amends RFC-0003 §F-cli-dist (path #2 Shipped; paths #1 zipapp + #3 Homebrew Deferred with one-line reasons). Depends on `agent-spec-cli`. |
| [`pack-allowed-adapters/`](pack-allowed-adapters/spec.md) | Shipped | RFC-0011 | Per-pack `[pack.install] allowed-adapters` declaration plus `--adapter` CLI flag plus `DEFAULT_USER_SCOPE_ADAPTER` module constant; user-scope only per RFC-0011's post-merge erratum. Contract v0.5 → v0.6, plus `[adapter.codex.scope]` table pointing user-scope codex installs at `~/.agents/skills/`. Replaces the agents-presence heuristic at `_resolve_user_scope_target_adapter` with a four-step lookup (flag → contract gate → CLI-home probe → greenfield-constant fallback); `DEFAULT_ADAPTER` + deprecation alias now live in `scope.py`, `--adapter` in `cli.py`, and seven packs declare `allowed-adapters`. Widens the literal `version != "0.3"` gate in `_kiro_target_adapters` to admit v0.6 packs. Four shipped user-scope packs (`atlassian`, `figma`, `converters`, `contracts`) bump from v0.2 → v0.6 and declare `allowed-adapters = ["claude-code", "kiro", "codex"]`. 29 ACs / 11 tasks; one PR per RFC-0004 atomicity. Amends `distribution-adapters/spec.md` (conformance cases for codex user-scope) and `agent-spec-cli/spec.md` (`--adapter` flag + four-step resolver). Depends on RFC-0011 / RFC-0009 / RFC-0005 / RFC-0004. |
| [`research-pack/`](research-pack/spec.md) | Shipped | RFC-0004, RFC-0009, RFC-0011, RFC-0012, ADR-0002 | User-scope agent-skills pack for evidence-grounded research, additive to the marketplace. Seven skills shipped at `packs/research/.apm/skills/` (`identify-perspectives`, `build-outline`, `source-map`, `research`, `devils-advocate`, `compare-hypotheses`, `decision-archaeology`); `/research` carries `mode: quick \| standard \| applied \| deep` with `quick` as default to prevent description-collision misfire and rabbit-holes on casual prompts. The fourth mode `applied` (added in the same-day amendment) calibrates the GRADE schema for prior-art / best-practice surveys across practitioner grey literature — drops `no peer review` as a downgrade factor, adds `survivorship bias` and `stale prior art` via an Applied-mode overlay. Two content-portable retrieval subagents (`evidence-retriever`, `source-extractor`) provide context-preserving parallel fetch on Claude Code; skills run inline on hosts without subagents. Methodology grounded in seven convergent disciplines (STORM, PRISMA, ACH, Wikipedia V/RS/NPOV, OSINT, GIJN, GRADE); GRADE-style confidence ratings on every standard/applied/deep artifact. Retriever convention supports MCP-native and credentialed Python scripts; examples ship for arXiv (unauth) and Perplexity (env-broker via RFC-0006-compatible declaration, forward-compatible with RFC-0013). Non-`/research` skills carry depth-via-prompt cues rather than formal mode parameters (collision-driven scaffolding does not extend to them); formal-mode promotion deferred until misfire evidence emerges. Flat directory layout per agentskills.io spec recommendation (one level under `references/` and `scripts/`). Four Diátaxis guides under `docs/guides/<quadrant>/` (tutorial / how-to / reference / explanation) ship in the same PR. 29 ACs / 22 tasks (25 ACs / 17 tasks originally, +AC26–29 and +T19–T23 from the applied-mode amendment); one PR per the converters-pack precedent. |
| [`agentbundle-config-subcommand/`](agentbundle-config-subcommand/spec.md) | Shipped | RFC-0011, RFC-0012 | Post-`pip install agentbundle`, the user can change the default adapter on their machine without monkey-patching `scope.DEFAULT_ADAPTER`. New `agentbundle config {get,set,unset,path}` subcommand (`commands/config.py`, wired in `cli.py`) reads/writes `<user-config-dir>/agentbundle/config.toml` (platform-conventional: `~/Library/Application Support/...` on macOS, `${XDG_CONFIG_HOME:-~/.config}/...` on Linux, `%APPDATA%\...` on Windows; rolled by hand, no `platformdirs` dependency). A new `scope.configured_adapter` reporter + a new pre-flight block in `_resolve_target_adapter` (inserted between Step 2 state-hint and Step 3, **only when `state_adapter is None`** — upgrades preserve their existing adapter) consult the user-config and either return the configured candidate or **raise `_AdapterResolutionRefused` with a clear "pack X is not supported with your configured adapter Y" message** (AC13 / AC14). Steps 3 / 4 / 4b / 5 are *untouched*; for users who never ran `config set`, every behavior on those steps matches today exactly. Loader fails soft on malformed TOML so `agentbundle --help` keeps working. New autouse conftest fixture redirects `HOME` / `XDG_CONFIG_HOME` / `APPDATA` per-test so no test reads or writes the developer's real user-config-dir. New reference page at `docs/guides/_shared/reference/agentbundle.md` (`pip install`, install a pack, configure default adapter). Zero new runtime deps. 20 ACs / 6 tasks; six rounds of adversarial spec review converged Clean. Depends on RFC-0011 / RFC-0012. |
| [`credential-broker-contract/`](credential-broker-contract/spec.md) | Shipped | RFC-0013, RFC-0006, RFC-0011 | Promotes RFC-0006's security invariants (Don't-block presence, argv ban, no plaintext dotfile reads, never-logged, corporate-network env passthrough) to a broker-agnostic contract; demotes the env → keychain → dotfile resolver from "the convention" to "one of four brokers" keyed by a new `metadata.auth: <broker-id>` frontmatter field (`env`, `cli`, `creds`, `sso-cookie`). Ships the `creds` broker as an in-process vendored Python shim (`credentials_shim.py` projected into every consumer skill's `scripts/` directory + `.agentbundle/bin/`) by a new `shared-libs/` build-pipeline primitive class; ships the `sso-cookie` broker as a subprocess at `~/.agentbundle/bin/sso-broker.py` (sibling to every adapter's skills directory, not LLM-auto-discoverable) projected by a new `adapter-root-bins/` build-pipeline primitive class. New user-scope pack `credential-brokers` (present at `packs/credential-brokers/`) carries both broker artefacts plus one LLM-cooperative `credential-setup` skill (deliberate exception with output discipline keeping cleartext out of LLM-visible scope). **Contract version label reconciled with RFC-0012:** the RFC text targets v0.6 → v0.7, but RFC-0012 already claims v0.7; per the RFC-0013 § 4 disjoint-label rule, this spec defers its label assignment to whichever bumps the contract later, resolved during the broker-pack PR. Governance-only contract delta (header-comment + `.agentbundle/` prefix non-regression). Migrates the five in-tree consumers (`figma` + four atlassian skills) to `from .credentials_shim import`; removes `agentbundle.credentials` + `agentbundle/creds/` in the last PR, bumping `agentbundle` 0.1.x → 0.2.0. *(The teaching skills `example-credentialed-skill` / `add-credentialed-skill` were retired 2026-05-31 — see the spec's § Changelog and RFC-0013 § Errata.)* 48 ACs / 15 tasks across three phases (broker first; consumers next; cleanup last). Amends `skill-secrets/spec.md` (AC34/AC35 invariants migrate to the shim's import surface) and `distribution-adapters/spec.md` (single Changelog bullet recording the contract bump + two new build-pipeline primitive classes). Depends on RFC-0013 / RFC-0011 / RFC-0012 (sequencing) / RFC-0006 / RFC-0004 / RFC-0007. |
| [`credbroker/`](credbroker/spec.md) | Shipped | RFC-0023 (reverses ADR-0003) | **RFC-0023 follow-on, Phase 1.** Replaces the build-projected `credentials_shim` with a standalone, pip-installable **`credbroker`** library at `packages/credbroker/` (sibling of `agentbundle`), consumed **in-process**. Stdlib-only core (env → OS keyring → 0600 dotfile, preserving the shim's full `__all__` verbatim) + an optional **`[crypto]` extra** adding an encrypted-at-rest vault (Argon2id → KEK → AES-256-GCM) for the Tier-3 floor; `_vault` imported **lazily** so base import-graph purity (AC4) holds. Master-secret unlock is **no-daemon** (OS keyring → env → 0600 file; per-invocation KEK). Migrates the **six** consumers off the projected shim: the five API CLIs (`jira`, `jira-align`, `confluence-publisher`, `confluence-crawler`, `figma`) are import-line-only via `_client.py`; **`credential-setup`** is the load-bearing case — it imports the shim's *private* surface today (`_dotfile_write`/`_parse_schema`/`_tier2_backend`/`_tier2_backend_label`), so credbroker must expose **public** write + schema-parse + Tier-2-introspection APIs, and it gains its **first** `requirements.txt` + a **new** `test_setup.py`. Retires the `auth: creds` `shared-libs/` projection into consumer `scripts/` + its drift gate, but **keeps** the `shared-libs/credentials_shim.py` source (still the `sso-broker` companion-shim projection source, AC22b — SSO migration out of scope). Resolution parity proven by the **ported** shim suite (not `test_exit_codes.py`, which is exit-codes only); `3–9` exit band stays unclaimed (no v1 CLI). Migration is **import-line-only** for the five CLIs and the resolver import stays lazy, so a missing `credbroker` exits 1 at runtime like a missing shim today — **no** exit-code-contract amendment (an exit-2 "pip install" UX would need a deliberate import hoist, deferred). **Phase 2 (PyPI publish + version pinning) shipped out-of-band 2026-06-10** (`credbroker 0.1.0` on PyPI; `docs/backlog.md#credbroker-phase-2`). The reversal ADR is a separate parallel governance artifact. 19 ACs / 10 tasks. Spec-mode adversarial review converged Clean (3 blockers/5 concerns/3 nits → 0). Depends on RFC-0023; preserves RFC-0006/RFC-0013 semantics. |
| [`kiro-ide-hook/`](kiro-ide-hook/spec.md) | Shipped | RFC-0005 | Stub / loop anchor for the `kiro-ide-hook` primitive — Kiro IDE event hooks plus the validate-time sibling-vocabulary lift and follow-on artifacts. Canonical design lives in RFC-0005; the spec amendments this work delivered landed in two existing specs (`distribution-adapters`, `agent-spec-cli` — the v0.4 primitive amendment) rather than this stub, which exists only so `loop-cohort.py` has a directory to point its state file at. Implementation merged via PR #99 (`kiro-ide-hook-impl`); subsumed by the now-v0.8 contract. See `plan.md` for the task breakdown. Depends on RFC-0005. |
| [`lint-packs-target-vocab/`](lint-packs-target-vocab/spec.md) | Shipped | RFC-0001 | Adds a `make build`-time gate so pack authors editing a skill `SKILL.md` or agent `.md` learn immediately when a name or description exceeds the strictest declared per-target constraint (today a 2000-char description is silently accepted, then rejected or ellipsis-truncated by Kiro / Codex at install). Constraints come from a new sibling file `docs/contracts/target-vocab.toml` — **not** `adapter.toml` (adding tables there would change the contract grammar = RFC scope; sibling-file = PR scope). Gate lives in `build/lint_packs.py` (tested by `build/tests/test_lint_packs.py`); it **refuses** offending source content and never rewrites at emit time, preserving RFC-0001's byte-equal projection invariant. No `adapter.schema.json` / `adapter.toml` change. Depends on RFC-0001. |
| [`converters-pack/`](converters-pack/spec.md) | Shipped | RFC-0007 | First user-scope pack: imports `file-to-markdown`, `markdown-to-html`, `msg-to-markdown` from the source catalogue as a vendored snapshot (shipped at `packs/converters/`; `mermaid-renderer` added later). `default-scope = "user"`, `allowed-scopes = ["user", "repo"]`. 8 ACs (pack.toml shape, attribution scrub, Rail C clean, evals carry-over, evals JSON validity, source SHA pinned, fixture-`$HOME` install/uninstall, pytest CI invocation, runtime-dep disposition, `node_modules/` gitignore note). 7 plan tasks; one PR. Depends on RFC-0004's user-scope dimension. |
| [`pluggable-api-standards/`](pluggable-api-standards/spec.md) | Shipped | RFC-0017 | **Stage 1** of RFC-0017: turns `api-contract` from a Zalando-hardcoded skill into a standard-driven engine — the skill keeps the API-design *method*, the ruleset becomes swappable **base+delta** YAML data (`extends: zalando` + override-to-`false` + house-rule `adds`, the Spectral model) so the catalogue is ready for different organisations without forking. Zalando ships as the bundled, replaceable base; existing direct invocations are unchanged. Bundle is agent-read (no program parses it; no linter — the first persistent contract lint is Stage 2); the manifest is the authoritative enumeration of applied rule IDs + attribution (CC-BY-4.0 + the 5 excluded IDs). Token-set-preservation guard (133 distinct `#NNN` over `SKILL.md` + `references/**`; the 9 SKILL-only tokens relocate to the manifest). Adds a standard-independent **design-discipline** section (rejected rationalizations + consistency red flags; rule-specifics deferred to the active standard). Custom-standard delivery via `adapt-to-project` Class 2 `.upstream` companion-merge (no new resolver, no `adapt` edit). Scoped to `packs/contracts` only — the `new-spec` seam, repo-level `contracts/<type>/` tree, traceability, and Class 3 amendment are **Stage 2** (`spec-contract-seam`). 12 ACs / 4 tasks; one PR. Spec-mode adversarial review converged Clean in 4 passes. Depends on RFC-0017. |
| [`doc-drift-prevention/`](doc-drift-prevention/spec.md) | Shipped | RFC-0016 | Implements RFC-0016: prevents spec-metadata drift through construction + judgment for adopters (canonical-by-birth `new-spec` template with the `(deferred: <anchor>)` hatch; sharpened `adversarial-reviewer` "Spec drift" check naming four invariants; the same invariants in the work-loop finish-time checklist; the contract pinned in `CONVENTIONS.md` § 4; a durable `docs/backlog.md` register replacing the "PR is the durable record" rule), and keeps a hard mechanical gate as **catalogue governance** — `lint-spec-status.py` runs from the Makefile `build-check` target (NOT the projected `pre-pr.py`). Lint checks four invariants over `docs/specs/*/spec.md`: (i) leading status token in vocabulary; (ii) ACs checked-or-deferred at the diff-triggered ship transition (Frozen corpus grandfathered); (iii) dangling intra-repo doc refs (warn-only, v1); (iv) `(deferred:)` anchors resolve in `docs/backlog.md`. Seeds `backlog.md` as a Manual placeholder (added to `EXCLUDED_PATTERNS`, preserved on self-host). 13 ACs / 10 tasks; one PR. Spec-mode adversarial review converged Clean in 2 passes. Depends on RFC-0016. **Erratum:** RFC-0016's "linters don't project" premise was wrong — the lint was relocated to a `work-loop` skill script and now ships to adopters too (see `lint-work-loop-delivery` + ADR-0007); this spec's catalogue-only Boundaries are superseded (spec § Changelog). |
| [`spec-code-ref-lint/`](spec-code-ref-lint/spec.md) | Shipped | RFC-0016 | RFC-0016 invariant-(iii) v1.1: extends the doc-drift lint's dangling-reference check from `.md` doc links to repo-relative **code** references (warn-only). Detects full paths rooted at a known top-level dir (or `../` link), code extension, locator suffix stripped; excludes bare basenames, `<placeholder>`, `*glob`, `{brace}` shorthand. Found ~15 genuine stale refs in the corpus (`.sh`→`.py` ports, files removed in the broker migration) — all on Frozen specs, vindicating warn-only; promote-to-hard stays deferred. 7 ACs / 3 tasks. Depends on `doc-drift-prevention`. |
| [`lint-work-loop-delivery/`](lint-work-loop-delivery/spec.md) | Shipped | RFC-0016 (§ Errata) | Corrects RFC-0016's false "linters don't project" premise (RFC § Errata + ADR-0007; `doc-drift-prevention` § Changelog erratum) and **relocates** the spec-metadata lint from `tools/` to `packs/core/.apm/skills/work-loop/scripts/lint-spec-status.py` — a projecting skill script (like `loop-cohort.py`). The agent runs it at the work-loop finish-time checklist on every adapter with Python (available, not fail-closed); the catalogue still runs it as a fail-closed CI gate via `make build-check`. Relocation + wiring only — no invariant-logic change. 7 ACs / 3 tasks. Depends on `doc-drift-prevention`, `spec-code-ref-lint`. |
| [`skill-secrets/`](skill-secrets/spec.md) | Shipped | RFC-0006, ADR-0002 | RFC-0006 implementation: two-layer architecture (skills don't hold credentials; credentialed primitives do); three storage tiers (env → OS keyring (macOS `/usr/bin/security`, Windows ctypes against `advapi32`) → `~/.agentbundle/credentials.env`); stdlib-only `agentbundle.credentials` loader; `agentbundle creds` verb (`setup`/`check`/`where`/`rm`, no `get`); SKILL.md `metadata.credentialed` + `metadata.primitive-class` frontmatter (nested under the agentskills.io-spec `metadata:` escape hatch); `conventions-check` argv-ban + "Don't"-block lint; ADR-0002 narrow-"hook-shaped" amendment. AC34/AC35 inheritance invariants enforced by future test PRs adding fixtures; everything else closed across T1–T13c. |
| [`wave-scheduled-supervisor/`](wave-scheduled-supervisor/spec.md) | Shipped | RFC-0015, ADR-0005 | Generalizes `work-loop` supervisor mode in `loop-cohort.py`: build the full `Depends on:` DAG, detect cycles + forward-references, execute in **topological order sequentially by default** on every adapter (removes the auto-parallel-on-`Depends on: none` branch). Parallel implementer **writes** become opt-in and **gated** (required AC5 / decision 3): a wave parallelizes only if every task is in a safe category (cannot-collide / typed-Group-B / textual-loud) **and** passes a `git merge-tree` file-disjointness check, else serial; `merge-abort` preserved. Makes `Depends on:` machine-parseable (robust parser + `spec:<name>/TN` cross-spec marker in the plan template) and adds a `new-spec` cycle/forward-ref lint. Reuse `loop-cohort` + worktrees — no new module/dep/dir (Boundary). 9 ACs / 6 tasks; T4 carries a mandatory real `git worktree add` dry-run (AC9). T2-live (real-implementer break frequency) out of scope. |
| [`supervisor-auto-parallel/`](supervisor-auto-parallel/spec.md) | Shipped | RFC-0015, ADR-0005 | ROADMAP follow-on 4 (autonomy capstone) of `wave-scheduled-supervisor`: a per-run `auto_parallel` `state.json` field (default off; set by `loop-cohort auto-parallel` / `--off`) pre-authorizes an **unattended** run to fan out a **gate-cleared** wave without the follow-on-1 human opt-in — so a plan finishes while you're away. **GO-approval-only**: never a gate input, never parallelizes a non-cleared wave, and a failed parallel wave still Surfaces and stops (no auto-recover). Per-run session scratch (fresh run defaults off — no forgot-it's-on footgun). Setter verb mirrors `approve-plan`; doc branch in SKILL §EXECUTE + supervisor-mode.md. All in `loop-cohort.py` + `state.json` + docs — no new module/dep/dir. 6 ACs / 2 tasks. Depends on `wave-scheduled-supervisor`. |
| [`supervisor-predict-disjointness/`](supervisor-predict-disjointness/spec.md) | Shipped | RFC-0015, ADR-0005 | ROADMAP follow-on 3 of `wave-scheduled-supervisor`: an **optional** per-task `**Touches:**` glob line lets `loop-cohort schedule` annotate each multi-task wave `predicted-disjoint: yes\|no\|unknown` from declared globs — a cheap pre-dispatch screen. **Screen-only, never greenlight**: a predicted overlap serializes early; `yes`/`unknown` never substitute for the authoritative post-write `git merge-tree` (the trust-globs path is rejected — under-declaration is unsafe). `globs_overlap` is conservative (returns overlap unless *provably disjoint* segment-wise; `*` never crosses `/`; `[…]` is a pattern not a literal). Missing `Touches:` → `unknown`, never an error (no backfill of the 244 existing tasks). `parse_plan` signature unchanged (separate `parse_touches_by_task` accessor). All in `loop-cohort.py` + plan template — no new module/dep/dir. 7 ACs / 4 tasks. Depends on `wave-scheduled-supervisor`. |
| [`supervisor-auto-classify/`](supervisor-auto-classify/spec.md) | Shipped | RFC-0015, ADR-0005 | ROADMAP follow-on 2 of `wave-scheduled-supervisor`: auto-derive each supervisor task's conflict category from its committed branch diff so `dispatch-decision` no longer needs a hand-supplied `--category`. Fail-closed: only all-added (status `A`) + no-danger-path + no cross-branch basename/dir collision → `cannot-collide`; rename/copy/delete, danger-paths (migrations incl. top-level/lockfiles/manifests/`__init__`/index/CI/`Makefile`/`marketplace.json`), and modified-existing serialize. A **cross-branch symbol guard** (`added_paths_may_share_symbol`) serializes all-`cannot-collide` waves whose added files share a basename/real-subdir; base resolution fails closed (unresolvable/ambiguous/empty → serial); the irreducible string-key/cross-file-symbol residual is **named, not claimed** (post-merge-test-gate backstop, same as RFC-0015 §2). `--category` becomes an optional human **override** (still the only way to assert `typed-group-b`, never auto-derived); `_dispatch_rationale` change additive so parent AC10 holds. All inside `loop-cohort.py` — no new module/dep/dir. 12 ACs / 3 tasks. ~5.4% measured `cannot-collide` yield (notes/yield.md); value is removing 100% of routine classification labor + fail-closed defence. Depends on `wave-scheduled-supervisor`. |
| [`wire-session-start-hook/`](wire-session-start-hook/spec.md) | Shipped | RFC-0001, RFC-0004 | Ships hook-wiring for the core pack's `session-start.py` hook body so `agentbundle install core` auto-writes the Claude Code `SessionStart` binding into `<output>/claude-plugins/core/.claude/settings.local.json` (dist-tree path; Claude Code's plugin marketplace consumes from there) and into the workspace flat path `<workspace>/.claude/settings.local.json` via self-host. Wiring TOML uses Claude Code's documented nested SessionStart schema. Bundled a legacy-fixture rewrite of three stale `pre-commit.toml` upgrade-catalogue fixtures to nested shape with a static stub command. Mid-EXECUTE spec amendment corrected paths from flat to dist-tree (root cause: original ACs cited paths without tracing them to the producing recipe). Fixed a latent self_host.py drift-loop bug along the way (now consults `EXCLUDED_PATTERNS`). 10 ACs, 7 tasks; PR #98. Kiro support deferred to a separate spec that needs a `steering` primitive. |
| [`windows-hooks-phase3/`](windows-hooks-phase3/spec.md) | Shipped | none | Step 3 of the Windows-support plan: rewrites the seven bash scripts forming the pre-PR + session-start runtime as pure-stdlib Python (`packs/core/.apm/hooks/{session-start,pre-pr}.py` + the `tools/` mirrors; `tools/lint-{agents-md,build,knowledge,agent-artifacts}.py` + `lint-skill-spec.py`), parity-pinned to identical stdout shapes / exit codes / check semantics, with the `.sh` siblings removed (one runtime, not two). Adds pytest smoke tests under `packages/agentbundle/tests/hooks/` mirroring the bash runners' corruption cases. `lint-agents-md.py` check #2 accepts the Windows-materialised-symlink shape (a regular file whose content is exactly `AGENTS.md`). Stdlib-only (enforced by `lint-build.py`'s import audit). Retroactively promoted from the work-loop TRIO to spec/plan on 2026-05-31; the `lint-skill-deps`→`lint-skill-spec` rename (PR #111) is noted in the plan. 7 ACs. |
| [`windows-ci-bundler/`](windows-ci-bundler/spec.md) | Shipped | none | Step 1 of the Windows-support plan: adds `.github/workflows/build-check-windows.yml` — a `build-check-windows` job on `windows-latest` that runs the bundler `build` + `check` (the Windows mirror of the Linux `make build-check`) with `PYTHONUTF8=1`, so the `windows-hooks-phase3` port can't silently regress. Shipped as a **separate workflow** (not an OS matrix on `build-check.yml`) because GitHub `paths-ignore` is workflow-level: `paths-ignore: docs/**` lets a docs-only PR skip the Windows parity job while the Linux required check still runs and stays untouched. Retroactively promoted from the work-loop TRIO to spec/plan on 2026-05-31; PR #77 + follow-up `dfb9dc8`. 5 ACs. |
| [`adopter-clean-enforcement-gate/`](adopter-clean-enforcement-gate/spec.md) | Shipped | RFC-0015, RFC-0013, RFC-0002 | Hook/command analogue of issue #190: shipped `core` primitives must not reference catalogue-internal tooling or break on arrival. The shipped `pre-pr.py` hard-crashes adopters (shells out to 8 repo-native checks — 7 linters + a self-test — that never install); the `new-spec` `plan.md` template name-drops repo-native `tools/lint-plan-deps.py`; `session-start.py` hints at a repo-native linter; `max_iterations` is double-sourced; `CONVENTIONS.md` § Enforcement / `README.md` describe a catalogue-internal CI gate as the adopter's. Fix: none of the 7 linters are adopter-generic, so the shipped gate ships **zero** of them — `pre-pr.py` runs only the adopter-relevant `loop-cohort.py check` + a "wire your own gate" stub, degrading gracefully; the 7 linters move to a repo-native `tools/pre-pr-catalogue.py` (never projected); template→`loop-cohort schedule`; `session-start` hint fixed; `max_iterations` single-sourced; doc honesty across `CONVENTIONS.md`/`conventions-check.md`/`README.md`; local-only principle recorded in `AGENTS.local.md` (not RFC-gated). `lint-plan-deps` (RFC-0015) and `add-credentialed-skill`/`example-credentialed-skill` (RFC-0013) are owned by other specs — routed to `docs/backlog.md`, untouched. 10 ACs / 8 tasks; one PR. |
| [`tdd-stub-generation/`](tdd-stub-generation/spec.md) | Shipped | RFC-0028 | **RFC-0028 implementation.** Weaves TDD-stub generation into the core loop via a load-on-demand, projected reference `packs/core/.apm/skills/work-loop/references/tdd-stubs.md`: PLAN's *Design tests up front* step turns each TDD-mode AC into a compilable, validated **red stub** in `plan.md`'s per-task `Tests:` subsections (one file per task; `// STUB:` comment + `stub: true` field), consumed unchanged by EXECUTE; `new-spec` Testing Strategy gains a testability self-check pointer; `CONVENTIONS.md` § 4 + `quality-engineer` get the handoff/timing note. Docs-only (no code) — verified goal-based (`make build-self`/`make build-check` zero-drift, `lint-skill-spec.py`) + a manual spike (stubs from a Shipped spec compile against the AC surface, red against an absent impl). **No new skill, no new gate, no `coverage-matrix.md`.** Reference ships a Python/pytest worked example + generic stack-detection. 10 ACs / 7 tasks. Depends on RFC-0028. |

## Adding a new spec

```bash
mkdir -p docs/specs/<feature-name>
cp .claude/skills/new-spec/assets/spec.md docs/specs/<feature-name>/spec.md
cp .claude/skills/new-spec/assets/plan.md docs/specs/<feature-name>/plan.md
```

Or, in Claude Code, run `/new-spec "<feature-name>"`.
