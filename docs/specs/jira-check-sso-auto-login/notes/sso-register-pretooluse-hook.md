# SSO pre-tool-use hook disposition anchor

This note preserves the frozen `sso-register-pretooluse-hook` deferral identity.
The optional hook is retired: no implementation work remains unless a future
request reactivates it under a new accepted intent.

