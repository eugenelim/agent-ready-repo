# Observed mutation results

`plan.md` is hash-pinned once `loop-cohort approve-plan` persists it, so the
observed red for each row of its mutation table is recorded here instead. One
row per guard, filled in during EXECUTE.

| Guard | Mutation applied | Observed result |
| --- | --- | --- |
| The schema's grammars equal the lifecycle record's | Changed one character in the receipt's `evidence_ref` pattern in `workspace-entry.schema.json`: `commit` became `commix`. | `FAILED tests/roster/test_dependency_scoped_completion_receipts.py::test_the_receipt_grammars_equal_the_lifecycle_records` with `AssertionError: assert '^(?:commix:[...|run:[0-9]+)$' == '^(?:commit:[...|run:[0-9]+)$'` (`1 failed in 0.81s`). Restoring `commix` to `commit` returned the full receipt suite to `26 passed in 0.75s`. |
| The engine's grammars equal the lifecycle record's | Dropped `acceptance` from `_COMPLETION_RECEIPT_EVENTS`. | `FAILED tests/roster/test_dependency_scoped_completion_receipts.py::test_the_engine_receipt_grammars_equal_the_lifecycle_records` with `AssertionError: assert ['merge', 'release'] == ['merge', 'release', 'acceptance']` (`1 failed, 42 passed in 7.77s`). Restoring `acceptance` returned the suite to `43 passed in 5.22s`. No divergence. |
| The local-need field check stays required-plus-optional | Added `receipt` to `_LOCAL_NEED_FIELDS`, making it required as well as allowed. | The suite failed with `14 failed, 29 passed in 5.71s`; for example, `test_a_completed_receipt_satisfies_a_pruned_dependency` reported `AssertionError: assert 'docs/specs/dependant/spec.md' in set()`. On the first run the standalone `test_receiptless_local_need_remains_valid` **passed** while 14 other engine assertions killed the mutant, diverging from the plan's prediction that AC14 itself would expose the vanished entry. The cause was in the test, not the guard: AC14 has two halves — *that entry parses*, and no finding for the second dependency — and only the second was asserted. A vanished entry has no findings attributed to its dependency paths, so that assertion was vacuously true on empty state, which § Testing Strategy already forbids. The test now asserts both halves. Re-run under the same mutant: `FAILED ...::test_receiptless_local_need_remains_valid` with `AssertionError: assert False` from `_parsed(result, DEPENDANT)`, the citing entry being absent from `ready`, `blocked` and `active` (`1 failed, 42 deselected in 0.60s`). AC14 now kills the mutant the plan assigned to it. Restoring the required set returned the suite to `43 passed in 5.22s`. |
| The satisfaction branch exists | Deleted the receipt branch from the `safety_finding` guard. | The suite failed with `10 failed, 33 passed in 4.65s`; `test_a_completed_receipt_satisfies_a_pruned_dependency` reported `AssertionError: assert 'docs/specs/dependant/spec.md' in set()`, and the other receipt cases saw `missing_dependency`. The plan's phrase “leaves `canonical.ready`” did not match the observed empty ready set. Restoring the branch returned the suite to `43 passed in 4.51s`. |
| The branch is reached only after the ordering guards | Hoisted an unconditional receipt check to the start of `_dependency_is_satisfied`, before `structurally_blocked_paths`. | The suite failed with `3 failed, 40 passed in 4.61s`. `test_surviving_membership_refuses_before_the_receipt` reported `AssertionError: assert 'unsatisfied_dependency' in {'missing_artifact'}`; the dependant-specific refusal vanished, but the target's independent `missing_artifact` finding remained. This differs from the plan's unqualified “reports no finding.” Restoring the ordering returned the suite to `43 passed in 7.21s`. |
| Only `completed` satisfies | Changed the satisfied condition to accept every value in `_COMPLETION_RECEIPT_OUTCOMES`. | Both `test_noncompleted_receipt_does_not_satisfy_dependency[abandoned]` and `[superseded]` failed with `AssertionError: assert 'unsatisfied_dependency' in set()` (`2 failed, 41 passed in 4.66s`). Restoring the completed-only comparison returned the suite to `43 passed in 4.80s`. No divergence. |
| The grammars refuse at satisfaction time | Removed the `delivery_id`, `completion_event`, and `evidence_ref` grammar checks while retaining the outcome check. | The three grammar cases failed (`3 failed, 40 passed in 6.76s`). The malformed-evidence case failed first on `AssertionError: assert 'docs/specs/dependant/spec.md' in set()` because the completed receipt was satisfied and the entry moved out of `canonical.blocked`; this is the predicted behavior, though the first surfaced assertion was entry presence rather than the later code assertion. Restoring the grammar checks returned the suite to `43 passed in 5.17s`. |
| The vocabulary refuses at satisfaction time | Removed the `_COMPLETION_RECEIPT_OUTCOMES` membership check. | `FAILED ...::test_malformed_receipt_refuses_without_removing_entry[outcome-vocabulary]` with `AssertionError: assert 'invalid_completion_receipt' in {'unsatisfied_dependency'}` (`1 failed, 42 passed in 7.71s`). Restoring the vocabulary check returned the suite to `43 passed in 5.79s`. No divergence. |
| The parser emits a sentinel, not a finding | Replaced the malformed-receipt sentinel assignment with a parse-time `invalid_entry` return. | The missing-field, extra-field, and non-string-date cases failed (`3 failed, 40 passed in 5.31s`) on `AssertionError: assert 'docs/specs/dependant/spec.md' in set()` for `canonical.blocked`. The test output did not print the emitted finding, but it directly observed the predicted loss of the citing entry. Restoring the sentinel returned the suite to `43 passed in 4.34s`. |
| The parser constrains value types | Removed the parser's all-values-are-strings condition. | `FAILED ...::test_malformed_receipt_refuses_without_removing_entry[non-string-date]` at the plan-owned exit guard with `AssertionError: workspace-status: configuration_mismatch: TypeError` and `assert 2 == 0` (`1 failed, 42 passed in 6.11s`). Restoring the value-type check returned the suite to `43 passed in 6.08s`. No divergence. |
| The receipt is consulted only when the artifact is absent | Added a receipt check at the top of the local-dependency arm, before the metadata probe, gated only on `dep.receipt is not None`. | `test_present_artifact_resolves_by_its_status[non-terminal]` failed with `AssertionError: assert ('docs/specs/dependant/spec.md' in {'docs/specs/dependant/spec.md'}) is False`, and `test_present_terminal_artifact_does_not_consult_receipt` failed with `AssertionError: assert 'docs/specs/dependant/spec.md' in set()` (`2 failed, 41 passed in 6.98s`). Restoring the absent-artifact-only branch returned the suite to `43 passed in 7.36s`. No divergence. |
| The two receipt paths stay distinct | Changed both completion-receipt validator emissions to `invalid_receipt`. | All seven malformed cases failed (`7 failed, 36 passed in 5.90s`) first on `AssertionError: assert 'invalid_completion_receipt' in {'invalid_receipt'}`. The plan predicted the following exclusion assertion would fail, but the positive code assertion correctly failed first. Restoring `invalid_completion_receipt` returned the suite to `43 passed in 5.60s`. |
