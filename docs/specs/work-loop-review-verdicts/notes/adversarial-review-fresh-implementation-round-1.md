## Blockers

**1. Shipping metadata is still open.** `docs/specs/work-loop-review-verdicts/spec.md:3`. Fix: set the completed spec to `Shipped`, plan to `Done`, and specs index to `Shipped`; AC12 remains explicitly deferred.
