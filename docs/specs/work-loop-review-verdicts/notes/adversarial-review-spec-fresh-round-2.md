## Blockers

**1. Light-mode adversarial absence is still readiness-compatible in the REVIEW path.** `packs/core/.apm/skills/work-loop/SKILL.md:654`. Fix: require the adversarial evidence in both modes before specialist routing; missing light-mode adversarial evidence is mandatory `missing` and `BLOCKED`.
