# Tracker materialized-child scope migration anchor

This note preserves the frozen `tracker-refresh-materialized-child-scope`
deferral identity. Its retained outcome is owned by
[`tracker-refresh-trusted-authority-inputs`](../../../product/intents/tracker-refresh-trusted-authority-inputs.md).

