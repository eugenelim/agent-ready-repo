# Packs normative-pointer disposition anchor

This note preserves the frozen `packs-agents-normative-pointer` deferral
identity. The gap is retired under the path-free option: `packs/AGENTS.md`
states that `pack.toml` fields belong to the pack JSON Schema and routes authors
to the catalogue authoring standards without embedding a repository-only schema
path. No schema copy is added to the authoring scaffold.
