# jsonl-otlp-exporter

Send JSONL records to an OpenTelemetry Collector as OTLP logs.

Nothing is sent until an endpoint is configured.

<!-- The published contract -- what this sends, the profile format, and the
     compatibility statement -- is authored by T7, which owns this file's
     content under AC-0030, AC-0031 and AC-0032. -->
