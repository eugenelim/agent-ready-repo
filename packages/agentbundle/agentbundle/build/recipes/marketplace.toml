# Default recipe: aggregate the per-pack Claude plugins that the route
# published — every user-capable pack — into one marketplace.json at
# dist/claude-plugins/marketplace.json.

[recipe]
name = "marketplace"
type = "aggregate"
input-subdir = "claude-plugins"
output-file = "claude-plugins/marketplace.json"
