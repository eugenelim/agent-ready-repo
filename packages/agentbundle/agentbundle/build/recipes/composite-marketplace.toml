# Self-host recipe: compose a self-host marketplace listing from each
# user-capable pack's .claude-plugin/plugin.json (distinct from the dist/-aggregating
# `marketplace` recipe).

[recipe]
name = "composite-marketplace"
type = "composite"
manifest-path = ".claude-plugin/plugin.json"
output-file = ".claude-plugin/marketplace.json"
