[pack]
name = "core"
version = "0.1.0"
description = "Core agent-ready-repo pack — fixture for T8 end-to-end."
seeds = ["AGENTS.md"]

[pack.adapter-contract]
version = "0.3"

[pack.install]
default-scope = "repo"
allowed-scopes = ["repo", "user"]
user-scope-hooks = true
