[pack]
name = "with-reserved"
version = "0.0.1"
description = "Windows-portability lint fixture: ships seeds/CON.md to prove the lint rejects Windows-reserved names. Not for installation."

[pack.adapter-contract]
version = "0.2"

[pack.install]
default-scope = "repo"
allowed-scopes = ["repo"]
