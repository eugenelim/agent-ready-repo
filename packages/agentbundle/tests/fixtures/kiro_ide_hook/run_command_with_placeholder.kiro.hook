{
  "name": "Lint on save (runCommand)",
  "description": "Invoke the pack's lint hook-body directly on save.",
  "version": "1",
  "when": {
    "type": "fileSave",
    "patterns": ["**/*.py"]
  },
  "then": {
    "type": "runCommand",
    "command": "${hook-body:lint}"
  }
}
