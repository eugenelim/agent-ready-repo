# Current declared intent. Structured fields are authoritative; durable rationale
# belongs in the cited artifact or Git history. Comments state only current facts
# that the schema cannot express.
#
# HOW TO WRITE AN ENTRY -- read this before adding one.
#
# Target entries are inline tables with exactly:
#   path, kind, source, summary, needs
#
# Example:
#   { path = "docs/specs/<slug>/spec.md", kind = "spec", source = { mode = "repo-origin" }, summary = "Example spec", needs = [] }
#
# DO NOT INFER THE SHAPE FROM NEIGHBOURING ENTRIES. Most records in this file
# are legacy compatibility forms -- bare "spec/<slug>" strings and {slug = ...}
# tables. They are retained deliberately, they reconcile as `unsupported_legacy`
# or `legacy_entry`, and they are never dispatchable. Copying one writes a
# non-dispatchable record. `workspace-status` reports every such record; run it
# after editing this file.
#
# `[backlog].open` takes the canonical shape like every other collection.
# `lint-spec-status.py` invariant (iv) resolves a spec's `(deferred: <slug>)`
# anchor from either shape -- a legacy record's `slug`, or a canonical record's
# `path` via `canonical_entry_anchor` -- so a deferring spec is not obliged to
# write a legacy record. This comment previously claimed the `slug` key was
# still required; that stopped being true when canonical anchoring shipped.
#
# A canonical entry's `path` must name an artifact that exists, or it reconciles
# as `missing_artifact`. For a deferral with no artifact of its own, point the
# entry at the document that records it.
#
# Comments, summaries, list order, tracker labels, and profile hints are
# non-semantic. They must not determine routing, dependency satisfaction,
# processor selection, or dispatch.
#
# Paths are repository-relative POSIX paths. Consumers must reject absolute
# paths, backslashes, ".." segments, and any symlink-resolved target outside the
# repository root.

["ini-002"]
name      = "Platform Core"
status    = "active"
milestone = "P5 · Adopt (M1–M5 shipped)"
parent    = "INI-001"

["ini-002".shaping_queue]
active  = []
backlog = [
  # `research` -- outcome NOT defined. What a loop does when it discovers the
  # artifact above it is wrong, at any phase, and which level it returns to.
  # Six competing hypotheses; two are measurable retrospectively against review
  # artifacts already on disk, so the first spikes are cheap. `needs` is empty
  # deliberately: an earlier edge to the prevention brief made this BLOCKED with
  # unsatisfied_dependency, which was wrong -- the relationship is shared evidence,
  # not sequencing, and the retrospective spikes run today. Cross-referenced in
  # prose instead.
  {path = "docs/product/intents/agent-loop-escalation-recovery.md", kind = "intent", source = {mode = "repo-origin"}, summary = "Decide what a work-loop does when PLAN, EXECUTE, GATES, or REVIEW discovers the contract above it is wrong -- which artifact level to return to and on what signal, against six competing hypotheses none of which is validated, plus the detection half that is small enough to ship on its own", needs = []},
  # Signal items — hand-authored by strategist; ongoing, non-graduating
  {slug = "ai-native-regulatory-watch", type = "signal"},
  {path = "docs/product/intents/ini-002-initiative-brief.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/rfc/0064-ini-001-ai-native-ecosystem.md", revision = "sha256-bytes-v1:26693aa8f9a49eba83258db06be0795b664cb2c09d4c0742ed9b502666d8efca"}, summary = "Reconcile Platform Core's accepted proposal, shipped milestones, and remaining adoption work into one living initiative brief", needs = [{type = "local", kind = "spec", path = "docs/specs/m2-place-bet/spec.md"}]},
  {path = "docs/product/intents/opp-assessment-pe-pack.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/rfc/0064-ini-001-ai-native-ecosystem.md", revision = "sha256-bytes-v1:26693aa8f9a49eba83258db06be0795b664cb2c09d4c0742ed9b502666d8efca"}, summary = "Reassess the current product-engineering opportunity against shipped Platform Core evidence", needs = [{type = "local", kind = "spec", path = "docs/specs/m2-frame-situation/spec.md"}]},
  {path = "docs/product/intents/capability-map-ini-002.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/rfc/0064-ini-001-ai-native-ecosystem.md", revision = "sha256-bytes-v1:26693aa8f9a49eba83258db06be0795b664cb2c09d4c0742ed9b502666d8efca"}, summary = "Map shipped and remaining Platform Core capabilities against current initiative ownership", needs = [{type = "local", kind = "spec", path = "docs/specs/m2-map-capabilities/spec.md"}]},
  {path = "docs/product/intents/nontechnical-pack-first-value-rollout.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/rfc/0064-ini-001-ai-native-ecosystem.md", revision = "sha256-bytes-v1:26693aa8f9a49eba83258db06be0795b664cb2c09d4c0742ed9b502666d8efca"}, summary = "Shape independently shippable first-value adoption slices for the remaining Level B packs", needs = [{type = "local", kind = "spec", path = "docs/specs/portfolio-first-run-pilot-architect/spec.md"}, {type = "local", kind = "spec", path = "docs/specs/portfolio-first-run-pilot-figma/spec.md"}]},
  {path = "docs/product/design/portfolio-outcome-entry-surfaces.md", kind = "design", source = {mode = "repo-origin", ref = "docs/rfc/0064-ini-001-ai-native-ecosystem.md", revision = "sha256-bytes-v1:26693aa8f9a49eba83258db06be0795b664cb2c09d4c0742ed9b502666d8efca"}, summary = "Design outcome-led portfolio entry surfaces without duplicating renderer or site-system ownership", needs = [{type = "local", kind = "spec", path = "docs/specs/portfolio-pack-first-value-contract/spec.md"}, {type = "local", kind = "spec", path = "docs/specs/portfolio-first-run-pilot-architect/spec.md"}, {type = "local", kind = "spec", path = "docs/specs/portfolio-first-run-pilot-figma/spec.md"}]},
  {path = "docs/product/intents/project-knowledge-engineering-operational-integrations.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/rfc/0077-distill-knowledge.md", revision = "sha256-bytes-v1:94de8754c752838d99562a6b5bbcfbaf64ed688072d0e26149063f5fbac81016"}, summary = "Shape project-knowledge handoffs for engineering and operational workflows", needs = [{type = "local", kind = "spec", path = "docs/specs/project-knowledge-research-integrations/spec.md"}]},
  {path = "docs/product/intents/project-knowledge-adoption-closeout.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/rfc/0077-distill-knowledge.md", revision = "sha256-bytes-v1:94de8754c752838d99562a6b5bbcfbaf64ed688072d0e26149063f5fbac81016"}, summary = "Shape the evidence and decision contract for portable project-knowledge adoption closeout", needs = [{type = "local", kind = "intent", path = "docs/product/intents/project-knowledge-engineering-operational-integrations.md"}]},
  {path = "docs/product/intents/project-knowledge-measured-follow-ons.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/rfc/0077-distill-knowledge.md", revision = "sha256-bytes-v1:94de8754c752838d99562a6b5bbcfbaf64ed688072d0e26149063f5fbac81016"}, summary = "Use adoption-closeout evidence to decide whether any of four conditional project-knowledge extensions should activate", needs = [{type = "local", kind = "intent", path = "docs/product/intents/project-knowledge-adoption-closeout.md"}]},
]

["ini-002".brief_queue]
# `executing` is a lifecycle LIST, like every other collection. A bare inline
# table here is not coerced: the engine accepts only a str or a list, so a table
# raises invalid_workspace, which then stamps every canonical entry in the file.
executing = [
  # Ready 2026-09-03. Draft -> Ready after a revision-bound clean shaping review
  # (three finding rounds, then a scoped verification pass) and the owner's
  # explicit confirmation. Slice U1 was confirmed separately the same day and is
  # registered in this initiative's work.queue; the brief stays Ready until that
  # spec reaches Implementing, at which point it becomes Executing.
  {path = "docs/product/briefs/universal-implementer-dispatch.md", kind = "brief", source = {mode = "repo-origin"}, summary = "Route every spec-backed plan task through the implementer agent sequentially and move implementation logic out of work-loop's SKILL.md, creating the dispatch envelope policy delivery needs", needs = []},
  # Executing 2026-09-03. Ready -> Executing when phase-policy-registry-and-selector
  # reached Implementing; a brief with an Implementing child is Executing by the
  # author-delivery-brief lifecycle. Draft -> Ready earlier the same day after a
  # revision-bound clean shaping review (ten findings, four blocking, then two
  # verification passes) and the owner's explicit confirmation. The gating
  # amendment the sibling recorded as owed is discharged in the same change: D3
  # gates on U1 and D2 on S1, at slice granularity. D2 and D3 stay unconfirmed.
  {path = "docs/product/briefs/phase-scoped-policy-delivery.md", kind = "brief", source = {mode = "repo-origin"}, summary = "Key policy-family selection to the recorded FSM phase and inline the matching families into the acting agent's brief, extending the existing module-index mechanism", needs = [{type = "local", kind = "brief", path = "docs/product/briefs/universal-implementer-dispatch.md"}]},
  {path = "docs/product/briefs/distribution-routes-programme.md", kind = "brief", source = {mode = "repo-origin", ref = "docs/rfc/0092-first-class-distribution-routes.md", revision = "b9f8523016bd800001347c6a8bfaeae88c006a70"}, summary = "Land the distribution-route layer accepted by RFC-0092", needs = []},
]
ready     = []
draft     = [
  # Logged to shape 2026-09-03 out of universal-implementer-dispatch U1 delivery:
  # eight structural facts were learned by failing a gate. Derived-vs-accreted is
  # the unmade decision; the owner ruled out a hand-maintained index.
  {path = "docs/product/briefs/internal-repo-topology.md", kind = "brief", source = {mode = "repo-origin", ref = "docs/product/briefs/universal-implementer-dispatch.md", revision = "sha256-bytes-v1:da5dba1805619b36228067d456e1be6a96b0b89975536f9c0fe4a2fb2e62e698"}, summary = "Give agents this repository's structural topology up front -- generated-vs-source, content pins, published trees, bare-identifier fields -- derived or accreted rather than hand-maintained", needs = []},
  # Split 2026-09-02 at 5ff0d3b19. One brief became three. The predecessor had
  # outgrown its shape by accumulating scope rather than prose: it went from
  # (rubric + pressure test + survey + measurement) to (+ two more readiness
  # boundaries + review sequencing + a second worker + sizing discipline).
  # Its own third finding says a readiness
  # gate that checks presence cannot tell whether the next stage can work -- and
  # the predecessor was the exhibit, because its first slice was unwritable
  # while seven review rounds passed it.
  # The seam, decided by the owner and preserved here: M gates both siblings, so
  # burying it in A would have blocked B on A's first slice. A owns sizing,
  # because the abandoned contract sat above the 96th percentile on ACs and
  # within 3% of the longest spec ever written here, and consumed eleven rounds
  # without converging -- sizing is a lever on A's outcome. B consumes the
  # sizing signal as a stall and gets no copy of the regenerator. A retains the
  # findings corpus; M and B cite it rather than duplicating it.
  # All three stay Draft. Ready needs a revision-bound clean shaping review plus
  # the owner's explicit confirmation, per brief.
  #
  # M -- the activation measurement, appetited first and alone (owner decision,
  # 5ff0d3b19). It gates A entirely and B's routing-ask half only. Separate
  # because burying it in either sibling makes the other wait on that sibling's
  # first slice for a report it does not otherwise depend on.
  # Estimand widened to PORTABLE by owner decision 2026-09-02, after a headless
  # Codex investigation (session 01a0651e-e82a-70b2-846f-8607b8268351) surveyed
  # the corpus options. The report's own recommendation was the narrower local
  # set; the owner took its stated falsifier instead -- the claim is now "does
  # this mechanism work across adopter repositories", not only here.
  # Consequences, all recorded in the brief: two strata that the report may
  # never blend, because all three sibling verdicts are LOCAL and a blended
  # score resolves none of them; a six-rule local floor M1 may not silently
  # drop; and a fourth slice for the external stratum whose enlarged appetite is
  # NOT yet confirmed. The local stratum alone unblocks A and B, so M4 can slip
  # or be dropped without stranding either sibling.
  # External-only and synthetic-only corpora were ruled nonviable, not on
  # principle but on decidability: neither can return a verdict for this repo's
  # anchoring rule, step 5a, or routing prose. Synthetic material enters as
  # seeded TASKS and known-compliant/known-violating artifacts, never as rules.
  # Corpus needs its own home: the harness's unit is a skill (all 96 eval homes
  # under packs/*/.apm/skills/*/evals/, and pack_evals.py hard-codes both the
  # coverage unit and that path), while half the local floor is root-context.
  # Fabricating skills to host them would change the context being measured.
  # Four proposed slices, none confirmed: corpus contract, corpus surface +
  # runner mode, local run and report, external stratum. The decision rule is
  # pre-registered on purpose -- M1 lands as its own reviewed change before M2
  # runs anything, because the Phase 3 harness validates its grader live while
  # that slice is built, so a co-located decision rule could be tuned to it.
  # Capability of the cross-adapter-behavior-enforcement intent. Measured: the
  # `--adapter` flag and the codex *judge* backend already ship, so only a
  # detector that drives a non-Claude model is missing.
  # Capabilities of the cross-adapter-behavior-enforcement intent, authored
  # 2026-09-03. Ordered by what unblocks what: the two dispatch envelopes come
  # first because policy has nowhere to arrive without them, then delivery, then
  # the validator that makes arrival provable.
  # universal-implementer-dispatch moved to `ready` 2026-09-03; the three briefs
  # below still `needs` it, and a Ready predecessor satisfies that edge.
  {path = "docs/product/briefs/spec-author-agent.md", kind = "brief", source = {mode = "repo-origin"}, summary = "Give spec authoring a dispatched agent so the SPEC-PLAN phases have an envelope, reusing the implementer's delivery and enforcement path", needs = [{type = "local", kind = "brief", path = "docs/product/briefs/universal-implementer-dispatch.md"}]},
  # phase-scoped-policy-delivery moved to `executing` 2026-09-03; policy-arrival-
  # validator below still `needs` it, and an open predecessor satisfies that edge.
  {path = "docs/product/briefs/policy-arrival-validator.md", kind = "brief", source = {mode = "repo-origin"}, summary = "Make policy delivery provable: a deterministic check that every phase-selected family has a verdict, with a precise-family compliance branch that can block", needs = [{type = "local", kind = "brief", path = "docs/product/briefs/phase-scoped-policy-delivery.md"}]},
  #
  {path = "docs/product/briefs/multi-adapter-eval-runner.md", kind = "brief", source = {mode = "repo-origin"}, summary = "Add a Codex detector so activation can be measured on a non-Claude model, keeping the default adapter and the scheduled workflow unchanged", needs = []},
  #
  {path = "docs/product/briefs/guidance-activation-measurement.md", kind = "brief", source = {mode = "repo-origin"}, summary = "Measure whether the written-guidance mechanism binds -- graded on what an author produced while a rule was in context rather than on whether it was loaded -- across a mandatory local stratum of six named rules and a portability stratum of pinned adopter repositories, reported separately and never blended", needs = [{type = "local", kind = "brief", path = "docs/product/briefs/policy-arrival-validator.md"}]},
  #
  # A -- keeps the predecessor's slug and file. Two artifacts outside this
  # split's scope link to it by path (work-loop-next-action.md and the
  # agent-loop-escalation-recovery intent), and neither was in scope to edit, so
  # deleting the file would have broken both. Residual: the intent's sentence
  # "the activation analysis lives there" now points one hop short -- the
  # analysis moved to M, which A links in its first section.
  # `needs` carries the M edge because A genuinely waits on M's report. This is
  # sequencing, unlike the edge removed from agent-loop-escalation-recovery,
  # where the relationship was shared evidence.
  # Five proposed slices, none confirmed: A1 rubric + instructions, A2 sizing
  # regenerator, A3 delegation anchor, A4 step-5a widening, A5 ownership survey
  # (conditional). Owed at slice confirmation, carried forward from the
  # predecessor's round-3 verdict of "does not block Ready":
  #   1. The "ran and did not work" assumption is now a Risks bullet, but must
  #      be answered before A5 is confirmed.
  #   2. The guide shippability test (CONVENTIONS.md "A phase whose tooling
  #      ships without its guide is not a complete slice") is unapplied. No
  #      slice names a guides/ artifact yet.
  #   3. A4 reuses step 5a's POSITION; B's routing spike reuses its SHAPE. 5a is
  #      a numbered step inside new-spec (SKILL.md:475, between steps 5 and 6)
  #      and the spike fires before new-spec is entered, so the two cannot be
  #      the same invocation. Killing A4 leaves B's spike intact. Keep the two
  #      consistent in shape when either is specified; never cut them as one
  #      slice. B now owns the full statement of this; A points at it.
  {path = "docs/product/briefs/agent-authoring-input-quality.md", kind = "brief", source = {mode = "repo-origin"}, summary = "Author specs and plans that a review loop converges on: a failure-point rubric and the authoring instructions derived from it, a delegation anchor, sizing discipline shipped as a regenerator, the step-5a satisfiability widening, and a conditional ownership survey", needs = [{type = "local", kind = "brief", path = "docs/product/briefs/guidance-activation-measurement.md"}]},
  #
  # B -- `needs` is deliberately empty. B1, the declared-shape gate, never
  # depended on the activation measurement and can start in parallel with M
  # (owner decision); a needs edge would mark the whole brief undispatchable and
  # hide that. Only B2, the routing ask, waits on M's report, and the brief
  # records that per slice. Five proposed slices, none confirmed: B1 gate, B2
  # routing ask + spike-or-redirect disposition, B3 new-spec input readiness,
  # B4 spec-and-plan-to-implementer readiness, B5 review sequencing.
  # B3 and B4 are split although the owner's cut named them together: same
  # question, two owning surfaces, and a two-file change resolves at roughly
  # half the rate of a one-file change. Recorded as a sizing judgment, not a
  # scope change.
  # B3 and B4 carry a prose component whose activation is unmeasured. The owner
  # gated only the routing ask on M's report, so these proceed without it; the
  # brief records that as a risk rather than re-gating them.
  # The non-goal on reviewers is narrowed by owner amendment: sequencing a
  # readiness gate before review is in scope; the review lens and the
  # adjudicator's contract stay out.
  {path = "docs/product/briefs/stage-input-readiness.md", kind = "brief", source = {mode = "repo-origin"}, summary = "Make each authoring handoff ask whether the next stage can work from its input and record the answer: a pre-creation pressure test with a redirect to brief authoring, a bounded-spike disposition for an open unknown, readiness checks at new-spec's input and at the spec-and-plan handoff, and a readiness gate sequenced ahead of review", needs = []},
  # Authored 2026-09-01 by `author-delivery-brief create` after the
  # work-loop-next-projection spec was abandoned at e1bdde746, ten pre-EXECUTE
  # review rounds in, on a model error no clause repair could reach. Draft only:
  # appetite and the slice cut are undecided, and two bounded observations are
  # owed before the slices that depend on them -- the brief records those two as
  # documentation, not unknowns. `continue` owns Ready after the owner closes
  # the two open decisions.
  {path = "docs/product/briefs/work-loop-next-action.md", kind = "brief", source = {mode = "repo-origin"}, summary = "Give a work-loop turn one authoritative next action from persisted state, and decide separately what owns stopping a non-converging authoring loop", needs = []},
]
# tech-site-completion is Status: Shipped, so `ready` was an impossible
# transition. It moves to the engine's terminal brief collection rather than
# leaving the lifecycle: nine shipped specs below still name this brief as both
# `ref` and `parent`, and dropping it would orphan those references.
shipped   = [
  {path = "docs/product/briefs/tech-site-completion.md", kind = "brief", source = {mode = "repo-origin", ref = "workspace.toml", revision = "7da8b07571e44fe5d6052f0efca7952484e173c8"}, summary = "Make the public technology sites reviewably complete", needs = []},
]

["ini-002".work]
active  = []
shipped = [
  {path = "docs/specs/distribution-route-registry/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "docs/product/briefs/distribution-routes-programme.md", revision = "sha256-bytes-v1:b8daba8a937964f1cc37233cd613cdc8993d53bdcf64bc580ea7799e4741829d", parent = "docs/product/briefs/distribution-routes-programme.md"}, summary = "Build all three routes through one generic six-field registry with no route-name branches in shared code", needs = [{type = "local", kind = "spec", path = "docs/specs/portable-agent-plugin-projection/spec.md"}]},
  # U1 of universal-implementer-dispatch, confirmed 2026-09-03. Registered at
  # spec-authoring time by owner decision rather than at work-loop dispatch.
  # Slice D1 of phase-scoped-policy-delivery, shipped 2026-09-04. Registered
  # here because a brief's child scope is derived from its registered spec
  # memberships: without this entry the brief sits in `executing` with no
  # execution evidence, which the canonical reconciliation reports as an
  # impossible_transition. `reconcile` does not catch it; the roster's
  # fail-closed lifecycle test does.
  {path = "docs/specs/phase-policy-registry-and-selector/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "docs/product/briefs/phase-scoped-policy-delivery.md", revision = "sha256-bytes-v1:a737f4f9dda60bd2bc611217146fecd763e372893d5d0e6af24c55c0d5554dc3", parent = "docs/product/briefs/phase-scoped-policy-delivery.md"}, summary = "Declare which policy families each work-loop phase teaches and turn one selection key into the ordered families it selects, with each family's tier, its teaching-text locator and a confined digest of the file that carries the rule", needs = []},
  {path = "docs/specs/sequential-implementer-dispatch/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "docs/product/briefs/universal-implementer-dispatch.md", revision = "sha256-bytes-v1:9aaed0b51aafb824851b8adf47973188fa3a4ceb996367de0e2a57a96e018b28", parent = "docs/product/briefs/universal-implementer-dispatch.md"}, summary = "Route every spec-backed CODE-IMPLEMENTATION plan task through the implementer agent sequentially, admit the primary working tree as an execution root, and remove the four surfaces that deny it", needs = []},
  {path = "docs/specs/status-projection-and-context-exclusion/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "docs/rfc/0096-portable-delivery-artifact-lifecycle.md", revision = "20c0ba50ebad22651694bc6f43715ee0df1a3938"}, summary = "Project closeout state, due reviews and exceptions, and exclude cooled artifacts from ordinary orientation", needs = [{type = "local", kind = "spec", path = "docs/specs/close-work-extraction-and-immediate-disposition/spec.md"}, {type = "local", kind = "spec", path = "docs/specs/thirty-day-cooling-and-retirement/spec.md"}]},
  {path = "docs/specs/rfc0099-migration-validation-record/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "docs/rfc/0099-cut-before-adding-and-artifact-shaping.md"}, summary = "Waive RFC-0099's unrun adopter study, close activation and shaping-review fixture gaps, and publish the maintained seven-family fixture register", needs = []},
  {path = "docs/specs/rfc-architecture-simplification/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "docs/rfc/0099-cut-before-adding-and-artifact-shaping.md", revision = "sha256-bytes-v1:a4a9a7d51de1c6ae7a434154e4dd17fa46d74693c60bc35af6a08341aa0965c4"}, summary = "Pre-write RFC artifact choice, adversarial RFC YAGNI review, architecture reuse and Stage-0 stopping, and architecture-specific YAGNI review without a new reviewer", needs = []},
  {path = "docs/specs/cooling-untrusted-input-refusals/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "docs/rfc/0096-portable-delivery-artifact-lifecycle.md", revision = "6e984d67b583b36798efddbb2717ce5784572a49"}, summary = "Refuse malformed cooling-record input with a named code instead of raising", needs = [{type = "local", kind = "spec", path = "docs/specs/thirty-day-cooling-and-retirement/spec.md"}]},
  {path = "docs/specs/thirty-day-cooling-and-retirement/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "docs/rfc/0096-portable-delivery-artifact-lifecycle.md", revision = "6e984d67b583b36798efddbb2717ce5784572a49"}, summary = "Compute the exact 30-day review date, persist the cooling record, and review it without auto-deleting", needs = []},
  {path = "docs/specs/close-work-extraction-and-immediate-disposition/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "docs/rfc/0096-portable-delivery-artifact-lifecycle.md", revision = "6e984d67b583b36798efddbb2717ce5784572a49"}, summary = "Portable close-work closeout with separately confirmed immediate disposition", needs = []},
  {path = "docs/specs/local-pytest-process-optimization/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "conversation:2026-08-25", revision = "f871fe506053ea17dd1702ccea37c802b78de557"}, summary = "Reduce pytest startup and repeated collection while preserving exact test semantics and proven isolation", needs = []},
  # RFC-0092 Phase 1A — shipped after Phase 0 established the route contract;
  # it supplied the third real route, which is what gates the Phase 2 registry
  # extraction that now follows it.
  {path = "docs/specs/portable-agent-plugin-projection/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "docs/product/briefs/distribution-routes-programme.md", revision = "sha256-bytes-v1:329d3aec010ffd0f0b090022bac7faf35b85f337d9b3c719ab8063b7c74dbc45", parent = "docs/product/briefs/distribution-routes-programme.md"}, summary = "Project portable-eligible canonical skills into deterministic Agent Plugins 1.0.0 packages", needs = [{type = "local", kind = "spec", path = "docs/specs/distribution-route-contract/spec.md"}]},
  {path = "docs/specs/distribution-route-contract/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "docs/product/briefs/distribution-routes-programme.md", revision = "sha256-bytes-v1:329d3aec010ffd0f0b090022bac7faf35b85f337d9b3c719ab8063b7c74dbc45", parent = "docs/product/briefs/distribution-routes-programme.md"}, summary = "Separate distribution-route ownership from runtime adapters without changing APM or Claude package bytes", needs = []},
  {path = "docs/specs/agents-md-lost-obligations/spec.md", kind = "spec", source = {mode = "repo-origin", revision = "0e1c1b04b0f7b46d9edaee319f6f194569816709"}, summary = "Restore AGENTS.md obligations lost during instruction-surface simplification", needs = []},
  {path = "docs/specs/rfc0088-round10-measurement/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "docs/rfc/0088-web-pilot-foundation.md", revision = "a02389adad6033ccd684969db66418b25a238d52"}, summary = "Measure the four bounded RFC-0088 round-10 residuals", needs = []},
  {path = "docs/specs/local-ci-shared-test-deduplication/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "conversation:2026-08-25"}, summary = "Execute five build-check-owned shared test files only once within one composed make ci", needs = []},
  {path = "docs/specs/review-finding-adjudication/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "conversation:2026-08-23", revision = "sha256-bytes-v1:6aee06c1ae0093929cf70d3088ce8db7ac05c128b2eea8cb2fd2f2d098af621b"}, summary = "Adjudicate every reviewer finding before it enters the main loop's decision or repair context", needs = []},
  {path = "docs/specs/adjudicator-evidence-and-remedy-predicate/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "conversation:2026-08-24"}, summary = "Bound machine-checkable adjudication evidence retries and distinguish wrong remedy mechanisms", needs = []},
  {path = "docs/specs/rfc0088-round12-consumer-shaped-residuals/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "Measure RFC-0088 open questions 4-6", needs = []},
  {path = "docs/specs/rfc0088-round14-destination-adapter-contract/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "Amend RFC-0088 open question 3 to a destination adapter contract and measure the fixture pair", needs = []},
  {path = "docs/specs/rfc0088-round15-reference-consumer/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "Run the reference-consumer observation on both admitted channels and record the per-group split-cost measurement result", needs = []},
  {path = "docs/specs/m6-astro-work-index/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "Give PMs a static read-only view of canonical workspace status", needs = []},
  {path = "docs/specs/m6-enterprise-rollout-playbook/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "Guide champions through pilot, wave, and organization-wide adoption", needs = []},
  {path = "docs/specs/project-knowledge-foundation/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "Establish safe portable project knowledge capture, distillation, and enquiry", needs = []},
  {path = "docs/specs/project-knowledge-authoring-integrations/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "Integrate project knowledge at authoring semantic gates", needs = [{type = "local", kind = "spec", path = "docs/specs/project-knowledge-foundation/spec.md"}]},
  {path = "docs/specs/project-knowledge-review-integrations/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "Integrate bounded project knowledge enquiry with review workflows", needs = [{type = "local", kind = "spec", path = "docs/specs/project-knowledge-authoring-integrations/spec.md"}]},
  {path = "docs/specs/project-knowledge-research-integrations/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "Integrate project knowledge with independently verified research workflows", needs = [{type = "local", kind = "spec", path = "docs/specs/project-knowledge-review-integrations/spec.md"}]},
  {path = "docs/specs/claude-plugin-hook-parity/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "Compile pack-authored hooks onto the Claude-plugin route behind a publisher-only branch control", needs = []},
  {path = "docs/specs/engine-export-boundary/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "engine export boundary", needs = []},
  {path = "docs/specs/guide-typed-asides-conversion/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "Convert load-bearing guide blockquotes to typed Starlight asides while preserving genuine quotations", needs = []},
  {path = "docs/specs/product-documentation-pack/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "product-documentation pack (Phase 1)", needs = []},
  {path = "docs/specs/agentbundle-first-value-handoff/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "agentbundle-first-value-handoff", needs = []},
  {path = "docs/specs/catalogue-curation/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "catalogue-curation pack", needs = []},
  {path = "docs/specs/m2-frame-situation/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "m2-frame-situation", needs = []},
  {path = "docs/specs/m1-work-queue/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "m1-work-queue", needs = []},
  {path = "docs/specs/m1-brief-queue/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "m1-brief-queue", needs = []},
  {path = "docs/specs/m1-governance-integration/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "m1-governance-integration", needs = []},
  {path = "docs/specs/spec-A-workspace-status-rename/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "spec-A-workspace-status-rename", needs = []},
  {path = "docs/specs/new-rfc-followon-queue-write/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "new-rfc follow-on queue-write guard", needs = []},
  {path = "docs/specs/queue-add/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "queue-add — bridge session-surfaced items into the work queue", needs = []},
  {path = "docs/specs/work-loop-queue-shipped-fix/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "work-loop queue-to-shipped fix", needs = []},
  {path = "docs/specs/spec-D-pack-workflow-guide/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "spec-D-pack-workflow-guide", needs = []},
  {path = "docs/specs/spec-B-pack-status-skills/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "spec-B-pack-status-skills", needs = []},
  {path = "docs/specs/workspace-status-next-actions/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "workspace-status-next-actions", needs = []},
  {path = "docs/specs/workspace-status-queue-reconciliation/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "workspace-status queue reconciliation", needs = []},
  {path = "docs/specs/m3-backlog-absorption/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "m3-backlog-absorption", needs = []},
  {path = "docs/specs/capture-work/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "capture-work — universal capture-then-triage front-door", needs = []},
  {path = "docs/specs/spec-C-workloop-argless-resume/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "spec-C-workloop-argless-resume", needs = []},
  {path = "docs/specs/workspace-journey-guides-and-planning-doctrine/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "workspace-journey-guides-and-planning-doctrine", needs = []},
  {path = "docs/specs/author-brief-docs/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "author-brief-docs", needs = []},
  {path = "docs/specs/m2-diverge-solutions/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "m2-diverge-solutions", needs = []},
  {path = "docs/specs/m2-lean-canvas-and-initiative-brief/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "m2-lean-canvas-and-initiative-brief", needs = []},
  {path = "docs/specs/m2-place-bet/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "m2-place-bet", needs = []},
  {path = "docs/specs/m2-identify-opportunities/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "m2-identify-opportunities", needs = []},
  {path = "docs/specs/m2-map-capabilities/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "m2-map-capabilities", needs = []},
  {path = "docs/specs/m2-frame-intent-jtbd/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "m2-frame-intent-jtbd", needs = []},
  {path = "docs/specs/m5-jira-align-brief-intake/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "m5-jira-align-brief-intake", needs = []},
  {path = "docs/specs/m5-github-brief-intake/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "m5-github-brief-intake", needs = []},
  {path = "docs/specs/portfolio-pack-first-value-contract/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "portfolio-pack-first-value-contract", needs = []},
  {path = "docs/specs/m6-role-journey-guides/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "m6-role-journey-guides", needs = []},
  {path = "docs/specs/m5-linear-brief-intake-and-sync/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "m5-linear-brief-intake-and-sync", needs = []},
  {path = "docs/specs/portfolio-first-run-pilot-architect/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "portfolio-first-run-pilot-architect", needs = []},
  {path = "docs/specs/portfolio-first-run-pilot-figma/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "portfolio-first-run-pilot-figma", needs = []},
  {path = "docs/specs/m5-tracker-guides/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "m5-tracker-guides", needs = []},
  {path = "docs/specs/new-guide-conversation-first/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "new-guide conversation-first", needs = []},
  {path = "docs/specs/site-ui-primitives/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "site-ui-primitives", needs = []},
  {path = "docs/specs/atlassian-sso-cookie-selector-integration-test/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "atlassian-sso-cookie-selector-integration-test", needs = []},
  {path = "docs/specs/credbroker-security-hardening/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "credbroker-security-hardening", needs = []},
  {path = "docs/specs/flaky-sso-store-concurrency-on-windows/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "Flaky SSO store concurrency on Windows", needs = []},
  {path = "docs/specs/site-design-system-spec/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "site-design-system-spec", needs = []},
  {path = "docs/specs/jira-check-sso-auto-login/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "jira-check-sso-auto-login", needs = []},
  {path = "docs/specs/confluence-crawler-check-auto-login/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "Confluence crawler check auto-login", needs = []},
  {path = "docs/specs/catalogue-test-carve-out/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "catalogue test carve-out", needs = []},
  {path = "docs/specs/workspace-status-simplification-order-2b/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "workspace-status simplification — Order 2B", needs = []},
  {path = "docs/specs/workspace-mcp/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "workspace-mcp — Stage 1 implementation", needs = []},
  {path = "docs/specs/m6-live-demo-guide/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "Run a peer-led shaping-to-spec demonstration on the adopting organization's repository", needs = []},
  {path = "docs/specs/guide-title-clarity/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "docs/product/briefs/tech-site-completion.md", revision = "7da8b07571e44fe5d6052f0efca7952484e173c8", parent = "docs/product/briefs/tech-site-completion.md"}, summary = "Apply the four approved guide-title wording changes", needs = []},
  {path = "docs/specs/docs-site-build-contract-hardening/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "docs/product/briefs/tech-site-completion.md", revision = "7da8b07571e44fe5d6052f0efca7952484e173c8", parent = "docs/product/briefs/tech-site-completion.md"}, summary = "Harden docs rendering with site-local cleanup and built-in plugin tests", needs = [{type = "local", kind = "spec", path = "docs/specs/site-ci-contract-closure/spec.md"}]},
  {path = "docs/specs/site-ci-contract-closure/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "docs/product/briefs/tech-site-completion.md", revision = "7da8b07571e44fe5d6052f0efca7952484e173c8", parent = "docs/product/briefs/tech-site-completion.md"}, summary = "Require the existing site construction tests and docs contrast checker in CI", needs = []},
  {path = "docs/specs/site-contract-provenance-cleanup/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "docs/product/briefs/tech-site-completion.md", revision = "7da8b07571e44fe5d6052f0efca7952484e173c8", parent = "docs/product/briefs/tech-site-completion.md"}, summary = "Reconcile frozen site decisions, living guidance, and legacy provenance", needs = []},
  {path = "docs/specs/site-now-surface/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "docs/product/briefs/tech-site-completion.md", revision = "7da8b07571e44fe5d6052f0efca7952484e173c8", parent = "docs/product/briefs/tech-site-completion.md"}, summary = "Publish released changelog highlights at Now without exposing in-progress work", needs = []},
{path = "docs/specs/guide-metadata-completion/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "docs/product/briefs/tech-site-completion.md", revision = "7da8b07571e44fe5d6052f0efca7952484e173c8", parent = "docs/product/briefs/tech-site-completion.md"}, summary = "Complete reviewed metadata for every rendered public guide", needs = [{type = "local", kind = "spec", path = "docs/specs/guide-title-clarity/spec.md"}]},
  {path = "docs/specs/docs-site-print-chrome-suppression/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "Suppress the docs footer navigation link groups in print", needs = []},
  {path = "docs/specs/site-browser-quality-gate/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "docs/product/briefs/tech-site-completion.md", revision = "7da8b07571e44fe5d6052f0efca7952484e173c8", parent = "docs/product/briefs/tech-site-completion.md"}, summary = "Gate emitted-site responsiveness, accessibility, and navigation behavior", needs = []},
  {path = "docs/specs/work-loop-in-process-guards/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "Run every work-loop transition guard in the engine's own process", needs = []},
  {path = "docs/specs/ci-gate-parallelization/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "Extract the SAST and export-boundary legs from build-check.yml into their own jobs behind a name-preserving aggregator, and split Gate A; targets in AC11", needs = []},
  {path = "docs/specs/ci-gate-credbroker/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "Extract the credbroker suite from gate-main into its own gate-credbroker job", needs = []},

  {path = "docs/specs/journey-page-completion/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "docs/product/briefs/tech-site-completion.md", revision = "7da8b07571e44fe5d6052f0efca7952484e173c8", parent = "docs/product/briefs/tech-site-completion.md"}, summary = "Complete priority journey evidence, orientation, and gate navigation", needs = []},
  {path = "docs/specs/site-shared-chrome/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "docs/product/briefs/tech-site-completion.md", revision = "7da8b07571e44fe5d6052f0efca7952484e173c8", parent = "docs/product/briefs/tech-site-completion.md"}, summary = "Share destination architecture while retaining renderer-local chrome", needs = []},
  {path = "docs/specs/local-ci-orchestration/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "Remove duplicate local CI orchestration and resolve Make's default Python once", needs = []},
  {path = "docs/specs/architecture-decision-surface-portability/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "docs/rfc/0096-portable-delivery-artifact-lifecycle.md", revision = "d5e68b6969bf601d06479a8ac14613bc9693d8fc"}, summary = "Resolve design, current architecture, and decision-record destinations without changing their methods", needs = []},
  {path = "docs/specs/shaping-intake-handoff/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "docs/rfc/0096-portable-delivery-artifact-lifecycle.md", revision = "f7660b00820dc06cf7f4618e0b6479d135e5c726"}, summary = "Reuse optional shaping and external work through portable core intake routing", needs = []},
  {path = "docs/specs/core-guidance-artifact-routing/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "docs/rfc/0099-cut-before-adding-and-artifact-shaping.md", revision = "sha256-bytes-v1:6d7e5dad22929f72ccdbcc3d168a852e0fabdd433b041872abbaeb4e67189ae4"}, summary = "Install the canonical cut-before-adding ladder and simplify Core intent and delivery-brief routing", needs = []},
  {path = "docs/specs/dependency-scoped-completion-receipts/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "docs/rfc/0096-portable-delivery-artifact-lifecycle.md"}, summary = "Project the dependency-scoped four-field completion receipt from the citing local need, so a dependant on a closed-out and pruned delivery resolves instead of reporting missing_dependency", needs = [{type = "local", kind = "spec", path = "docs/specs/close-work-extraction-and-immediate-disposition/spec.md"}, {type = "local", kind = "spec", path = "docs/specs/status-projection-and-context-exclusion/spec.md"}]},
  {path = "docs/specs/reclassified-lifecycle-result/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "docs/rfc/0096-portable-delivery-artifact-lifecycle.md"}, summary = "Carry Reclassified in the lifecycle record by pairing it with retain-exception, reachable only from a retained record on a supplied acceptance and terminal thereafter", needs = []},
  {path = "docs/specs/cooling-brief-child-scope-closure/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "docs/rfc/0096-portable-delivery-artifact-lifecycle.md"}, summary = "Give a cooled child a parent link readable without opening its body: source.parent answers three ways, and an absent or unresolvable value is unknown scope that names the entry and fails closed on every brief dependency", needs = []},
  {path = "docs/specs/guide-callout-inventory/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "Derive guide callout rendering expectations from source and validate the frozen ledger against itself", needs = []},
]
queue   = [
]

# ─────────────────────────────────────────────────────────────────────────────
# ini-003 · Digital Experience Doctrine
#
# Cross-pack doctrine update across experience-design, product-engineering,
# product-strategy, and core (frontend-engineering). Mission: change the
# definition of done so digital work is complete only when it produces an
# observable outcome, not merely a valid artifact or green build.
#
# ─────────────────────────────────────────────────────────────────────────────
["ini-003"]
name      = "Digital Experience Doctrine"
status    = "active"
milestone = "M2 · Adoption + Shaping Doctrine"

["ini-003".work]
active  = []
queue   = [
]
shipped = [
  {path = "docs/specs/shaping-review-contracts/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "docs/rfc/0099-cut-before-adding-and-artifact-shaping.md", revision = "sha256-bytes-v1:6d7e5dad22929f72ccdbcc3d168a852e0fabdd433b041872abbaeb4e67189ae4"}, summary = "Cold-review intent, delivery-brief, and spec contracts without adding shaping-loop state", needs = [{type = "local", kind = "spec", path = "docs/specs/core-guidance-artifact-routing/spec.md"}]},
  {path = "docs/specs/digital-experience-contract/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "digital-experience-contract", needs = []},
  {path = "docs/specs/xd-copy-direction/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "xd-copy-direction", needs = []},
  {path = "docs/specs/frontend-engineering-doctrine-update/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "frontend-engineering-doctrine-update", needs = []},
  {path = "docs/specs/xd-skill-boundaries/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "xd-skill-boundaries", needs = []},
]
["ini-003".brief_queue]
executing = []
ready     = []
draft     = [
  {path = "docs/product/briefs/digital-experience-doctrine-completion.md", kind = "brief", source = {mode = "repo-origin", ref = "docs/rfc/0071-digital-experience-doctrine.md", revision = "a03b9d3f8df15a9b88cdabda5c10f21c662bfd0f"}, summary = "Frame the remaining Digital Experience Doctrine outcomes as one Draft delivery brief", needs = []},
]

["ini-003".shaping_queue]
active  = []
backlog = [
  {path = "docs/product/intents/product-strategy-adoption-doctrine.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/rfc/0071-digital-experience-doctrine.md", revision = "a03b9d3f8df15a9b88cdabda5c10f21c662bfd0f"}, summary = "Make product strategy carry an adoption hypothesis, causal metrics, and experience handoff", needs = [{type = "local", kind = "spec", path = "docs/specs/digital-experience-contract/spec.md"}]},
  {path = "docs/product/intents/product-engineering-shaping-doctrine.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/rfc/0071-digital-experience-doctrine.md", revision = "a03b9d3f8df15a9b88cdabda5c10f21c662bfd0f"}, summary = "Make product-engineering bets thin, observable, evidence-led, and learning-oriented", needs = [{type = "local", kind = "spec", path = "docs/specs/digital-experience-contract/spec.md"}]},
  {path = "docs/product/intents/xd-design-system-foundations.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/rfc/0071-digital-experience-doctrine.md", revision = "a03b9d3f8df15a9b88cdabda5c10f21c662bfd0f"}, summary = "Establish a working design-system foundation after XD boundaries and the experience contract are settled", needs = [{type = "local", kind = "spec", path = "docs/specs/digital-experience-contract/spec.md"}, {type = "local", kind = "spec", path = "docs/specs/xd-skill-boundaries/spec.md"}]},
  {path = "docs/product/intents/xd-ia-archetypes-objects.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/rfc/0071-digital-experience-doctrine.md", revision = "a03b9d3f8df15a9b88cdabda5c10f21c662bfd0f"}, summary = "Give experience design a shared method for page archetypes and product-object mapping", needs = [{type = "local", kind = "spec", path = "docs/specs/digital-experience-contract/spec.md"}, {type = "local", kind = "spec", path = "docs/specs/xd-skill-boundaries/spec.md"}]},
  {path = "docs/product/intents/xd-state-reviewer-doctrine.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/rfc/0071-digital-experience-doctrine.md", revision = "a03b9d3f8df15a9b88cdabda5c10f21c662bfd0f"}, summary = "Evaluate experience states with a reviewer doctrine grounded in information architecture", needs = [{type = "local", kind = "spec", path = "docs/specs/xd-skill-boundaries/spec.md"}, {type = "local", kind = "intent", path = "docs/product/intents/xd-ia-archetypes-objects.md"}]},
  {path = "docs/product/intents/cross-pack-experience-eval.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/rfc/0071-digital-experience-doctrine.md", revision = "a03b9d3f8df15a9b88cdabda5c10f21c662bfd0f"}, summary = "Prove the digital-product workflow through one cross-pack golden-path evaluation", needs = [{type = "local", kind = "intent", path = "docs/product/intents/product-strategy-adoption-doctrine.md"}, {type = "local", kind = "intent", path = "docs/product/intents/product-engineering-shaping-doctrine.md"}, {type = "local", kind = "spec", path = "docs/specs/xd-skill-boundaries/spec.md"}, {type = "local", kind = "intent", path = "docs/product/intents/xd-design-system-foundations.md"}, {type = "local", kind = "intent", path = "docs/product/intents/xd-ia-archetypes-objects.md"}, {type = "local", kind = "intent", path = "docs/product/intents/xd-state-reviewer-doctrine.md"}, {type = "local", kind = "spec", path = "docs/specs/frontend-engineering-doctrine-update/spec.md"}]},
  {path = "docs/product/intents/digital-product-guides-update.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/rfc/0071-digital-experience-doctrine.md", revision = "a03b9d3f8df15a9b88cdabda5c10f21c662bfd0f"}, summary = "Publish integrative guidance for the validated digital-product workflow", needs = [{type = "local", kind = "intent", path = "docs/product/intents/cross-pack-experience-eval.md"}]},
]

# Repo-level backlog — open work not scoped to any active initiative.
# Populated by the m3-backlog-absorption migration (docs/specs/m3-backlog-absorption/).
# New entries take the canonical shape documented at the top of this file:
# {path, kind, source, summary, needs}. `path` must name an artifact that exists —
# for a deferral with no artifact of its own, the document recording it.
# Legacy {slug, needs?, source?} records remain below and are capped by the
# write-side ratchet in tests/roster/test_workspace_status_projection.py; do not
# add new ones.
# Per-entry TOML comments must be cold-start-sufficient: problem, fix, file/skill, unblock.
# When the unblock condition is another tracked item (a hard AND dependency), also
# add a `needs` edge — not prose alone. Disjunctive/untracked/external unblocks stay prose.
# Distinct from [ini-NNN.shaping_queue].backlog (per-initiative non-active tier).
[backlog]
open = [
  # NOT migrated to a `path` entry, and not an oversight: AC16 of the Shipped
  # spec/agent-skill-engineering-consumer-integrations is discharged by this
  # exact `slug`/`source` pair, and tests/roster's
  # test_ac16_provider_absence_follow_on_is_registered pins it. Repointing the
  # record at an intent document would orphan a ticked AC on a frozen spec.
  # The three sibling controls from that security review closed 2026-09-08; see
  # docs/specs/agent-skill-engineering-consumer-integrations/notes/consumer-security-review-closures.md.
  # RFC-0097:189's behavioural half -- "tested without this pack installed" --
  # is not discharged by the consumer-integrations slice. For a prose consumer
  # there is no runtime to exercise, and verify_catalogue over a provider-less
  # catalogue runs a strict subset of the provider-present checks. RFC-0097:575
  # offers a cheaper mode than an executable harness -- a fixture versioned with
  # its expected result before the implementation runs, judged by an independent
  # reviewer -- which that slice did not evaluate.
  {slug = "agent-skill-engineering-provider-absence-behaviour", source = "docs/specs/agent-skill-engineering-consumer-integrations/spec.md#follow-ons", summary = "Discharge the behavioural half of the provider-absence obligation: evaluate a pre-registered fixture-and-expected-result mode, judged by an independent reviewer, against an executable absence harness"},
  # Product-strategy parent of the three capability briefs below it, framed
  # 2026-09-02. Re-gates them: the policy validator is the activation
  # measurement's compliance test, so enforcement precedes measurement.
  # Load-bearing unknown: no host is known to admit a blocking hook verdict.
  {path = "docs/product/intents/cross-adapter-behavior-enforcement.md", kind = "intent", source = {mode = "repo-origin"}, summary = "Make a published behavior control run regardless of model or host by putting it on the pre-execution path rather than in prose: a structured policy primitive an adopter can extend, a blocking control whose per-host contract is established first, and a multi-adapter runner so a non-Claude model can be measured at all", needs = []},
  {path = "docs/specs/cooling-scope-closure/notes/review-findings.md", kind = "defect", source = {mode = "repo-origin"}, summary = "Two of nine adjudicated findings on the withdrawn cooling-scope-closure amendment remain open; the file records each closure with its evidence. Open: a plan whose Done-when requires editing a hash-frozen plan.md, which nothing detects at approval time; and AC31 being unable to fail when the release surfaces agree at a version whose published code differs from what ships. Both need an owner decision rather than a repair", needs = []},
  # Only reproduces on a case-insensitive filesystem, so macOS sees it and a
  # Linux remote does not. The skip itself is correct — the fixture writes
  # `Notes.md` and `notes.md` into one envelope and a case-insensitive host
  # collapses them, so there is no refusal to assert. What is missing is the
  # sdist gate's allowance for it. Introduced 2026-08-31 in 4e270684e, an
  # ancestor of this branch's base 1134701ba, and confirmed by that ancestry
  # rather than by a clean-worktree run.
  # Environmental, not a code defect: nothing in the tree under `web/` changed.
  # Reproduced outside `make` by running `npm audit --json` in `web/` directly,
  # which prints `npm warn audit network timeout at:
  # https://registry.npmjs.org/-/npm/v1/security/advisories/bulk`. The sibling
  # `docs-site/` lockfile audits clean in the same shell, so it is that one
  # bulk-advisory request, not npm or the network as a whole. Seen twice with a
  # host load average of 86-200.
  {slug = "pre-existing-audit-npm-bulk-advisory-timeout", source = "pre-flight/2026-09-03", summary = "tools/audit-npm.py --root . exits 2 with 'npm audit reported an error instead of a report' because the web/ lockfile's bulk advisory request to registry.npmjs.org times out; the tool reports the empty npm error payload rather than distinguishing a network timeout from a real audit error. Give the bulk request a retry with backoff, or classify a timeout separately from an audit failure so a transient registry stall does not read as a SAST gate failure"},
  {slug = "pre-existing-sdist-gate-rejects-case-insensitive-skip", source = "pre-flight/2026-09-03", summary = "tools/test_check_artifact_contents.py::test_real_sdist_carries_and_executes_complete_engine_suite fails on any case-insensitive filesystem: the published suite emits 'SKIPPED tests/unit/test_direct_admission.py:937: case-insensitive filesystem cannot hold both spellings', which is outside the gate's _EXPECTED_SKIP_REASONS allowlist, so a correct platform skip reads as a forbidden one. Add the reason to that allowlist, or make the fixture build a case-distinct pair, so the release gate is runnable on macOS"},
  {path = "docs/specs/dependency-scoped-completion-receipts/notes/follow-ons.md", kind = "defect", source = {mode = "repo-origin"}, summary = "Two of three RFC-0096 receipt follow-ons stay open. Entry-removal precondition: reassigned to rfc0096-wave7c-pruning -- stranding harm refuted by measurement, but nothing makes the file and entry removals atomic and a record field cannot supply that. Cooled cross-repo deferral slug: still Wave 7b's. Reclassified schema gap: closed by docs/specs/reclassified-lifecycle-result/. Both settled in rfc0096-wave7c-lifecycle-record-decisions.md; the linked notes file is frozen, so status lives here", needs = []},
  {path = "docs/specs/loop-cohort-state-lock/notes/reclaim-residual.md", kind = "defect", source = {mode = "repo-origin"}, summary = "A state-lock reclaim's identity check and its rename are two operations, so a holder acquiring between them is displaced and reports StateLockLost having done nothing wrong. Not closable by checking harder -- POSIX has no rename-if-unchanged. Atomic takeover is disproved and recorded: rename replaces unconditionally, so every contender with the same stale snapshot wins in turn, measured at three to five holders. The note carries the forced-interleaving reproduction", needs = []},
  {path = "docs/specs/reclassified-lifecycle-result/notes/follow-ons.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/rfc/0096-portable-delivery-artifact-lifecycle.md"}, summary = "Two RFC-0096 Wave 7c follow-ons. (1) rfc0096-wave7c-pruning: a pruning session must prove its own precondition -- an artifact's file and its workspace entry must be removed together -- and nothing makes the two removals atomic. (2) Moving AC23-pinned files falsifies cooling-scope-closure's AC23 TITLE and its Durable Outputs line \"neither may change\"; the AC body still holds, so the repair is a retitle. Caused by this delivery and Wave 7b; its owner restates it. RFC-0096:380 names the slice", needs = []},
  {path = "docs/specs/cooling-brief-child-scope-closure/notes/follow-ons.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/rfc/0096-portable-delivery-artifact-lifecycle.md"}, summary = "Two RFC-0096 Wave 7b follow-ons. (1) cooled-parent-scope-declaration-writer: a declared empty source.parent on an already-cooled entry is trusted rather than verified, because provenance_mismatch is emitted below the cooled early return; closing it needs a closeout-time writer in close-work. (2) Two test names AC17 pins in test_status_projection_and_context_exclusion.py now contradict the bodies they assert, because closing the residual inverted them; the repair is a rename plus an AC17 amendment", needs = []},
  # Unblocks when: promoted through normal intake into a controlled benchmark.
  {path = "docs/product/intents/code-graph-review-benchmark.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/work-loop-review-verdicts/notes/code-graph-code-review-effectiveness-survey.md", revision = "local-2026-08-23"}, summary = "Benchmark repository-native targeted exploration against graph-assisted code review before adopting graph infrastructure", needs = []},

  # The current cognitive-load slice uses one tool-neutral lookup. This intent
  # owns native adapter projections and their singleton-conflict contract.
  {path = "docs/product/intents/catalogue-rules-primitive.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/cognitive-load-reduction/notes/guidance-pointer-patterns-survey.md", revision = "local-2026-08-27"}, summary = "Add a portable rules primitive with native adapter projections and safe context-file fallbacks", needs = []},

  {path = "docs/product/intents/canonical-mcp-install-parity.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/product/briefs/distribution-routes-programme.md", revision = "sha256-bytes-v1:b8daba8a937964f1cc37233cd613cdc8993d53bdcf64bc580ea7799e4741829d"}, summary = "Add canonical MCP with same-wave direct agentbundle and route projection parity", needs = [{type = "local", kind = "spec", path = "docs/specs/distribution-route-registry/spec.md"}]},
  # Phase 2b — the schema genericization cut from the Phase 2 slice on 2026-09-03.
  # Blocked on Phase 2 landing: it removes the per-route enum pins that four of the
  # eight shipped route-contract refusals rest on, so it needs its own confinement
  # and compatibility-authority contract. Must land before the Codex route.
  {path = "docs/product/intents/distribution-route-set-opening.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/product/briefs/distribution-routes-programme.md", revision = "sha256-bytes-v1:b8daba8a937964f1cc37233cd613cdc8993d53bdcf64bc580ea7799e4741829d"}, summary = "Open the route set so a route needs no schema entry, with confinement for the values the per-route pins currently fix", needs = [{type = "local", kind = "spec", path = "docs/specs/distribution-route-registry/spec.md"}]},
  {path = "docs/product/intents/codex-plugin-route.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/product/briefs/distribution-routes-programme.md", revision = "sha256-bytes-v1:b8daba8a937964f1cc37233cd613cdc8993d53bdcf64bc580ea7799e4741829d"}, summary = "Emit native Codex plugin packages and marketplace with user-scope publication parity", needs = [{type = "local", kind = "intent", path = "docs/product/intents/canonical-mcp-install-parity.md"}, {type = "local", kind = "intent", path = "docs/product/intents/distribution-route-set-opening.md"}]},
  {path = "docs/product/intents/kiro-power-route-profile.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/product/briefs/distribution-routes-programme.md", revision = "sha256-bytes-v1:329d3aec010ffd0f0b090022bac7faf35b85f337d9b3c719ab8063b7c74dbc45"}, summary = "Add the Kiro Power route profile over portable package bytes", needs = [{type = "local", kind = "intent", path = "docs/product/intents/codex-plugin-route.md"}]},
  {path = "docs/product/intents/claude-manifest-migration-support-claims.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/product/briefs/distribution-routes-programme.md", revision = "sha256-bytes-v1:329d3aec010ffd0f0b090022bac7faf35b85f337d9b3c719ab8063b7c74dbc45"}, summary = "Migrate Claude manifest residue and publish machine-readable support claims", needs = [{type = "local", kind = "intent", path = "docs/product/intents/kiro-power-route-profile.md"}]},
  # Unblocks when the current Kiro contract is reacquired and the migration
  # receives its own approved spec.
  {path = "docs/product/intents/kiro-hook-schema-v1.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/codex-hook-adaptation-handoff/hook-adapter-audit.md", revision = "sha256-bytes-v1:3e6d9b52c6316d6fb13eacbd46737a12d5acd437b310ba1eb3d1876df959d8c1"}, summary = "Migrate direct Kiro hook projection to the standalone v1 schema", needs = []},
  # Unblocks when each runtime contract is reacquired and the output boundary is
  # approved.
  {path = "docs/product/intents/adapter-hook-context-output.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/codex-hook-adaptation-handoff/hook-adapter-audit.md", revision = "sha256-bytes-v1:3e6d9b52c6316d6fb13eacbd46737a12d5acd437b310ba1eb3d1876df959d8c1"}, summary = "Render session-start context through each adapter's documented output protocol", needs = []},
  # Unblocks when runtime root semantics are reacquired and the command strategy
  # is approved.
  {path = "docs/product/intents/root-stable-hook-dispatch.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/codex-hook-adaptation-handoff/hook-adapter-audit.md", revision = "sha256-bytes-v1:3e6d9b52c6316d6fb13eacbd46737a12d5acd437b310ba1eb3d1876df959d8c1"}, summary = "Make Claude Code and Codex repository hook dispatch root-stable", needs = []},
  {slug = "changelog-promote-marooned-entries", source = "rfc/0095 D3", summary = "Promote the 59 versioned changelog entries nested under [Unreleased] to free-standing `##` sections so they publish to /now/, separating them from the 48 interleaved genuinely-unreleased bare sections across three [Unreleased] regions; lower _MAROONED_RELEASE_BASELINE as they land, and restore Keep a Changelog ordering with [Unreleased] first"},
  {path = "docs/product/intents/route-decision-analysis-depth.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/distribution-route-registry/spec.md"}, summary = "Give the route-decision checker cross-module depth under its own contract: follow route values across module boundaries, derive the discriminator field set from the route types, cover projector-identity and .get() dispatch, and derive the single-route exemption from the registration the lookup resolves through -- each exemption pinned by a mutation. Split out of distribution-route-registry after five review rounds in which each widening opened the next round's hole", needs = []},
  {slug = "mcp-servers-from-package-registries", source = "rfc/0092 D7", summary = "Open an RFC for MCP servers acquired from npm, PyPI, or another registry: a typed acquisition descriptor extending the unused runtime-dependencies shape, an immutable-artifact-identity requirement before execution, a provenance trust policy, and a lifecycle-script policy"},
  {slug = "guide-sidebar-label-order-coupling", source = "spec/guide-metadata-completion AC (sidebar)", summary = "project_guide_sidebar orders kind buckets by resolved label, so adopting a curated title necessarily reorders its bucket"},
  # Unblocks when: someone decides whether the seal should admit a prose-only
  # plan edit. Discovered by spec/okf-follow-ons, whose plan corrections were
  # blocked mid-run and landed only after an owner-authorized cohort re-seal.
  {slug = "cohort-seal-blocks-in-phase-plan-correction", summary = "loop-engine applies `schedule check-current` to every CODE-* transition except `done`, so any plan.md edit blocks the rest of a run even though the plan contract and docs/CONVENTIONS.md require in-phase correction while Status is Executing; the only escape is a cohort re-seal that clears the review-retry and stasis counters and resets current_wave_index", source = "spec/okf-follow-ons (adversarial review rounds 11-12, tooling conflict)"},
  {slug = "agent-skill-engineering-guide-and-docsurl", summary = "agent-skill-engineering is the only pack in GUIDE_OPTIONAL_PACKS and its site docsUrl points at the guides index; the planned docs slice must add guides/agent-skill-engineering/, link it from guides/README.md, drop the exemption, and repoint docsUrl", source = "spec/agent-skill-engineering-foundation AC21 (owner decision 2026-08-27)"},
  # Unblocks when: the release checklist owner decides.
  {slug = "readme-pypi-whats-new-unenforced", summary = "Whether README-pypi must name the current release is undecided; 12 of the last 25 version bumps did not", source = "spec/guide-typed-asides-test-gate (review blocker 2)"},
  # Deferred rather than bundled: packages/agentbundle/ is a protected tree
  # (tools/lint-catalogue-curation-guard.py refuses without an `Engine-Change-
  # RFC:` trailer) and AGENTS.local.md requires a pyproject version bump for an
  # engine change. A config-drift fix should not become an engine release. The
  # parity gate anchors the literal instead, which catches divergence but not
  # the duplication.
  # Unblocks when: an engine-scoped RFC is open to cite in the trailer.
  {slug = "marketplace-description-fourth-statement-in-self-host", summary = "self_host.py restates the marketplace description a fourth time; removing it needs an Engine-Change-RFC and a version bump", source = "spec/marketplace-generator-single-source (governance gate)"},

  # Blocker: the fix touches protected packages/agentbundle/**; its landing
  # commit needs an Engine-Change-RFC trailer naming a real RFC.
  {slug = "marketplace-envelope-config-authority", summary = "Make catalogue.toml the marketplace-envelope authority for all six render.py entrypoints, after deciding toml_emit's 'main' scaffold default", source = "spec/marketplace-generator-single-source (review blocker 3)"},

  # Blocker: the fix touches protected packages/agentbundle/**; its landing
  # commit needs an Engine-Change-RFC trailer naming a real RFC.
  {slug = "output-drift-silent-without-dist", summary = "CAT-V-014 cannot fire in CI because _step_output_drift early-returns when dist/ is absent", source = "spec/marketplace-generator-single-source (assumption)"},

  # Blocker: the fix touches protected packages/agentbundle/**; its landing
  # commit needs an Engine-Change-RFC trailer naming a real RFC.
  {slug = "marketplace-ref-not-git-ref-validated", summary = "marketplace-entry.schema.json's ref and sha are unvalidated strings on an ingress path (archive.py, verify.py --root)", source = "spec/marketplace-generator-single-source (review concern 3)"},

  # Blocker: the fix changes protected packages/agentbundle/** and needs an
  # Engine-Change-RFC trailer naming a real RFC.
  {slug = "catalogue-branch-empty-falls-back-to-upstream-constant", summary = "An empty or absent claude-plugin-branch makes a fork's install route resolve to upstream's repo and ref", source = "spec/marketplace-generator-single-source (review concern 4)"},
  # Unblocks when: a maintainer runs the canary arm; settings writes are theirs.
  # Deferred rather than bundled: closing it needs a repository-settings action
  # and a live canary run, neither of which is a code change this PR could
  # carry.
  {slug = "publish-control-evidence-freshness-unbounded", summary = "Publish-control evidence has no freshness bound and the live-branch negative test was never run; a settings-side ruleset removal leaves every gate green", source = "spec/marketplace-generator-single-source (review concern 4)"},
  # Unblocks when: SAST rule authoring is in scope (sast-cwe-delta-review is
  # adjacent).
  # Deferred rather than bundled: bounding it needs a different instrument
  # entirely — a runtime assertion inside the build, or a semgrep rule — not
  # more static reading.
  {slug = "marketplace-envelope-post-import-rebind-unbounded", summary = "Neither parity layer bounds a post-import rebind of _DIST_BRANCH or _MARKETPLACE_DESCRIPTION", source = "spec/marketplace-generator-single-source (review residual)"},
  # Unblocks when: the publisher's import surface is next touched.
  # Deferred rather than bundled: the fix is to change the publisher's import
  # surface, which is publish-path code this PR deliberately does not touch.
  {slug = "marketplace-publisher-branch-layer-2-only", summary = "The publisher's BRANCH push target is covered by the parity gate's literal layer only, not the resolved-value layer", source = "spec/marketplace-generator-single-source (review concern 4)"},
  # lint-traceability.py classifies any `<dir>/<file>.md` producer pointer as a
  # cross-repository reference, so now that a spec's `Brief:` back-link is the
  # canonical path form it reports a brief that lives in THIS repo as "unknown /
  # not-yet-catalogued (cross-repo, unresolvable)" and drops the brief↔spec edge
  # it exists to generalize. Informational, exit 0, so no gate catches it. A
  # mistyped path is also only informational, where a mistyped bare slug has
  # always been a fatal dangling pointer — so path-form typos are detected less
  # strictly than slug-form ones. Fix: resolve a canonical local brief path to
  # its brief node before endpoint classification
  # (packs/core/.apm/skills/work-loop/scripts/lint-traceability.py). Attempted
  # and withdrawn during the brief-path provenance fix: the shipped
  # docs/specs/traceability-lint/spec.md makes this Ask-first ("changing a
  # producer-pointer field") and its Never-do list forbids identifying a node by
  # file path, hardcoding an artifact path, and treating a well-formed cross-repo
  # reference as dangling — and a naive alias hard-failed on brief files that
  # exist under a `[traceability].briefs` override. Unblocked by a reviewed
  # amendment to that spec carrying the Ask-first sign-off; no `needs` edge.
  {slug = "traceability-brief-path-backlink-unresolved", source = "spec-less brief-path provenance fix (review rounds 1 and 4)"},
  # lint-brief-coverage.py and lint-traceability.py both still match a bare-slug
  # `Brief:` back-link, so a slug-form spec passes every early gate and only
  # fails later at workspace-status reconciliation, where it blocks dispatch.
  # The docstrings now say the slug is backward-compatible only, but nothing
  # tells an author at lint time. Fix: have the coverage lint emit a non-fatal
  # note naming any spec whose back-link matched by slug rather than by path
  # (lint-brief-coverage.py, receive-brief skill). Deferred because it adds
  # output to a lint many repositories run in CI. Unblocked once the note is
  # agreed to be non-fatal and that lint's output contract is versioned; no
  # tracked dependency, so no `needs` edge.
  {slug = "brief-backlink-slug-form-needs-lint-note", source = "spec-less brief-path provenance fix (review concern 5)"},

  {path = "docs/product/intents/ci-gate-main-runtime-budget.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/ci-gate-parallelization/notes/ci-gate-parallelization-critical-path-measurement.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Bring gate-main within its shipped 200-second budget after a docs-only run measured 302 seconds and localized 201 seconds to two steps", needs = []},
  {path = "docs/specs/ci-gate-parallelization/notes/ci-gate-parallelization-critical-path-measurement.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/specs/ci-gate-parallelization/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Preserve the frozen deferral identity and completed docs-only CI measurement while the 302-second gate-main miss moves to its runtime-budget intent", needs = []},

  {path = "docs/specs/local-ci-orchestration/notes/pre-existing-enterprise-agentbundle-full-suite.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/specs/local-ci-orchestration/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Run the complete AgentBundle suite in a supported profile that permits its isolated trust-store and key-material fixtures", needs = []},
  {path = "docs/specs/rfc0088-round15-reference-consumer/notes/rfc0088-native-addon-confinement-bypass.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/specs/rfc0088-round15-reference-consumer/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Test native-addon confinement before any supported RFC-0088 configuration is permitted to grant Node's addon flag", needs = []},
  {path = "docs/specs/rfc0088-round15-reference-consumer/notes/rfc0088-r12-fact-negative-tests-red.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/specs/rfc0088-round15-reference-consumer/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Reacquire the authoritative out-of-tree round-12 negative-test apparatus and record a current-versus-clean-checkout differential", needs = []},

  {path = "docs/product/intents/sso-browser-state-protection.md", kind = "intent", source = {mode = "repo-origin", ref = "workspace.toml", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Persistent SSO browser state is time-bounded, minimally stored, serialized across browser launches, and protected by measured keychain and recapture budgets", needs = []},
  {path = "docs/product/intents/credential-pack-defaults-projection.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/jira-check-sso-auto-login/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Credential-pack catalogue defaults reach standard-library-only scripts without becoming trusted sign-in or automatic-refresh authority", needs = []},
  {path = "docs/product/intents/sso-cookie-lint-contract-wording.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/jira-check-sso-auto-login/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Catalogue lint wording forbids automatic SSO setup while preserving the shipped non-mutating check carve-out", needs = []},
  {path = "docs/product/intents/workspace-mcp-production-readiness.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/workspace-mcp/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Workspace-MCP reaches production readiness through Stage 1 behavioral proof, non-breaking Claude permission projection, and canonical brief-queue exposure", needs = []},
  {path = "docs/specs/jira-check-sso-auto-login/notes/sso-recapture-audit-sink.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/specs/jira-check-sso-auto-login/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Preserve the frozen recapture-audit deferral identity while its outcome is owned by the SSO contention and audit-trail intent", needs = []},
  {path = "docs/specs/jira-check-sso-auto-login/notes/pack-dependency-scope-qualifier.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/specs/jira-check-sso-auto-login/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Preserve the frozen pack-dependency qualifier identity while its outcome is owned by agentbundle install integrity", needs = []},
  {path = "docs/specs/jira-check-sso-auto-login/notes/browser-state-lifetime.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/specs/jira-check-sso-auto-login/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Preserve the frozen browser-state lifetime identity while its outcome is owned by SSO browser-state protection", needs = []},
  {path = "docs/specs/jira-check-sso-auto-login/notes/pack-config-catalogue-sso-defaults.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/specs/jira-check-sso-auto-login/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Preserve the frozen catalogue SSO-defaults identity while its outcome is owned by credential-pack defaults projection", needs = []},
  {path = "docs/specs/jira-check-sso-auto-login/notes/sso-broker-register-concurrency.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/specs/jira-check-sso-auto-login/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Preserve the frozen broker-registration concurrency identity while its outcome is owned by SSO browser-state protection", needs = []},
  {path = "docs/specs/jira-check-sso-auto-login/notes/sso-broker-at-rest-minimisation.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/specs/jira-check-sso-auto-login/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Preserve the frozen at-rest minimisation identity while its outcome is owned by SSO browser-state protection", needs = []},
  {path = "docs/specs/jira-check-sso-auto-login/notes/sso-register-pretooluse-hook.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/specs/jira-check-sso-auto-login/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Preserve the frozen optional-hook identity and its retired disposition unless a future accepted intent reactivates it", needs = []},
  {path = "docs/specs/jira-check-sso-auto-login/notes/sso-recapture-cooldown.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/specs/jira-check-sso-auto-login/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Preserve the frozen recapture-cooldown identity while its outcome is owned by SSO browser-state protection", needs = []},
  {path = "docs/specs/jira-check-sso-auto-login/notes/sso-cookie-lint-phrase-amendment.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/specs/jira-check-sso-auto-login/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Preserve the frozen cookie-lint wording identity while its outcome is owned by the SSO cookie-lint contract intent", needs = []},
  {path = "docs/specs/sso-store-transition-serialization/notes/sso-keychain-call-timeouts.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/specs/sso-store-transition-serialization/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Preserve the frozen keychain-timeout identity while its outcome is owned by SSO browser-state protection", needs = []},
  {path = "docs/specs/tracker-refresh-writeback/notes/tracker-refresh-projection-repair-confirmation.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/specs/tracker-refresh-writeback/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Preserve the frozen projection-repair confirmation identity while its outcome is owned by trusted tracker-refresh inputs", needs = []},
  {path = "docs/specs/tracker-refresh-writeback/notes/tracker-refresh-enforced-capability-state.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/specs/tracker-refresh-writeback/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Preserve the frozen capability-state identity while its outcome is owned by trusted tracker-refresh inputs", needs = []},
  {path = "docs/specs/tracker-refresh-writeback/notes/tracker-refresh-materialized-child-scope.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/specs/tracker-refresh-writeback/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Preserve the frozen materialized-child scope identity while its outcome is owned by trusted tracker-refresh inputs", needs = []},
  {path = "docs/specs/workspace-mcp/notes/workspace-mcp-stage1-behavioral-tests.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/specs/workspace-mcp/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Preserve the frozen Stage 1 behavioral-test identity while its outcome is owned by Workspace-MCP production readiness", needs = []},
  {path = "docs/specs/workspace-mcp/notes/workspace-mcp-permissions-projection-contract.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/specs/workspace-mcp/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Preserve the frozen permission-projection identity while its outcome is owned by Workspace-MCP production readiness", needs = []},
  {path = "docs/specs/workspace-mcp/notes/workspace-mcp-brief-queue-exposure.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/specs/workspace-mcp/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Preserve the frozen brief-queue exposure identity while its outcome is owned by Workspace-MCP production readiness", needs = []},
  {path = "docs/specs/stale-reference-corrections/notes/packs-agents-normative-pointer.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/specs/stale-reference-corrections/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Preserve the frozen packs-pointer identity and its path-free disposition without copying a repository-only schema into the scaffold", needs = []},

  {path = "docs/specs/kiro-install-alias-parity/notes/upgrade-orphan-removal-on-projection-shape-change.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/specs/kiro-install-alias-parity/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Preserve the upgrade-orphan deferral identity while its accepted intent owns confinement-safe projection cleanup", needs = []},

  {path = "docs/specs/digital-experience-contract/notes/digital-experience-contract-pe-journey-xref.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/specs/digital-experience-contract/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Add the Digital Experience Contract cross-reference while the product-engineering shaping doctrine updates and regenerates its journey", needs = []},

  {path = "docs/specs/convenient-install-defaults/notes/convenient-install-defaults-followons.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/specs/convenient-install-defaults/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Preserve the default-source integrity deferral identity while its accepted intent owns commit pinning and archive verification", needs = []},

  {path = "docs/specs/apm-install-route-parity/notes/apm-install-route-parity.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/specs/apm-install-route-parity/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Record the outstanding APM core, converters, Copilot, Cursor, and Gemini live-install checks in manual-QA matrix rows 32 through 34", needs = []},

  {path = "docs/specs/house-voice-writing-craft/notes/apm-leak-lint-rfc.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/specs/house-voice-writing-craft/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Retire the obsolete APM leak-lint RFC proposal after the concrete sweep shipped and catalogue verification replaced the deleted standalone linter", needs = []},

  {path = "docs/specs/agentic-well-architected-overlay/notes/ml-saas-serverless-workload-class-lenses.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/specs/agentic-well-architected-overlay/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Remove the unsupported SaaS workload-lens claim while preserving the backed GenAI, data and ML, and serverless routes", needs = []},

  {path = "docs/specs/atlassian-sso-cookie/notes/atlassian-sso-cookie-live-dc-read-transcript.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/specs/atlassian-sso-cookie/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Capture the approved corporate-SSO Atlassian Data Center read flow in a credential-free transcript", needs = []},
  {path = "docs/specs/agentbundle-wheel-release/notes/artifactory-first-publish-gesture.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/specs/agentbundle-wheel-release/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Have an authorized owner configure the Artifactory Actions secrets and record the first tagged package upload without exposing credentials", needs = []},
  {path = "docs/specs/extraction-msg-to-markdown-python-contract/notes/extraction-msg-realworld-sample.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/specs/extraction-msg-to-markdown-python-contract/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Run the shipped message converter against approved PII-free real-world MSG and EML samples and record both manual fidelity reviews", needs = []},

  {path = "docs/product/intents/role-journey-agent-swarm-section.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/product/journeys/engineer-scales-to-swarm.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Extend the core Agent role journey with coordinated headless CLI swarm guidance after the source journey has an approved contract", needs = []},

  {path = "docs/specs/claude-plugins-manifest-correctness/notes/plugin-root-name-collision-guard.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/specs/claude-plugins-manifest-correctness/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Preserve the plugin-root collision deferral identity while its accepted intent owns the contract-derived pre-write guard", needs = []},

  # Deliberately NOT backlogged: extending SAST_DIRS to web/ (62 JS/TS/Astro
  # files). Decided 2026-08-07 — web/ is the platform site, built and deployed
  # from this repo only; it is never installed into an adopter repo, so it
  # carries none of the shipped-artifact exposure that motivates the entries
  # above. Revisit only if web/ ever gains a server-side runtime.

  {path = "docs/product/intents/capture-work-alias-removal.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/work-intake-migration-docs/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Remove the capture-work alias and legacy workspace reader only after every RFC-0083 compatibility, notice, audit, rollback, specification, and authorization gate is satisfied", needs = []},
  {path = "docs/product/intents/tracker-refresh-trusted-authority-inputs.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/tracker-refresh-writeback/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Tracker refresh receives trusted scope, lifecycle and capability state, and explicit projection-repair confirmation before affected writes", needs = []},
  {path = "docs/product/design/workspace-anchor-staleness-invariant.md", kind = "design", source = {mode = "repo-origin", ref = "docs/rfc/0083-work-intake-and-artifact-routing.md", revision = "sha256-bytes-v1:8176272b14747e07e70beb752d6b709ccac502c284a9c114ae03c0cb48312a9d"}, summary = "Design detection for dependency or membership changes that stale a queued spec's approved assumptions", needs = []},

  {path = "docs/specs/catalogue-corporate-trust-store/notes/catalogue-trust-store-trust-settings.md", kind = "defect", source = {mode = "repo-origin", ref = "docs/specs/catalogue-corporate-trust-store/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Preserve the Never Trust deferral identity while its accepted intent owns macOS trust-settings enforcement", needs = []},
  # ---------------------------------------------------------------------------
  # Tech-site design review 2026-08-14 — deferred findings + implementation
  # surfacings.
  #
  # Source: an in-session design review of both site surfaces (`web/` marketing
  # at `/`, `docs-site/` Starlight docs at `/docs/`) rendered at 1440x900 and
  # 360/375/390/414 phone widths in light and dark, benchmarked against an
  # external reference docs site (supplied in-session; deliberately not named
  # in-tree). The review produced 2 blockers, 7 majors, 8 minors. The mechanical
  # subset shipped as spec/tech-site-polish-batch; these are the rest, plus
  # three items that only surfaced while implementing it.
  #
  # The completed wayfinding cluster has left this open queue. The remaining
  # entries are deferred review findings, implementation surfacings, and design
  # work that must be shaped before specification.
  # ---------------------------------------------------------------------------

  # 142 of 189 guides carry no frontmatter at all, so they have no `summary:`
  # and therefore no page-specific meta description — Starlight falls back to
  # the site-level description on every one of them, which means no distinct
  # social-preview text, no search-engine snippet, and no Pagefind result
  # snippet. spec/tech-site-polish-batch built the plumbing (build-site.py maps
  # `summary:` -> Starlight `description`, and the PageTitle override renders it
  # as a visible deck), and 47 guides already benefit; this item is the content
  # backfill for the remaining 142. Deliberately NOT auto-generated from first
  # paragraphs — that was declined during the batch because it would ship 142
  # unreviewed strings into meta tags and social previews. Each needs a human
  # one-liner. Consider batching by pack. Files: guides/**/*.md frontmatter.
  # Unblocks when: ready now.
  # Merged 2026-08-17 into spec/guide-metadata-completion.

  # The marketing site and the docs site are two visually disconnected products.
  # At /docs/ the Starlight chrome replaces everything: the header loses the
  # marketing nav (leaving title + search + GitHub icon) and the footer loses
  # every link. The reference keeps one header and one footer across both
  # surfaces, so a docs reader is one click from every top-level destination.
  # Also: the marketing nav renders "Docs ↗" with an external-link arrow and a
  # dimmer colour than its siblings, which reads as leaving the product. Fix:
  # a shared header band and shared footer across both surfaces; drop the ↗ and
  # match sibling colour. HAZARD: docs-site/AGENTS.md explicitly forbids aligning
  # docs colour to web/'s amber system without a new spec, and the two palettes
  # are deliberately separate (see spec/docs-site-design-refresh). Shared chrome
  # must either stay palette-neutral or amend that rule through an RFC — do not
  # quietly work around it.
  # Unblocks when: ready now, but confirm the palette question first.
  # Merged 2026-08-17 into spec/site-shared-chrome after RFC-0089 and ADR-0085.

  # tools/lint-guide-titles.py (added by spec/tech-site-polish-batch) found 9
  # guides whose frontmatter title and body H1 disagreed. The H1s were
  # RECONCILED to match `title:` — not deleted. Deletion was considered and
  # rejected: guides/_shared/** ships verbatim into adopter catalogues and
  # bundles where frontmatter never renders, so a deleted H1 leaves those files
  # opening with no heading at all. Reconciling means the frontmatter title is
  # what publishes, and in most cases that is the WEAKER of the two strings:
  # "Scaffold a Component" beat "Tutorial: Scaffold a component from a screen
  # brief"; "Run an Audit" beat "How-to: Run a frontend-engineering audit";
  # "Frontend Engineering Pack" beat "`frontend-engineering` — the skills and
  # the reviewer"; "IaC (Terraform) guides" beat "`iac-terraform` — guides".
  # Two of the nine went the other way (the longer string won). No content is
  # lost — the old H1s are in git history at the batch's commit.
  # Fix: pick the better wording for those 9 and put it in `title:`. Files: the
  # 9 guides listed in that commit. Watch: guide-nav-baseline.toml resolves
  # sidebar labels from a frozen baseline before falling back to the page title,
  # so a title change may not move the sidebar label — check both.
  # Unblocks when: ready now.
  # Merged 2026-08-17 into spec/guide-title-clarity.

  # Test coverage landed for tools/lint-guide-titles.py and build-site.py's
  # leading_h1 / _strip_leading_h1 / _strip_guide_metadata transforms
  # (tools/test_lint_guide_titles.py, 33 assertions, wired into the docs
  # workflow). What is still uncovered: the rehype table-wrapper in
  # docs-site/src/plugins/rehype-scrollable-tables.ts has no unit test — its
  # only coverage is the built-output assertion in
  # web/src/test/rendered-output.test.ts, which proves the emitted HTML but not
  # the hast-level edge cases (already-wrapped table, table inside an aside or
  # blockquote, root-level table). A unit test needs a test runner in
  # docs-site/, which is a new dependency and therefore an AGENTS.md
  # dependency-table entry — hence deferred rather than bundled.
  # Unblocks when: ready now; decide whether docs-site earns a test runner.
  # Merged 2026-08-17 into spec/docs-site-build-contract-hardening.

  # Docs pages carry a large number of interactive elements under the WCAG 2.2
  # 24x24 target-size minimum — 40-47 on the docs landing, 12-26 on content
  # pages at 360-414px. Many are inline links in prose (exempt under the
  # inline-in-text carve-out) and several are Starlight-owned (`.sl-anchor-link`
  # heading anchors measure 24x35). Needs a design pass to separate the genuine
  # failures from the exempt and the framework-owned before any fix is specced —
  # which is why this is shaping, not build. Note the marketing surface was
  # already fixed in spec/tech-site-polish-batch (drawer links went from 17px to
  # 44px full-width rows).
  # Unblocks when: ready now.
  # Merged 2026-08-17 into spec/site-browser-quality-gate; the canonical audit
  # scaffold is that spec's notes/docs-tap-target-audit.md.

  # There is no docs/design/principles/ artefact for either site surface, so the
  # in-session design review could trace none of its findings to a project
  # principle — every finding had to map to the accessibility quality floor, a
  # recognized usability heuristic, or a genre rubric, and pure aesthetic calls
  # had to be quarantined as unarbitrable "director's notes". Without this, taste
  # arguments about the two surfaces have no arbiter and every future review
  # re-litigates them. Known open questions it should settle: whether the two
  # palettes stay deliberately divergent (amber/dark-hero marketing vs cobalt/
  # cool-neutral docs — currently mandated by docs-site/AGENTS.md); whether the
  # docs surface should use display serif at H1/H2 scale at all; and whether the
  # product wants an illustrative register (it currently has none).
  # Unblocks when: ready now.
  # Closed 2026-08-17 to docs/design/principles/tech-site.md.
  {path = "docs/product/intents/selftest-untracked-plant-window.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/lint-performance-p0/spec.md", revision = "sha256-bytes-v1:9bf95e7308260b96ba5456eb61aab8d5ab45b9fe32091c3092571ef02c724a0e"}, summary = "Remove the live-worktree plant window without losing end-to-end catalogue boundary proof", needs = []},

  # Surfaced while specing knowledge-enquiry-scope-reachability, and required by
  # that spec's round-2 review (Concern 12) before it can pin selection-level
  # reachability. The topic corpus is too coarsely scoped for a bounded envelope:
  # 17 of 76 topics apply corpus-wide (14 exclusively) and 20 share the base
  # `tools`, so a `tools` query has 20 matches tied at one specificity tier against
  # a 12-body envelope and the tiebreak decides. Measured 69 of 76 topics returned
  # across the committed query set; 7 are starved. No ranking rule fixes this — it
  # is scope granularity, not matching. Fix: curate scopes so the largest tier fits
  # the envelope, most likely by narrowing `tools` and re-scoping corpus-wide topics.
  # Unblocks when: knowledge-enquiry-scope-reachability ships, since its matcher
  # decides which topics land in which tier.
  {path = "docs/product/intents/knowledge-topic-scope-granularity.md", kind = "intent", source = {mode = "repo-origin"}, summary = "Curate topic scopes so a specificity tier fits the bounded enquiry envelope", needs = []},
  {path = "docs/product/intents/spec-review-validation-guidance.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/spec-authoring-discipline/spec.md", revision = "local-2026-08-28"}, summary = "Align standalone new-spec review with existing measurement and adjudication controls, add finding-origin attribution, and surface lint gate scope", needs = []},

  # PINNED, like the 34 marker-pinned entries above, but by a different mechanism:
  # `tests/roster/test_credential_broker_contract_docs.py::test_ac42_roadmap_entry_carries_manual_qa_matrix`
  # asserts this exact slug is present in workspace.toml AND that the six rows below
  # appear within 600 characters before it. That test is the oracle for AC42 of the
  # shipped credential-broker-contract spec, whose body is frozen, so the entry cannot
  # be absorbed into a slice without orphaning a ticked acceptance criterion.
  # Its subject matter is otherwise the same as the `live-credential-transcripts`
  # slice, which records the relationship.
  # Unverified: six real platform-by-mode transcripts remain under
  # docs/specs/credential-broker-contract/notes/:
  # `creds` × macOS; `creds` × Windows; `creds` × Linux;
  # `sso-cookie` × macOS; `sso-cookie` × Windows; `sso-cookie` × Linux.
  # Settle when every row uses its real platform store and an approved corporate-SSO
  # endpoint, and records PAT resolution / sso-broker test exit 0.
  {slug = "credential-broker-contract-manual-qa"},

  # ── Slices absorbed from the legacy register ──────────────────
  # Each entry replaces one or more retired legacy entries. The retired entry's
  # problem, recorded fix, blocker and unblock condition live in the named intent's
  # `## What this absorbs` section, keyed by its original slug. One entry is one
  # working session.
  #
  # FOLLOWING A REFERENCE TO A RETIRED SLUG. A few comments under `packs/` still cite
  # a retired slug's old location. The identifier still resolves: every absorbed slug
  # is kept as a `### <slug>` heading in its intent, so `grep -r '<slug>'
  # docs/product/intents/` finds the work. Those sit under `packs/`, one in a
  # protected tree whose landing commit needs an `Engine-Change-RFC:` trailer.
  {path = "docs/product/intents/adopter-catalogue-test-command.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/adr/0071-pack-runtime-export-boundary-and-test-placement.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "Catalogue adopters can run the tests they receive through one canonical command that discovers pack and conformance suites from the ADR-0071 layout, preserves one process per skill, and keeps test dependencies development-only", needs = []},
  {path = "docs/product/intents/agent-skill-engineering-guide-slice.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/agent-skill-engineering-foundation/spec.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "The agent-skill-engineering pack has a dedicated guide that its site record links to", needs = []},
  {path = "docs/product/intents/agentbundle-install-artifact-layout.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/engine-export-boundary/spec.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "`agentbundle install` preserves compatible installation artifacts without pack conflicts", needs = []},
  {path = "docs/product/intents/agentbundle-install-integrity.md", kind = "intent", source = {mode = "repo-origin", ref = "workspace.toml", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "`agentbundle install`, uninstall, and upgrade preserve installed-pack integrity through dependency provenance, serialized state changes, and pre-render pack refusal", needs = []},
  {path = "docs/product/intents/agentbundle-verify-fail-open-closure.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/agentbundle-engine-stragglers/spec.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "`agentbundle catalogue verify` reports bounded diagnostics whenever pack-schema validation or version-parity inputs cannot be validated", needs = []},
  {path = "docs/product/intents/argv-path-boundary-validator-sweep.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/pack-script-root-boundary-validation/spec.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "The remaining argv-to-path boundaries use the repository validator before untrusted command-line values become filesystem paths", needs = []},
  {path = "docs/product/intents/authoring-doctrine-reseal-and-replication.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/rfc/0099-cut-before-adding-and-artifact-shaping.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "Maintainers can govern authoring contracts through evidence-backed, RFC-0099-compliant rules", needs = []},
  {path = "docs/product/intents/binder-publishing-build-spec.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/architecture/binder-publishing/README.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "The planned binder-publishing design has an implementation build specification beyond its existing gate-propagation contract", needs = []},
  {path = "docs/product/intents/brief-backlink-provenance-lints.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/traceability-lint/spec.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "Brief backlink lints resolve canonical local paths and advise authors when legacy slug form remains in use", needs = []},
  {path = "docs/product/intents/catalogue-archive-guide-corpus.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/documentation-entry-navigation/spec.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "Source-flavor catalogue archives include the adopter guide corpus under a decided archive layout and prove the package and install contract", needs = []},
  {path = "docs/product/intents/changelog-marooned-entry-promotion.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/rfc/0095-changelog-entry-obligation.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "Every released changelog entry is a free-standing version section that publishes to `/now/`, while genuinely unreleased content remains under the first `[Unreleased]` heading", needs = []},
  {path = "docs/product/intents/checked-then-reopened-path-confinement.md", kind = "intent", source = {mode = "repo-origin", ref = "workspace.toml", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "Confinement-sensitive operations hold verified directory or file objects rather than reopening a path that a concurrent writer can replace", needs = []},
  {path = "docs/product/intents/ci-parity-linter-scope.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/local-gate-ci-parity/spec.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "Each newly admitted CI workflow has a local-correspondence check and a workflow-specific exemption inventory", needs = []},
  {path = "docs/product/intents/claude-marketplace-pack-identity.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/claude-plugin-route-scope/spec.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "Each Claude marketplace pack has an independently identifiable and recoverable content digest compatible with the upstream marketplace schema", needs = []},
  {path = "docs/product/intents/credential-broker-bounded-modes-and-errors.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/jira-check-sso-auto-login/spec.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "Credential-broker capture and refresh expose explicit operator modes and bounded caller-facing failures", needs = []},
  {path = "docs/product/intents/credential-operator-guide.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/architecture/credentials.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "An operator can store a credential by following a dedicated credential-broker setup guide", needs = []},
  {path = "docs/product/intents/digital-product-maker-profile.md", kind = "intent", source = {mode = "repo-origin", ref = "workspace.toml", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "Digital product makers have a portable profile for the complete four-discipline toolkit", needs = []},
  {path = "docs/product/intents/discovery-sidecar-drift-hard-fail.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/rfc/0048-autonomous-product-team-operating-model.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "Traceability lint rejects a discovery sidecar whose on-disk edge set drifts from `_state/traceability.json`", needs = []},
  {path = "docs/product/intents/experience-reviewer-content-brief-scope.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/rfc/0062-content-design-and-copy-direction-skills.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "`experience-reviewer` reviews the content briefs produced by `content-design`", needs = []},
  {path = "docs/product/intents/frozen-record-errata-mechanism.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/pack-test-boundary-remaining-packs/spec.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "Maintainers can add dated, additive corrections to frozen ADRs and related frozen records without rewriting historical bodies, and classify the maintained lifecycle of distribution adapters", needs = []},
  {path = "docs/product/intents/gates-that-read-clean-while-gating-nothing.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/semgrep-selftest-batching/spec.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "Each affected gate rejects its declared failure condition instead of reporting a clean result that proves nothing", needs = []},
  {path = "docs/product/intents/growth-strategy-pack-charter.md", kind = "intent", source = {mode = "repo-origin", ref = "workspace.toml", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "The company OS has a charter decision that determines whether a dedicated growth strategy pack can be scoped and proposed", needs = []},
  {path = "docs/product/intents/guide-sidebar-label-ordering.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/guide-metadata-completion/spec.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "Maintainers can adopt a curated guide title without unintentionally reordering the guide's sidebar kind bucket", needs = []},
  {path = "docs/product/intents/installed-skill-governance-inventory.md", kind = "intent", source = {mode = "repo-origin", ref = "workspace.toml", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "Installed skills have a declared governance record that accounts for content identity, scan status, and the skill version that instructed an agent action", needs = []},
  {path = "docs/product/intents/live-adapter-and-client-smoke-evidence.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/claude-plugin-route-scope/spec.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "Maintainers have dated, reproducible live-client evidence for generated adapter artifacts and client marketplace behavior, including recovery paths and stated client limitations", needs = []},
  {path = "docs/product/intents/live-credential-transcripts.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/atlassian-sso-cookie/spec.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "Credential and SSO-cookie flows have the live platform evidence needed to complete their deferred validation decisions", needs = []},
  {path = "docs/product/intents/loop-state-lock-cross-spec-serialization.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/loop-cohort-state-lock/spec.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "Work-loop state recovery and its repo-global event outbox can be operated concurrently without one spec's transition mutating another spec's in-flight state, with the lock failure and reclaim cases proved by deterministic evidence", needs = []},
  {path = "docs/product/intents/marketplace-config-single-authority.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/marketplace-generator-single-source/spec.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "Maintainers can set the marketplace branch and description in `catalogue.toml`, and every public render path uses those values after the `toml_emit.py` main-scaffold default has been decided", needs = []},
  {path = "docs/product/intents/marketplace-ingress-validation.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/marketplace-generator-single-source/spec.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "Marketplace ingress rejects invalid Git references and empty configured branch values before a fork can resolve an install route to upstream", needs = []},
  {path = "docs/product/intents/native-platform-verification-coverage.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/product/intents/remote-ci-verification-parity.md"}, summary = "Native Windows and macOS verification gets a coverage contract worth dispatching: host-dependent checks, an explicit expected-skip set, and a classification enforced only where a mechanism can decide it", needs = []},
  {path = "docs/product/intents/roster-suite-parallel-execution.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/product/intents/remote-ci-verification-parity.md"}, summary = "The roster suite runs 2.5x faster in parallel but at least one case reads the live working tree, so a peer worker reds a clean tree: measured 540s serial vs 218s at four workers, wait-bound rather than CPU-bound, no lease contention, and test_workspace_status_projection.py confirmed non-isolated", needs = []},
  {path = "docs/product/intents/dispatch-workflow-posture-assertions.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/product/intents/remote-ci-verification-parity.md"}, summary = "The two dispatch-only workflows have no posture assertions of their own: actionlint and zizmor verify their present state but nothing pins their trigger set, absent inputs, read-only token, timeout or runner label against drift, and the repo-wide excessive-permissions pass is scoped to three named files with the broad scanner floor above that finding class", needs = []},
  {path = "docs/product/intents/new-spec-review-phrase-contract.md", kind = "intent", source = {mode = "repo-origin", ref = "workspace.toml", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "The new-spec review protocol and its remote gate agree on the exact clean-result phrases that determine acceptance", needs = []},
  {path = "docs/product/intents/pack-eval-rollout-completion.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/rfc/0037-pack-activation-evals.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "Eligible pack skills have stable behavior coverage and the remaining pack-eval rollout decisions are ready for their next dedicated slices", needs = []},
  {path = "docs/product/intents/pack-javascript-ci-workflow.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/pack-test-boundary-remaining-packs/spec.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "JavaScript living in pack skills has CI coverage, committed lockfiles, dependency auditing, and no stale runner exemptions", needs = []},
  {path = "docs/product/intents/packages-marker-rename-sweep.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/packages-governance-marker-sweep/spec.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "Every retained runtime governance-marker string has its matching pinning assertion updated in the same reviewed change", needs = []},
  {path = "docs/product/intents/plugin-publish-trust-boundary.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/claude-plugin-route-scope/spec.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "Marketplace publication has an independently reviewable actor boundary, an approver preview, and current live-control evidence", needs = []},
  {path = "docs/product/intents/project-knowledge-retryable-capture-handling.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/ci-gate-parallelization/spec.md", revision = "79b23d2944ef2e4e534ae26331c4393aacd7360a"}, summary = "Workflows handle retryable project-knowledge capture timeouts through a bounded recovery path without silent loops or changed diagnostic semantics", needs = []},
  {path = "docs/product/intents/pytest-tmpdir-worktree-attribution.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/rfc/0097-agent-skill-engineering.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "Pytest temporary trees can be attributed to their worktree and reclaimed when that worktree is dead, while single-checkout behavior remains unchanged", needs = []},
  {path = "docs/product/intents/registry-acquired-mcp-servers.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/rfc/0092-first-class-distribution-routes.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "MCP servers acquired from package registries have an approved acquisition and execution trust model", needs = []},
  {path = "docs/product/intents/required-check-and-deploy-ordering.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/ci-gate-parallelization/spec.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "Repository controls preserve trustworthy pull-request gating and current Pages publication", needs = []},
  {path = "docs/product/intents/rfc0088-browser-follow-on-specs.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/rfc/0088-web-pilot-foundation.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "The RFC-0088 browser pilot has a complete set of three separately scoped follow-on specifications for foundation delivery, behavior results, and repair tooling", needs = []},
  {path = "docs/product/intents/rfc0088-exposure-and-privacy-anchoring.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/rfc/0088-web-pilot-foundation.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "RFC-0088's browser-endpoint exposure and cross-round term identity claims have evidence at their stated boundaries", needs = []},
  {path = "docs/product/intents/rfc0088-signing-and-cost-observations.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/rfc0088-round12-consumer-shaped-residuals/spec.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "RFC-0088 has the missing dated observations for signing-identity update survival and destination-group interactive sign-in cost", needs = []},
  {path = "docs/product/intents/sast-scanner-coverage-expansion.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/pack-script-root-boundary-validation/spec.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "Shipped JavaScript receives Semgrep and CodeQL analysis, and the repository can make CodeQL findings merge-blocking when remote branch protection permits it", needs = []},
  {path = "docs/product/intents/scanner-suppression-and-pin-hygiene.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/build-check-coverage-gaps/spec.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "SAST and npm-audit suppressions and the package installs that support their gates have explicit, durable integrity and expiry controls", needs = []},
  {path = "docs/product/intents/skill-description-semantic-drift-gate.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/local-gate-ci-parity/spec.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "Skill descriptions retain valid activation and handoff semantics as the catalogue and Codex skills-context budget evolve", needs = []},
  {path = "docs/product/intents/sso-store-contention-and-audit-trail.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/sso-store-transition-serialization/spec.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "An unattended SSO re-authentication can retain its recapture record, recover correctly from short store contention, and have its macOS keychain timing evidenced", needs = []},
  {path = "docs/product/intents/test-runner-boundary-completion.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/build-check-coverage-gaps/spec.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "Every in-scope tool and end-to-end test has an invoked runner or an explicit, recorded no-runner contract", needs = []},
  {path = "docs/product/intents/tracker-writeback-result-contracts.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/tracker-refresh-writeback/spec.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "Tracker write-back boundaries reject receipt-shaped impostors and expose deliberate, closed result-code vocabularies for each processor result type", needs = []},
  {path = "docs/product/intents/workflow-posture-harness-consolidation.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/specs/ci-gate-parallelization/spec.md", revision = "768c0a90b87e635220c3c784d7cdae67644d7e1d"}, summary = "Workflow-posture checks use a maintainable shared harness and structural workflow parsing where appropriate, while each workflow's distinct security assertions and mutation contract remain intact", needs = []},

]

# Resolved repo-durable items. Kept rather than deleted so a reader who arrives
# from a stale reference finds the disposition instead of a silent gap.
closed = [
]

# ─────────────────────────────────────────────────────────────────────────────
# ini-007 · Catalogue Contracts, Composition, Semantics, and Discovery
#
# Create one converged catalogue information model and one clear discovery
# system for five audiences: enterprise catalogue maintainers (including
# air-gapped), marketing evaluators, platform engineers, repository engineers,
# and coding agents. Introduces the optional [[pack.integrations]] convention,
# a portable authoring-standards hub, a bundled-contract inspection surface,
# a deterministic neutral catalogue index, and audience-specific information
# architecture across the docs site, marketing site, and repository README.
#
# Governing RFC: RFC-0076 (to be authored and accepted before schema changes).
# Source brief: .context/attachments/HZQyuH/pasted_text_2026-07-29_13-27-49.txt
#
# Non-goals: hosted registry, publisher namespaces, exact version selectors,
# multi-version dependency solving, subjective trust scores, automatic
# integration dispatch, executable `when` expressions, CI workflow generation.
# ─────────────────────────────────────────────────────────────────────────────
["ini-007"]
name      = "Catalogue Contracts, Composition, Semantics, and Discovery"
status    = "active"
milestone = "M2 · Authoring Discovery + Information Architecture"

["ini-007".work]
active  = [
  {path = "docs/specs/direct-skill-repository-installation/spec.md", kind = "spec", source = {mode = "repo-origin", ref = "docs/rfc/0098-direct-skill-repository-installation.md", revision = "38e017322df0a622ffba530116f97f204da594c2"}, summary = "Admit bounded direct skill repositories through the canonical AgentBundle install lifecycle", needs = []},
]
shipped = [
  {path = "docs/specs/okf-authoring-projection/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "Project pack-local OKF knowledge into deterministic portable Skills", needs = []},
  {path = "docs/specs/catalogue-verifier-correctness/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "catalogue-verifier-correctness", needs = [{type = "local", kind = "spec", path = "docs/specs/catalogue-wave2-pack-integrations/spec.md"}, {type = "local", kind = "spec", path = "docs/specs/catalogue-wave3-enterprise-authoring-discovery/spec.md"}]},
  {path = "docs/specs/lint-performance-p0/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "Batch git-ignore, reuse one lint inventory, and make lint falsification fixture-based", needs = []},
  {path = "docs/specs/governance-guides-cleanup/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "Governance guide reference cleanup", needs = []},
  {path = "docs/specs/catalogue-verify-classification/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "catalogue verify classification", needs = []},
  {path = "docs/specs/pack-test-boundary-remaining-packs/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "pack-test-boundary-remaining-packs", needs = []},
  {path = "docs/specs/catalogue-wave1-contract-convergence/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "catalogue-wave1-contract-convergence", needs = []},
  {path = "docs/specs/catalogue-wave2-pack-integrations/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "catalogue-wave2-pack-integrations", needs = []},
  {path = "docs/specs/documentation-entry-navigation/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "documentation-entry-navigation", needs = []},
  {path = "docs/specs/catalogue-source-identity/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "Define adapter-neutral local catalogue source identity", needs = []},

  {path = "docs/specs/catalogue-wave3-enterprise-authoring-discovery/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "Expose bundled contracts for offline enterprise authoring", needs = [{type = "local", kind = "spec", path = "docs/specs/catalogue-wave2-pack-integrations/spec.md"}]},
  {path = "docs/specs/rendered-site-link-debt/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "Repair rendered internal links and enforce generated-site link closure", needs = []},
  {path = "docs/specs/okf-catalogue-discovery/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "Expose bounded live OKF metadata through AgentBundle catalogue inspection", needs = []},

  {path = "docs/specs/catalogue-wave4-semantic-contracts-index/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "Catalogue Wave 4 — Semantic contracts and neutral index", needs = [{type = "local", kind = "spec", path = "docs/specs/catalogue-wave1-contract-convergence/spec.md"}, {type = "local", kind = "spec", path = "docs/specs/catalogue-verifier-correctness/spec.md"}]},
  {path = "docs/specs/pack-test-compatibility-classes/spec.md", kind = "spec", source = {mode = "repo-origin"}, summary = "Keep pack tests isolated by default and permit proven same-pack compatibility classes", needs = []},
]
queue   = [
]

["ini-007".brief_queue]
executing = []
ready     = []
draft     = [
  {path = "docs/product/briefs/catalogue-discovery-and-release-integrity.md", kind = "brief", source = {mode = "repo-origin", ref = "docs/rfc/0076-catalogue-contracts-composition-semantics-discovery.md", revision = "a03b9d3f8df15a9b88cdabda5c10f21c662bfd0f"}, summary = "Frame the remaining catalogue discovery and release-integrity outcomes as one Draft delivery brief", needs = []},
]

["ini-007".shaping_queue]
active = []
backlog = [
  {path = "docs/product/intents/catalogue-search-verb.md", kind = "intent", source = {mode = "repo-origin", ref = "workspace.toml", revision = "a03b9d3f8df15a9b88cdabda5c10f21c662bfd0f"}, summary = "Expose a catalogue search verb over the neutral semantic index", needs = [{type = "local", kind = "spec", path = "docs/specs/catalogue-wave4-semantic-contracts-index/spec.md"}]},
  {path = "docs/product/intents/catalogue-wave5-release-integrity.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/rfc/0076-catalogue-contracts-composition-semantics-discovery.md", revision = "a03b9d3f8df15a9b88cdabda5c10f21c662bfd0f"}, summary = "Protect catalogue releases with digests, comparisons, and same-version mutation refusal", needs = [{type = "local", kind = "spec", path = "docs/specs/catalogue-wave4-semantic-contracts-index/spec.md"}]},
  {path = "docs/product/intents/catalogue-wave6-technical-docs-ia.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/rfc/0076-catalogue-contracts-composition-semantics-discovery.md", revision = "a03b9d3f8df15a9b88cdabda5c10f21c662bfd0f"}, summary = "Create an author-focused technical documentation route for building a catalogue", needs = [{type = "local", kind = "spec", path = "docs/specs/catalogue-wave4-semantic-contracts-index/spec.md"}, {type = "local", kind = "spec", path = "docs/specs/documentation-entry-navigation/spec.md"}]},
  {path = "docs/product/intents/catalogue-wave7-marketing-evaluator.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/rfc/0076-catalogue-contracts-composition-semantics-discovery.md", revision = "a03b9d3f8df15a9b88cdabda5c10f21c662bfd0f"}, summary = "Give prospective adopters an evaluator-oriented catalogue marketing surface", needs = [{type = "local", kind = "intent", path = "docs/product/intents/catalogue-wave6-technical-docs-ia.md"}]},
  {path = "docs/product/intents/catalogue-wave9-migration-closeout.md", kind = "intent", source = {mode = "repo-origin", ref = "docs/rfc/0076-catalogue-contracts-composition-semantics-discovery.md", revision = "a03b9d3f8df15a9b88cdabda5c10f21c662bfd0f"}, summary = "Close the first-party catalogue migration after release and discovery outcomes are complete", needs = [{type = "local", kind = "spec", path = "docs/specs/catalogue-wave2-pack-integrations/spec.md"}, {type = "local", kind = "intent", path = "docs/product/intents/catalogue-wave5-release-integrity.md"}, {type = "local", kind = "intent", path = "docs/product/intents/catalogue-wave7-marketing-evaluator.md"}, {type = "local", kind = "spec", path = "docs/specs/documentation-entry-navigation/spec.md"}]},
]
# ─────────────────────────────────────────────────────────────────────────────
# ini-009 · Agent Skill Engineering
# Authority: RFC-0097 (Accepted; implementation waits for approved specs).
# Portable workflows + governed knowledge are the product. AgentBundle
# packaging, projections, versions, and publication remain external mechanics.
#
# Staged-delivery constraint (routed from the 2026-08-27 distillation of an
# observation captured at RFC-0097's adversarial-review-clean gate):
# advertised workflow modes, required knowledge topics, and validation fixtures
# must share the same milestone boundary. Two failure directions follow, and
# they pull opposite ways, so each milestone needs both checked:
#   - a later-stage fixture must not gate an earlier foundation release, and
#   - a mode that is not yet available must not be advertised as complete.
# Check per milestone that it lists its own advertised modes, required topics,
# and fixtures, and that no fixture cites a mode belonging to a later
# milestone. This binds M0 onward, not only the first release.
# ─────────────────────────────────────────────────────────────────────────────
["ini-009"]
name      = "Agent Skill Engineering"
status    = "active"
milestone = "M3 · slice 4 consumer integrations shipped; 3c and 3e are unblocked and parallel, 3d needs 3c, 5 needs 3c, 6 closes — see the brief's slice table"

["ini-009".brief_queue]
executing = [
  { path = "docs/product/briefs/agent-skill-engineering.md", kind = "brief", source = { mode = "repo-origin", ref = "docs/rfc/0097-agent-skill-engineering.md", revision = "sha256-bytes-v1:bcafd5c77e8a849d831b8a8a47aaeeda24f35d124e423ec28ccacb0146c23098" }, summary = "Deliver the agent-skill-engineering pack, corpus, integrations, and repository adaptation", needs = [] },
]
ready = []
draft = []

["ini-009".shaping_queue]
active  = []
backlog = []

["ini-009".work]
active  = [
]
queue   = [
]
shipped = [
  { path = "docs/specs/agent-skill-engineering-consumer-integrations/spec.md", kind = "spec", source = { mode = "repo-origin", parent = "docs/product/briefs/agent-skill-engineering.md" }, summary = "Wire work-loop and architect-design to the installed agent-skill-engineering provider with a bounded request-inlining step, and publish the seam's diagnostic vocabulary to an installed surface", needs = [] },
  { path = "docs/specs/agent-skill-engineering-composition-floors/spec.md", kind = "spec", source = { mode = "repo-origin", ref = "docs/product/briefs/agent-skill-engineering.md", revision = "sha256-bytes-v1:f4ecebf39306f44b06cee660db055899ade9dbc71bf563196c844537e8a94063", parent = "docs/product/briefs/agent-skill-engineering.md" }, summary = "Admit the three portable composition floors and a probe-verified Claude Code pilot profile, and build the runtime capability-claim ledger with its lifecycle states and profile roll-up", needs = [{ type = "local", kind = "spec", path = "docs/specs/agent-skill-engineering-languages-and-execution/spec.md" }] },
  { path = "docs/specs/agent-skill-engineering-languages-and-execution/spec.md", kind = "spec", source = { mode = "repo-origin", ref = "docs/product/briefs/agent-skill-engineering.md", revision = "sha256-bytes-v1:fef790246776e6716ee7eb94e4aad9c2805a8a608ced3f0917676fa4ad59803b", parent = "docs/product/briefs/agent-skill-engineering.md" }, summary = "Add language-specific and execution-economics corpus depth after the corpus baseline ships", needs = [{ type = "local", kind = "spec", path = "docs/specs/agent-skill-engineering-corpus/spec.md" }] },
  { path = "docs/specs/agent-skill-engineering-corpus/spec.md", kind = "spec", source = { mode = "repo-origin", ref = "docs/product/briefs/agent-skill-engineering.md", revision = "sha256-bytes-v1:fef790246776e6716ee7eb94e4aad9c2805a8a608ced3f0917676fa4ad59803b", parent = "docs/product/briefs/agent-skill-engineering.md" }, summary = "Deliver the governed corpus, skill-pattern census, topology, retrieval baseline, and knowledge-provider authoring mode", needs = [] },
  { path = "docs/specs/agent-skill-engineering-foundation/spec.md", kind = "spec", source = { mode = "repo-origin", ref = "docs/product/briefs/agent-skill-engineering.md", revision = "sha256-bytes-v1:42fe891e902f906db83b55c12a67d5362589afd9b00765aeeb5632a0b2d14e9f", parent = "docs/product/briefs/agent-skill-engineering.md" }, summary = "Build the portable agent-skill-engineering foundation and close its two OKF compiler prerequisites", needs = [] },
]
