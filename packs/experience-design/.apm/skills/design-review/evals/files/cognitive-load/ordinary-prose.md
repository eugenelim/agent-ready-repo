The main task is clear, but the save state needs work. Users cannot tell when a save is done, and an error has no retry action.

Add a saved label and a retry button first. The rest of the flow can stay as it is.
