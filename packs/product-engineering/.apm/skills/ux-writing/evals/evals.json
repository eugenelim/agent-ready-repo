{
  "skill_name": "ux-writing",
  "evals": [
    {
      "id": 1,
      "prompt": "Write the UI microcopy for the states the user named.",
      "expected_output": "UI copy where error states are blame-free (describe the situation, never fault the user) and every error and empty state offers a next action (no dead ends); buttons/CTAs are verb+object matching the user's goal (not 'Submit'/'OK'); labels are concise, front-loaded, and terminology-consistent (one term per concept). Tone fits each context (calm and plain in an error, warmer in success) while the product voice stays constant. It shapes words only — no visual/layout decisions and no documentation prose.",
      "assertions": [
        "Error copy is blame-free — describes the situation, never faults the user",
        "Every error and empty state offers a next action — no dead-end copy",
        "Buttons / CTAs are verb + object matching the user's goal, not generic ('Submit' / 'OK')",
        "Labels are concise, front-loaded, and terminology-consistent (one term per concept)",
        "Tone fits the context (calm in errors, warmer in success) while the voice stays constant",
        "Shapes words only — no visual/layout decisions and no documentation prose"
      ]
    },
    {
      "id": 2,
      "prompt": "Write the empty-state copy for a 'no projects yet' screen and the error-state copy for a failed save. A brand register exists at docs/design/copy/brand-register.md — use it as the upstream voice input.",
      "expected_output": "UI copy for the empty state (orient + first action) and error state (blame-free + next step), shaped by the voice loaded from the fixed path docs/design/copy/brand-register.md — not re-derived from a type: tone-of-voice marker scan.",
      "assertions": [
        "Resolves the brand register from the fixed path docs/design/copy/brand-register.md (or the configured output_dir equivalent) — does not scan for type: tone-of-voice markers",
        "Empty-state copy orients the user (what belongs here) and invites a first action — no decorative dead end",
        "Error-state copy is blame-free (describes the situation, not user fault) and offers a next step",
        "Output is UI copy strings, not copy direction or brand-level register content"
      ]
    }
  ]
}
