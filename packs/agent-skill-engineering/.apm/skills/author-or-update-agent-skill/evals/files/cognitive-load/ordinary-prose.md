Use the frame mode first. It names the skill's boundary before any file is written, so a wrong shape costs a paragraph rather than a tree.

The change adds one skill and one reference file. It does not alter an existing activation description.

All 12 skill-spec checks pass in 3 seconds. No next step is needed.
