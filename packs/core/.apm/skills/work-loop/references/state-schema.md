# Work-loop state

> **SUPERSEDED — schema, verbs, and gating behaviour.** The pre-Phase-1 model
> (per-session lifetime, shared `iteration_count`, `check --phase plan` expecting
> exit 1, `plan_review_status` gating all phases) is fully replaced by the Phase-1
> design in `docs/architecture/loop-infrastructure.md`, which is the authoritative
> reference for field definitions, verb surface, guard semantics, and session
> resumption. This file will be rewritten as `docs/architecture/loop-infrastructure.md`
> lands and PR #816 ships. **Do not use any part of this file as implementation
> guidance.**
