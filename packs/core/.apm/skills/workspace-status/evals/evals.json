[
  {
    "id": 1,
    "prompt": "workspace status",
    "expected_output": "The agent invokes the workspace-status backend (python3 <skill-dir>/scripts/workspace_status.py --root <root>) and surfaces the initiative queue from the JSON output. For this fixture: one active initiative ('Example Initiative'), one queued item (docs/specs/example-feature/spec.md not yet started), empty active/shipped lists, no reconciliation findings. The agent does NOT re-derive readiness or reconciliation independently — all classification data comes from the backend JSON.",
    "assertions": [
      "Invokes the backend script rather than independently parsing workspace.toml or recomputing the DAG",
      "Reports the active initiative name from the JSON output ('Example Initiative')",
      "Reports queue state: the queued spec is blocked or not-yet-started (the spec file is absent — no spec.md exists in the fixture)"
    ],
    "files": ["evals/files/workspace.toml"],
    "expect": {
      "produces": [],
      "output_contains": ["Example Initiative"],
      "output_excludes": ["Traceback", "workspace-status error:"]
    }
  }
]
