{"id": "K-0001", "kind": "gotcha", "scope": "packages/agentbundle/agentbundle/build/**", "title": "Render-to-temp + copy breaks merge/splice on-conflict against the working tree", "body": "If a projection renders into a fresh temp dir and the rendered output is then copied into the working tree, any merge-with-existing semantics in the projecting code (merge-managed-key-only for JSON, preserve-outside-block for managed-block-inline) sees only the empty temp dir — the merge target is always blank — so the working tree's unrelated keys / outside-block content gets clobbered on the copy step. For real-write modes, project DIRECTLY into the working tree so the merge/splice code sees the existing state. For dry-run, clone the relevant subset of the working tree into a shadow temp dir first, project into the clone, then diff clone vs working tree. Bit us in the post-EXECUTE review of RFC-0001's distribution-adapters spec; the unit tests passed because they re-ran into the same output dir (which carried the prior render), but the integrated --self path broke silently.", "source": "PR#distribution-adapters / commit bab5a9d"}
{"id": "K-0002", "kind": "gotcha", "scope": "**/*.py", "title": "shutil.copytree follows symlinks by default — supply-chain hazard for published artifacts", "body": "`shutil.copytree(src, dst)` defaults to `symlinks=False`, which means symlinks in `src` are DEREFERENCED — the link target's contents land in `dst`. Same for `shutil.copy2` (default `follow_symlinks=True`). When projecting source files that may be attacker-controlled (third-party packs, plugin submissions, untrusted catalogues) into a published output tree, a malicious symlink to `/etc/passwd` exfiltrates the build host's secrets into the artifact. Pass `symlinks=True` to copytree and `follow_symlinks=False` to copy2 — they preserve symlinks as symlinks rather than dereferencing. Caught by the security-reviewer pass on RFC-0001's distribution pipeline; not exploitable today (packs are repo-owned) but lethal the day production packs from PRs are admitted.", "source": "PR#distribution-adapters / commit d3ebacc"}
{"id": "K-0003", "kind": "pattern", "scope": "packages/agentbundle/agentbundle/build/**", "title": "Document a JSON-Schema subset in spec + plan + validator docstring, all three in sync", "body": "A stdlib-only Python validator forces a documented subset (no $ref, no oneOf, etc.). The spec's AC needs to enumerate the subset; the plan's task Approach needs to enumerate it; the validator's module docstring needs to enumerate it. If any of the three drifts, a future agent extends one and not the others, and the next adversarial review surfaces it as a Blocker. Worth a single source of truth across docs + code from day one. Applies to any RFC where stdlib-only is a hard constraint and a subset of an external standard is the contract.", "source": "PR#distribution-adapters / commit bab5a9d"}
{"id": "K-0004", "kind": "gotcha", "scope": "*", "title": "Rename audits must enumerate docstring + comment + error-message sites, not just code constants", "body": "When a rename touches a name-that-is-an-identifier (filename, directory, package), the per-task line-level audit in the plan must list every string-literal occurrence — including module docstrings, header comments, assertion messages, help strings, and CLI-arg examples — not just code constants. The brief's `grep` Done-when test surfaces every site; the plan's per-task list is what executes against it. If the plan enumerates only constants, docstrings drift past EXECUTE and the post-EXECUTE adversarial-reviewer raises them as Blockers. Caught on PR#22 (relocate adapter contract to docs/contracts/): the round-1 plan listed only `CONTRACT_PATH` / `SCHEMA_PATH` constants in T3/T4; round-2 post-EXECUTE review found 14 docstring + assertion-message hits the audit had missed. Fix: when the reference taxonomy says 'Class X always rewrites', the per-task audit's grep must cover docstrings, comments, and message strings in the same sweep.", "source": "PR#22 / commit f791743"}
