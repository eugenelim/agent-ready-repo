# AGENTS.local.md — `packs/` (insider context; not exported with catalogue init)

This file covers self-hosting relationships and projection rules specific to this catalogue.
Fork contributors working on a downstream catalogue don't need this context.

## Self-hosting projection — insider notes

**Vendored copy:** `packs/credential-brokers/.apm/user-libs/credbroker/` is byte-synced from `packages/credbroker/credbroker/`. Edit the `packages/` source; never the `.apm/user-libs/` copy.

## Marketplace and release pipeline

The `marketplace.json` aggregate is regenerated by `make build-self` (or `FORCE=1 make build-self`
for dirty trees). Never edit it directly. A pack version bump requires both `pack.toml` and
`.claude-plugin/plugin.json` to match before `build-self` will accept the change.

`publish-claude-plugins.yml` is the upstream publish workflow; it runs only from `main` on
a workflow-dispatch or tag event. Forks replace this with their own publish mechanism.
