{
  "id": "linear-brief-sync",
  "name": "Linear Brief Sync",
  "version": "0.1.0",
  "description": "Compatibility route for the legacy Linear brief-sync trigger. Delegates the brief path and Linear source locator to the configured work-intake refresh processor; it owns no separate lifecycle, authority, diff, approval, local-write, or remote-mutation path.",
  "category": "workflows",
  "tags": ["linear", "brief", "refresh", "compatibility"],
  "license": "MIT",
  "deps": {
    "skills": [
      {
        "name": "work-intake",
        "source": "core pack",
        "purpose": "Owns configured refresh routing, authority, lifecycle, reviewed local updates, and separately confirmed remote mutations."
      }
    ],
    "system": []
  },
  "input": {
    "arguments": "an existing brief path and its Linear issue identifier"
  },
  "output": {
    "artifacts": []
  },
  "targets": {
    "default": { "file": "SKILL.md" }
  }
}
