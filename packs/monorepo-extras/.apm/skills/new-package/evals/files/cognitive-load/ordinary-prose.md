The new package is ready at packages/metrics. It has a typed public entry point, unit test folder, and build metadata.

It does not add a new runtime dependency. All 11 checks pass in 3 seconds.

The package is ready for code review.
