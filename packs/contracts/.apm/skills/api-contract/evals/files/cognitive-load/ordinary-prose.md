The API contract is ready. A client can create a note, read it, and delete it.

Bad input returns 400 with the field name. A missing note returns 404.

All 16 contract checks pass in 3 seconds. The next step is client review.
